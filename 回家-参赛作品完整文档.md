# 回家：用端云协同 AI 促成异地子女常回家陪伴

参赛作品完整文档

更新时间：2026-08-11
当前版本：GoHome `1.0.0 (16)`
Bundle ID：`com.gohome.family`

> 本文用于参赛提交与评审展示，重点说明产品定位、系统闭环、当前实现、验证证据和提交边界。内部问题台账见 `issue.md`；产品定义、实施顺序和实现记录分别见 `想家了吗-PRD.md`、`想家了吗-Plan.md`、`想家了吗-Implement.md`。

## 1. 项目摘要

GoHome 不是“远程看监控”的老人看护产品，而是一套把家庭状态转成联系理由、回家计划和异常处置证据的端云协同服务。它由树莓派 5 + Hailo-8 HAT+ 家庭守护盒、腾讯云业务平台和原生 iOS App 组成：边缘盒负责摄像头接入、视觉推理、隐私合成和本地事件生成，云端负责账号家庭、事件复核、媒体分发和消息推送，App 负责绑定、查看、确认和联系。

核心闭环是：

```text
家庭状态信号 -> 联系理由 -> 电话或消息 -> 回家计划 -> 真实陪伴
              -> 异常候选 -> 四帧复核 -> 家属确认 -> 事件复盘
```

当前项目已经完成真实树莓派双摄接入、Hailo 视觉推理、云端事件平台、H.264 / WHEP / WebRTC 实时媒体、阿里云百炼四帧复核协议、APNs 生产通道接入、iOS TestFlight Build 16 和主线文档闭环。它已经是一个可试点的家庭系统，但还不是可宣称“医疗级准确率”或“百分之百不漏报”的产品。

## 2. 产品要解决什么

居家养老的关键问题不是“有没有摄像头”，而是“家庭状态能不能变成真正的行动”。普通摄像头只能让子女看一眼，不能把天气、距离、老人近况、内容偏好和异常风险整理成可执行的联系理由；而真正发生风险时，单张截图也不足以解释老人是正常坐卧、弯腰、宠物经过、电视干扰，还是确实发生了跌倒。

GoHome 要解决两类问题：

1. 日常关系问题：今天为什么该联系、聊什么、什么时候该回家。
2. 安全托底问题：家属不盯直播时，系统能否在异常后提供可解释、可复核、可处置的证据。

所以，产品不把直播放在首页中心，也不鼓励全天盯守；首页负责提醒联系和回家，守护页负责查看实时状态，事件页负责处理异常证据。

## 3. 用户与使用流程

第一阶段面向三类用户：

| 用户 | 关注点 |
|---|---|
| 在家父母或老人 | 不想被复杂设备打扰，也不希望被持续监控 |
| 异地子女与家属 | 想知道家里是否平稳，收到异常时能快速判断并处理 |
| 安装和运维人员 | 负责盒子配网、摄像头接入、故障排查和验收 |

典型使用流程如下：

| 阶段 | 用户动作 | 系统动作 |
|---|---|---|
| 安装 | 盒子通电，手机登录 App，创建家庭，填写老人资料 | 云端建立家庭、成员、设备和规则关系 |
| 绑定 | App 绑定守护盒，摄像头由盒子在家庭局域网内接入 | 盒子主动从云端拉取配置，开始接入 RTSP 摄像头 |
| 定位 | 家庭创建者在家中确认一次家庭固定位置 | 手机当前位置只用于计算到家的距离，社区服务只按家庭位置取值 |
| 日常 | 查看天气、关怀卡、回家间隔和内容推荐 | 后台生成关怀内容、回家提醒和本地生活信息 |
| 守护 | 打开实时画面，切换原画、模糊或骨架模式 | 盒子生成隐私成品流，云端和 App 只显示，不二次绘制 |
| 异常 | 查看事件、确认安全、标记误报、联系老人 | 事件进入确认、排除、归档和模型反馈闭环 |

## 4. 核心功能

| 页面 | 作用 |
|---|---|
| 首页 | 呈现天气、家庭距离、上次回家、关怀内容和事件角标 |
| 守护 | 查看实时画面、切换三种隐私模式、查看双摄状态 |
| 事件 | 查看安全事件、关键帧、处理进度和家属确认结果 |
| 陪伴 | 管理联系计划、重要日期、家庭联系人和内容偏好 |
| 社区 | 提供家庭位置相关服务，不使用手机当前位置附近的服务替代家庭事实 |
| 我的 | 管理家庭资料、摄像头、通知、隐私和账号信息 |

三种隐私模式的定义必须保持一致：

| 模式 | 正式行为 |
|---|---|
| 原画 | 显示当前摄像头原始画面，不绘制骨架 |
| 模糊 | 保留当前场景，只模糊当前帧人物，不绘制骨架 |
| 骨架 | 保留非人物场景像素，移除人物像素，只绘制与当前帧同步的一套骨架 |

骨架模式的成品帧只能由盒子合成，不能由 App 或管理端二次绘制，也不能复用旧帧或跨摄像头混帧。

## 5. 总体技术架构

系统采用“家庭边缘盒 + 云平台 + 家属 App”的三层架构：

```text
RTSP 摄像头 / 后续传感器
        |
        | 家庭局域网
        v
树莓派守护盒
  - 摄像头唯一采集所有者
  - Hailo Pose / Object / Segmentation
  - 隐私画面唯一合成方
  - SQLite/WAL 事件与上传队列
  - H.264 发布与本地诊断
        |
        | HTTPS + 设备令牌 + WHEP / WebRTC
        v
腾讯云服务
  - 用户、家庭、老人、设备、摄像头、规则
  - 事件、媒体、模型任务、APNs、关怀卡
  - PostgreSQL、Nginx、MediaMTX、Coturn、systemd
        |
        | App Session + 短效媒体授权
        v
GoHome iOS App
  - 首页、守护、事件、陪伴、社区、我的
```

### 5.1 云边职责

| 层级 | 职责 |
|---|---|
| 盒子 | 摄像头接入、帧同步、Hailo 推理、隐私合成、事件候选、本地留存 |
| 云端 | 家庭与账号、事件复核、媒体访问、推送、消息、内容智能 |
| App | 登录、绑定、查看、确认、联系和配置 |

### 5.2 当前运行事实

| 层级 | 当前事实 |
|---|---|
| 盒子 | `/health` 显示 Pose、Object、Segmentation 均为 `ready`；两路摄像头保持 `streaming`，实时投递接近源流 15 fps，隐私成品约 14 fps，当前推理失败数为 0 |
| 云端 | `/health` 显示 PostgreSQL、事件 494、资产 1295、媒体访问已配置，WHEP / H.264 已接入，推送管道已接入 |
| App | TestFlight `1.0.0 (16)` 已安装可用，首页、记忆馆、陪伴页可正常浏览 |

## 6. 视觉算法与隐私模式

GoHome 的视觉链路不是“只看一个框”或“只跑一个姿态模型”，而是把摄像头、姿态、场景和时间序列统一放进盒子的单一流水线里处理。Hailo 负责高频推理，盒子负责统一合成，云端负责复核和通知。

关键原则是：

1. 单路摄像头只有一个采集所有者。
2. Pose、Object、Segmentation 共用同一 runtime 调度，不重复抢算力。
3. 骨架模式只输出一套同步骨架，不输出人物像素，不保留旧骨架。
4. 模糊模式只做当前帧人物模糊，不把它和骨架模式混成一类。
5. 宠物、电视、遮挡、半身、弯腰、蹲下和摄像头断流要分开处理，不互相污染。

### 6.1 跌倒不是“躺姿”

跌倒判断不是把“躺着”直接等于“危险”。盒子会先维护人形、姿态、场景、宠物和时间序列，再由规则层判断是否进入事件候选。正常坐下、弯腰、沙发躺卧、宠物经过、电视人物和画面冻结都不能直接替代跌倒结论。

### 6.2 当前可证明的效果

当前 `/health` 反映的不是“论文成绩”，而是当前工程链路的健康状态：

| 指标 | 当前值 |
|---|---|
| Pose runtime | `ready`，当前失败数为 0，近期 P95 约 40ms |
| Object runtime | `ready`，当前失败数为 0，近期 P95 约 47ms |
| 摄像头 31 | `streaming`，实时投递约 15 fps，隐私成品约 14 fps |
| 摄像头 32 | `streaming`，实时投递约 15 fps，隐私成品约 14 fps |

这些数字说明链路在当前家庭环境下是活的，但不等于真实家庭准确率已经完成最终验收。

## 7. 四帧多模态复核与事件闭环

安全候选事件不直接变成用户警报。盒子会从风险发生前后挑选四张按时间排序的证据帧：

- `before`
- `transition`
- `evidence`
- `current`

云端通过阿里云百炼的 OpenAI-compatible 接口调用 `qwen3.7-plus-2026-05-26`，四帧作为一次序列提交。模型必须返回严格 JSON，结果分四级：

| 结果 | 产品行为 |
|---|---|
| `confirmed` | 进入 App 待处理并推送 |
| `suspected` | 进入 App 待处理并推送，提示家属确认 |
| `rejected` | 只留痕，不推送 |
| `uncertain` | 只留痕，不推送 |

只有 `confirmed` 和 `suspected` 会打扰家属。`rejected` 和 `uncertain` 可以进入系统事件留存，用于后续复盘和模型迭代，但不应该变成警报。

事件闭环是：

```text
边缘候选 -> 本地留存 -> 云端复核 -> 推送 -> 家属确认 -> 恢复 / 误报 -> 复盘
```

## 8. 数据安全、隐私与留存

GoHome 采用边缘优先原则。家庭视频首先在盒子内分析，云端不做长期连续录像，只接收事件、关键帧、短视频和必要的结构化结果。实时视频采用短效媒体授权和 WHEP 会话，不把家庭摄像头裸露给 App。

当前留存设计包括：

- 盒子本地 SQLite / WAL、对象和上传队列按环形留存，接近容量上限时按最老数据循环回收。
- 严重未解决事件证据保留更长，普通事件和临时上传按类别分别设定保留策略。
- 云端 COS、本地对象和 PostgreSQL 资产按事件和家庭权限隔离。
- 第三方多模态模型只接收事件证据图，隐私政策必须明确服务商、用途和保留策略。

## 9. 当前实现与验证证据

### 9.1 当前能力状态

| 能力 | 状态 |
|---|---|
| 树莓派双摄接入 | 已跑通 |
| Hailo 视觉推理 | 已跑通，Pose / Object ready |
| 实时媒体 | H.264 + WHEP / WebRTC 主链路已接入 |
| 三种隐私模式 | 已定义并按盒子唯一合成收口 |
| 四帧复核 | 已确定为百炼 `qwen3.7-plus-2026-05-26` 协议 |
| iOS App | Build 16 已安装验证 |
| 事件与通知 | 云端事件、媒体和 APNs 已接入 |
| 数据留存 | 本地和云端生命周期已收口 |

### 9.2 当前证据索引

本次提交使用的证据以当前仓库的最新采集结果为准。正文不再复用 7 月旧图作为当前成果图。

| 证据 | 说明 |
|---|---|
| `report-evidence/13-ios-build15-home.png` | 当前首页样式截图 |
| `report-evidence/mobile-memory_gallery.png` | 当前记忆馆截图 |
| `report-evidence/mobile-companionship.png` | 当前陪伴页截图 |
| 盒子 `/health` | 当前双摄、Pose、Object、帧率健康信息 |
| 云端 `/health` | 当前事件量、资产量、媒体链路和推送健康信息 |

## 10. 行业机会与商业化

GoHome 面向的是真实而长期的家庭场景，不是一次性的演示系统。第一阶段先验证 20-50 户家庭试点，再逐步走向量产、订阅和社区协作。

| 阶段 | 重点 | 验收目标 |
|---|---|---|
| 试点期 | 设备稳定、误报收口、通知到达 | 20-50 户家庭试点 |
| 产品期 | 量产盒、订阅、远程诊断、OTA | 可复制家庭版产品包 |
| 扩展期 | 物业、社区、摄像头厂商合作 | 多场景接入和服务闭环 |

可选收入包括硬件销售、设备租赁、云服务订阅、高级复核、安装服务、售后诊断和社区服务对接。

## 11. TRAE 开发实践与参赛信息

TRAE / Codex 参与的不是一次页面生成，而是贯穿产品定义、盒子部署、Hailo 算法、云端服务、iOS TestFlight、文档和问题台账的持续工程协作。项目里形成的关键做法是：

1. 先在 `issue.md` 记录问题、根因、处理和验收条件，再动代码。
2. 每个核心能力都区分代码验证、真实设备验证、真机验证和家庭试点验证。
3. 盒子、云端和 App 的职责边界收敛为“盒子唯一合成，云端和 App 只显示”。
4. 旧 MJPEG、公网 JPEG、客户端骨架绘制和默认管理密码都逐步退出正式产品。

### 11.1 参赛与体验信息

| 项目 | 当前值 |
|---|---|
| 参赛标题 | 硬件交互｜回家：用树莓派守护盒和 AI 提醒子女常回家 |
| 当前版本 | GoHome `1.0.0 (16)` |
| 正式 iOS 工程 | `ios-shell/GoHomeShell.xcodeproj` |
| 体验地址 | 以最终确认的 GoHome 线上地址为准，旧入口不再作为提交口径 |

### 11.2 提交前还要确认的边界

| 项目 | 状态 |
|---|---|
| 公开体验地址 | 待最终确认 |
| 演示视频 | 待补拍 |
| App Store 提交材料 | 待收口 |
| 首页与社区文案 | 持续优化 |

## 12. 社会价值与项目总结

GoHome 的价值不在于把家庭关系做成一个更复杂的监控面板，而在于把天气、距离、节日、兴趣、家庭状态和上次回家时间整理成一次电话、一条消息和一次真实回家。它要证明的是：当家人分隔两地时，技术能不能安静地守住安全底线，并把“看见”变成“行动”。

当前项目已经从概念原型推进到真实树莓派、Hailo、双摄、云端、百炼模型、APNs 和 iPhone TestFlight 的系统闭环。接下来要做的不是继续堆概念，而是把当前证据、体验、视觉和提交材料统一收口。

## 附录 A：当前能力状态表

| 模块 | 当前状态 |
|---|---|
| 守护盒 | 树莓派 5 + Hailo-8 HAT+ |
| 视觉推理 | Pose / Object ready |
| 实时媒体 | H.264 / WHEP / WebRTC |
| 四帧复核 | 百炼 `qwen3.7-plus-2026-05-26` |
| iOS | TestFlight Build 16 |
| 云端 | PostgreSQL / MediaMTX / Coturn / APNs |

## 附录 B：提交材料索引

| 文件 | 用途 |
|---|---|
| `issue.md` | 产品交付问题台账、根因和验收条件 |
| `想家了吗-PRD.md` | 产品定义与阶段边界 |
| `想家了吗-Plan.md` | 实施路线与验收顺序 |
| `想家了吗-Implement.md` | 当前真实实现与部署记录 |
| `report-evidence/` | 当前截图、证据和演示素材 |
