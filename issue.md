# GoHome 产品交付问题台账

更新时间：2026-08-04
适用范围：树莓派 5 + Hailo-8 HAT+ 盒子、生产云端、盒子管理端、正式 GoHome iOS App
正式 iOS 工程：`ios-shell`
正式 Bundle ID：`com.gohome.family`

## 处理规则

- 状态只使用：`待处理`、`处理中`、`待实机验收`、`已关闭`、`阻塞`。
- `已关闭` 必须同时具备：根因消除、自动回归通过、运行指标达标、产品文档同步、真机验收通过。
- 禁止用 UI 隐藏、异常兜底、重复管线或降低标准代替根因修复。
- 盒子是隐私画面的唯一合成方；云端、管理端和 App 只传输和显示完整成品帧。
- 旧 WebView “回家” iOS 工程不参与开发、构建、测试或 TestFlight。

## 隐私模式产品契约

| 模式 | 唯一正确行为 |
| --- | --- |
| `original` | 显示未经隐私处理的当前摄像头画面，不绘制骨架。 |
| `person_blur` | 保留当前场景，只模糊当前帧中的人物，不绘制骨架。 |
| `skeleton` | 保留当前场景，完全移除当前帧中的全部人物像素，只绘制一套与该帧同步的实时骨架。 |

骨架模式禁止出现：模糊人物、黑底、中性色块、矩形替换、修复扭曲、历史人物像素、人物残影、双重骨架、跨摄像头像素、客户端二次绘制、旧帧倒退。

## 当前交付门槛

| 编号 | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| GH-001 | P0 | PRD、Plan、Implement 将“模糊 + 骨架”错误记录为正式骨架模式 | 已关闭 |
| GH-002 | P0 | 盒子骨架渲染直接调用人物模糊，违反产品契约 | 待实机验收 |
| GH-003 | P0 | 背景校准、持久化与场景变化状态机不完整 | 待实机验收 |
| GH-004 | P0 | Pose、人物掩码、视频帧缺少不可分割的同帧完成契约 | 待实机验收 |
| GH-005 | P0 | iOS 骨架流出现“服务器返回无法识别的响应” | 已关闭 |
| GH-006 | P0 | 云端同时保留 Pose SSE、safe-scene 和合成 MJPEG 三条骨架路径 | 待实机验收 |
| GH-007 | P0 | 多路并发 JPEG 上传产生乱序、忙丢帧和大量云端 stale reject | 待实机验收 |
| GH-008 | P0 | 双摄真人骨架模式尚未通过无残影、无泄漏、无串流验收 | 待处理 |
| GH-009 | P1 | 每个摄像头独占分割 HEF runtime，Hailo 运行时所有权重复 | 待实机验收 |
| GH-010 | P1 | OpenCV/FFmpeg 解码、CPU 合成和 JPEG 链路存在重复复制与编码 | 处理中 |
| GH-011 | P1 | 当前端到端仅约 6.5-8.5 FPS，明显低于约 12-15 FPS 的实际源流 | 处理中 |
| GH-012 | P1 | 独立旧视频服务与现行云中继重叠，并暴露已失效的丢帧控制 | 待实机验收 |
| GH-013 | P1 | SQLite、快照、上传队列、COS 对象没有统一留存清单和对账任务 | 处理中 |
| GH-014 | P1 | 推送需要事件级幂等、单次通知、回执和延迟监控的生产复验 | 待实机验收 |
| GH-015 | P1 | 家庭固定位置、手机当前位置、回家距离、社区家庭位置需闭环复验 | 待实机验收 |
| GH-016 | P1 | 宠物类别和人物误检没有正式标注集、混淆矩阵及阈值验收 | 待处理 |
| GH-017 | P2 | 管理端展示由公式推导的“准确率”，容易被误认为模型实测指标 | 待处理 |
| GH-018 | P2 | iOS `AppEnvironment` 集中装配过多接口和兼容上传逻辑 | 待处理 |
| GH-019 | P2 | 本地与盒子部署包含备份、旧入口、旧权重和生成物，缺少清理清单 | 待处理 |
| GH-020 | P0 | 尚未形成可重复的双摄长时、断线、隐私、跌倒、推送和 TestFlight 交付报告 | 待处理 |
| GH-021 | P1 | 摄像头 DHCP 地址变化后 App 无法原位编辑连接配置，只能误导用户删除重建 | 待实机验收 |
| GH-022 | P0 | 摄像头连接配置更新后旧读取器继续运行，数据库状态与实际采集源分裂 | 处理中 |
| GH-023 | P0 | 冻结和重复画面仍被计入实时 FPS，旧姿态结果没有随源停滞失效 | 待实机验收 |
| GH-024 | P0 | 管理端算法诊断骨架错误依赖全局隐私模式，并可能用 Tracker 旧像素替代当前视频 | 处理中 |
| GH-025 | P0 | Tracker 短暂丢失目标时把旧 `coasting` 骨架绘制到后续新视频帧，形成残留骨架 | 待实机验收 |
| GH-026 | P0 | 电视、灯光等短暂变化会删除持久空房校准，重启后骨架流失效 | 处理中 |
| GH-027 | P0 | 隐私模式只有全局期望值，没有逐路就绪校验和原子生效状态 | 处理中 |
| GH-028 | P0 | 隐私渲染异常被静默吞掉，HTTP 保持成功但没有任何成品帧 | 处理中 |
| GH-029 | P0 | 近黑新帧会被旧画面替换，却沿用新帧身份和时间戳 | 待实机验收 |
| GH-030 | P0 | Hailo Pose、分割和物体模型共用低频串行整管线，真实 Pose 锚点仅约 1-2 Hz | 处理中 |
| GH-031 | P1 | 源解码、有效输入、端侧合成、云端接收和客户端显示 FPS 口径混用 | 处理中 |
| GH-032 | P1 | 管理诊断流与隐私成品流骨架颜色、粗细和语义不统一 | 处理中 |
| GH-033 | P1 | 正式实时链路仍是逐帧 JPEG/MJPEG，缺少持久编码视频传输 | 处理中 |
| GH-034 | P1 | 盒子入口、Worker 和 Storage 职责过度集中，生产与历史云业务混杂 | 处理中 |
| GH-035 | P0 | 自动回归未覆盖重启、电视变化、逐路校准差异和 TestFlight 长时真实链路 | 处理中 |
| GH-036 | P1 | 盒子生产接口仍提供模拟天气和本地用户/家庭/消息等云端职责 | 处理中 |
| GH-037 | P0 | 盒子管理端保留固定默认密码且默认不强制修改，登录缺少失败限流 | 待实机验收 |
| GH-038 | P0 | 应用和模型升级包缺少签名/校验强制验证，归档解压存在目录穿越与任意执行风险 | 待实机验收 |
| GH-039 | P1 | 盒子残留云端媒体上传接口，存在未定义变量、整包入内存和双数据源问题 | 待实机验收 |
| GH-040 | P1 | 盒子残留 macOS LaunchAgent、旧 Web App 链接和直连 APNs 等非生产职责 | 处理中 |
| GH-041 | P0 | 当前骨架显示错误复用跌倒证据门槛，遮挡或半身场景直接丢弃可用 Pose | 处理中 |
| GH-042 | P1 | EACP 风险图和姿态规则由手工阈值堆叠，缺少版本化数据集与量化验证 | 处理中 |
| GH-043 | P0 | 火灾功能仅使用暖色纹理启发式，却以“火灾应急报警”产品语义暴露 | 处理中 |
| GH-044 | P1 | 管理端保留三套失效运行时和历史跳转页面 | 待实机验收 |
| GH-045 | P1 | 管理端保留失效算法剧场和无依据百分比指标 | 待实机验收 |
| GH-046 | P0 | 暖色启发式仍进入正式安全事件与通知链路 | 待实机验收 |
| GH-047 | P0 | 安全升级后管理登录仍显示失效默认密码 | 已关闭 |
| GH-048 | P0 | 算法诊断画面与用户隐私模式语义混合 | 待实机验收 |
| GH-049 | P1 | 未校准纯骨架仍持续消耗人物分割算力 | 已关闭 |
| GH-050 | P0 | 切换摄像头时旧 MJPEG 像素会显示在新摄像头名称下 | 待实机验收 |
| GH-051 | P0 | 公网媒体平面缺少正式鉴权与部署边界 | 处理中 |
| GH-052 | P0 | 云端发布安装器使用相对 unit 路径且中途失败不回滚 `current` | 处理中 |

## GH-001 文档产品契约错误

**现象**：产品文档把人物模糊底图叠加骨架记录为正式完成方案。
**根因**：骨架隐私的视觉定义没有成为跨端唯一契约。
**处理**：同步修正 PRD、Plan、Implement，废止 Build 8 的错误完成结论。
**验收**：三份文档对三种模式的定义完全一致，并明确盒子是唯一合成方。

## GH-002 骨架实现调用人物模糊

**现象**：骨架模式与模糊模式视觉相同，只多一套骨架。
**根因**：`PrivacyFrameRenderer._render_skeleton()` 直接调用 `_render_person_blur_for_camera()`。
**处理**：建立独立的纯骨架合成器，不复用模糊模式输出。
**验收**：人物区域没有任何原始或模糊人物像素，场景非人物区域保持当前帧，且只有一套骨架。

## GH-003 背景模型污染和伪造区域

**现象**：人物残影、旧人物矩形、家具纹理扭曲、黑场或中性色块。
**根因**：早期自动学习和中性色填充虽已删除，但当前显著场景不匹配仍调用 `_invalidate()` 清空内存背景并删除持久文件；用户重校准一开始也先删旧基线，超时或磁盘失败后校准会长期停在 active。持久资产、流代复验、场景变化和校准事务仍被混成一个布尔状态。
**处理**：按 `camera_id + configured_source_key + resolution` 保存持久空房基线，流代只作为运行复验状态。新校准使用候选平均帧，先压缩、fsync、原子替换磁盘文件，成功后才切换内存基线；重复校准直接拒绝，超时、人物进入、分割不可用和磁盘失败统一取消候选并保留旧基线。电视、灯光和局部物品变化通过全局色偏抵消和可见区域残差判断继续使用；摄像头移动或强场景变化只进入 `scene_review_required`、阻断骨架并保留基线，不自动删除。状态公开基线 SHA-256、进程内 revision、场景匹配率、残差、复验进度和场景复核次数。
**本地验证**：专项覆盖未校准、纯骨架、近期人物掩码、相邻 Pose 拒绝、新流代复验、服务重启、电视局部变化、整体灯光变化、摄像头移动、移动后旧基线恢复、失败重校准、并发重校准、磁盘原子提交失败和成功替换新基线。API 专项确认成功唤醒中继，超时和持久化失败返回结构化状态且取消 active；云中继能区分 calibrating、revalidating、scene_review_required 和 calibration_required。完整边缘回归通过后保持“待实机验收”。
**启动恢复修复**：背景重建器现在会按当前摄像头配置主动发现持久 `.npz`，服务重启后公开 `calibrated=true`、`baseline_retained=true`、`ready=false`、`status=revalidating`，不再把已存基线误报为“未校准”。复验独立于当前隐私模式运行，每个源帧只允许计入一次；等待复验的纯骨架流可以明确返回复验状态，不再因状态未实例化形成死锁。
**首轮实机反证**：最初复验只要求 Pose 同帧且以背景局部变化判断人物，摄像头 31 的有人画面被错误接受为 `local_change`。现已改为每次复验强制执行当前帧 Hailo 人物分割锚点；Pose 或分割任一发现人物都将进度重置为 `0/3` 并返回 `person_present`，禁止使用传播掩码替代真实锚点。
**负载修复与现场状态**：强制锚点首版挂在每个实时帧入口，有人持续停留时会逐帧重试并把该路成品输出压低。现按 `camera_id + source_key + resolution` 使用线程安全调度，每路首次立即检查、之后每 1 秒最多一次，每次仍为独立 Hailo 锚点；调度间隔、活动流和下次检查时间进入 `/health`。提交 `5642c9d` 完整边缘回归 `58/58` 后部署 Pi，服务 `active`、`NRestarts=0`，两路源流约 `15 FPS`、重连与 Hailo 失败均为 0。摄像头 31 的 `.npz` 文件 SHA-256 仍为 `291639114ffe4064d3e11e503a150ccb7b8b4f5dbd56bc992bf070a970256bd8`，基线像素 SHA-256 为 `cc8ff649c3611f51ae063ba1c089758140415c48c56d12d4566750ffcab5f969`；20:51 现场证据清楚显示两个人，因此状态正确保持 `revalidating / person_present / 0/3`。摄像头 32 随后在真实空房画面完成 `8/8` 校准与 `3/3` 独立 Hailo 复验，拒绝数为 0，状态为 `ready`；持久文件大小 `369281` 字节，文件 SHA-256 为 `8a2cda89ea9412841142193ef7ea9114c4ccb8c9ca51d7ec271ddd2a4fd63344`，基线像素 SHA-256 为 `05eae00d1c0faa0fc818d05e68a70a707130d91e9e3647e1e63e456918a49aab`。当前仍只有一路 ready，不能开始双路纯骨架交付验收。
**自动复验与纯骨架现场结果**：摄像头 31 在真实空房后自动完成 `3/3`，调度活动流清零且两份 `.npz` 文件哈希均未变化；两路同时 `ready` 后管理端正式切换为 `skeleton`。摄像头 32 的当前成品帧只显示一套蓝色骨架，人物像素由持久空房背景替换，没有模糊人物、双骨架或跨路像素；端侧约 `12.8-13.6 FPS`，云端接收约 `10.5-11.4 FPS`，上传失败为 0。随后人物重新进入摄像头 31 画面，该路正确回到 `revalidating / person_present / 0/3`，端侧和云端输出 FPS 均为 0，未回退原画或输出旧帧；摄像头 32 保持 `ready`。因此只能确认单路纯骨架视觉与阻断契约，GH-003 和双路交付门槛仍不关闭。
**验收**：两路分别完成空房校准，执行人物进入/停留/离开、电视内容变化、开关灯、移动局部物品、服务重启、断流重连和摄像头真实移动。前六类不得丢失持久基线或产生残影；真实移动必须立即阻断骨架、管理端显示“场景变化，需重校准”，重校准成功后原子替换并恢复。原画和模糊模式始终可用。

## GH-004 同帧身份和完成帧契约缺失

**现象**：骨架错位、旧结果覆盖新画面、跨摄像头内容和画面倒退。
**根因**：视频、Pose、分割和背景结果可以由不同线程、不同代次和不同时间完成。
**处理**：所有输入与输出强制携带并校验 `camera_id`、`source_key`、`stream_generation`、`frame_id`、单调时钟和合成版本。
**验收**：任一身份不一致的结果不得进入成品帧；两路摄像头连续运行时零串流、零旧帧回写。

## GH-005 iOS 与生产云端协议不一致

**现象**：骨架模式提示“服务器返回无法识别的响应”。
**根因**：手机当前安装的 TestFlight `1.0.0 (8)` 归档于 2026-08-01；其骨架分支要求已经废止的 `safe-scene-pose-v1` 分拆协议，由 App 分别取得安全场景和 Pose 后二次绘制。2026-08-03 的 `2678233` 已将正式链路收敛为盒子唯一合成的 `edge-composed-mjpeg-v1`，生产云会话和实际 MJPEG 响应均正确声明 `composition-owner=edge`。Build 8 在骨架分支主动拒绝新协议并抛出 `APIError.invalidResponse`；原画和模糊没有进入旧分拆分支，因此仍可播放。
**处理**：不恢复旧 Pose/scene 路由，不放宽协议校验，不增加兼容兜底。正式 iOS 已删除 Pose SSE、scene MJPEG 和客户端骨架绘制，只消费盒子生成的一条成品 MJPEG，并同时校验会话 `display_transport=edge-composed-mjpeg-v1` 与响应头 `X-GoHome-Composition-Owner=edge`。版本提升至 `1.0.0 (9)`，从唯一正式工程 `ios-shell/GoHomeShell.xcodeproj` 重新归档上传。
**发布前验证**：Build 9 单元测试 `128/128`、UI 测试 `25/25` 全部通过，`xcodebuild` 返回 `TEST SUCCEEDED`；版本源 `project.yml` 与生成工程均为 Build 9。纯骨架双摄连续监控 `1804.6` 秒、`360` 次采样，读取错误、模式不一致、服务故障、重连和上传失败均为 `0`；摄像头 31/32 的云端帧龄 P95 分别为 `217.5 / 180.3 ms`。Build 9 已于 2026-08-03 23:21 上传 App Store Connect，Delivery UUID 为 `d067a363-40e2-4723-93e6-e32282de6bba`，上传无错误或警告。
**TestFlight 验收**：用户已从 TestFlight 安装 Build 9，并在真实账户、生产云和双摄盒子环境进入骨架模式；画面可见、显示约 `7-13 FPS`，不再出现“服务器返回无法识别的响应”。同时段生产云存在 `1` 个正式视频客户端、Pose/scene 客户端均为 `0`，证明 App 使用唯一成品流而非旧分拆协议。GH-005 协议故障关闭；FPS 波动和持久视频传输继续由 GH-010、GH-011、GH-033 处理，不能用协议通过替代性能达标。
**验收**：TestFlight 从前台、后台恢复、换摄像头和换隐私模式均不会触发协议错误。

## GH-006 多条骨架展示路径

**现象**：管理端黄色骨架、App 蓝色骨架、粗细骨架重叠。
**根因**：服务端烘焙、Pose SSE 和客户端覆盖层同时存在。
**处理**：正式链路只保留盒子完成帧；删除云端兼容 Pose/scene 正式入口和所有客户端骨架绘制。
**验收**：管理端和 App 对同一 JPEG/视频帧显示完全一致的颜色、位置和数量。

## GH-007 实时上传调度错误

**现象**：流越看越慢、偶发卡死，云端累计大量 stale reject。
**根因**：每路允许多个 HTTP POST 并发，完成顺序不同于源帧顺序；槽位占满时丢弃当前新帧。
**处理**：每路一个在途请求和一个可覆盖的最新待发送帧，不建立积压队列。
**验收**：持续 30 分钟帧龄不增长，发送序号单调，stale reject 接近零，断网恢复后不补发旧直播帧。

## GH-008 双摄纯骨架验收缺失

**验收动作**：两路分别执行空场校准、进入、横向行走、静止、坐下、躺下、快速跌倒模拟、离开、遮挡、摄像头重连。
**通过标准**：无人物像素、无残影、无重复骨架、无串流；跌倒事件只通知一次；原画和模糊模式不受骨架链路影响。

## GH-009 Hailo 运行时重复

**现象**：共享 VDevice 上同时存在 Pose、Object 及两路 Segmentation runtime。
**处理**：每种 HEF 只实例化一个 runtime，使用跨摄像头的有界最新帧调度器。
**验收**：runtime 数量与 HEF 类型一致；双摄长时运行无内存增长、无饿死、无跨路状态。

## GH-010 至 GH-012 视频性能与旧链路

**处理**：测量 RTSP 实际码流；评估 GStreamer 硬件解码与 Hailo 管线；减少 BGR/JPEG 往返；最终使用持久 H.264/H.265/WebRTC，MJPEG 仅保留明确降级用途；移除重叠视频服务和失效参数。
**正式架构定稿（2026-08-03）**：盒子继续作为唯一隐私合成方，每路摄像头只有一个采集所有者和一个成品帧发布者。`PrivacyFrameRenderer` 直接输出 BGR 成品帧，常驻 FFmpeg 进程使用 `libx264 + ultrafast + zerolatency + yuv420p + 无 B 帧 + 约 1 秒关键帧` 编码，并通过 RTSP/TCP 持久发布到云端 MediaMTX。云端不解码、不重新合成、不缓存 JPEG，只做鉴权、媒体转发和可观测性；iOS 通过 WHEP/WebRTC 接收 H.264 并使用原生 WebRTC 视频渲染。正式 App 不允许自动回退 MJPEG，MJPEG 只保留在盒子局域网管理诊断接口，不能成为第二条公网产品链路。
**设备结论**：当前 Pi 5 系统虽列出 `h264_v4l2m2m`，真实编码探测返回无可用设备，不能把不存在的硬件编码能力写进方案。运行中使用 `libx264` 对 `640x360@15 FPS`、450 帧基准达到约 `36.1x` 实时、CPU 时间约 `1.4s`、峰值内存约 `63 MB`、码率约 `1.26 Mbps`；双路软件 H.264 编码具备明确余量。Hailo 只负责 Pose、分割和目标模型，不承担视频编码。
**验收**：所有 FPS 都标明源流、端侧合成、云端接收和客户端解码口径；不得以重复帧或 UI 刷新伪造 FPS。

GH-012 的本地结构已收口：边缘端删除独立 `video_app.py`、视频节点调度、旧视频档位、播放票据和全部 `/api/app/*`、`/api/v1/video/*` 路由；管理端仅保留 `/api/cameras/*` 局域网管理与诊断 MJPEG。旧 `/api/v1/device/cameras/*/stream.mjpg` 公网设备入口及宽松的设置令牌兼容已删除；正式公网视频只允许盒子通过 RTSPS 发布 H.264，云端通过短效鉴权 WHEP 会话向 App 转发。事件证据和受鉴权媒体读取统一归属云端；正式 App 的 `/api/v1/video/*` 始终归生产云端，不由盒子实现。尚需树莓派部署、生产云端和 TestFlight 实机确认后关闭。

## GH-013 存储与 COS 生命周期

**现象**：盒子数据库曾膨胀到约 1.10 GB，本轮检查仍有约 735 MB 已分配空间和大量可复用页；旧清理会先删 SQLite 行再删物理文件，COS、快照、对象目录和运行日志没有同一容量口径。
**根因**：媒体没有保留类别、删除状态、失败重试和删除审计；盒子只按表龄删除，云端只回收过期上传意图，没有正式资产保留和 COS/本地孤儿对账。
**处理**：盒子新增持久化媒体生命周期队列，统一快照、对象、上传会话和事件留存；在同一写事务中先删除物理字节，再提交引用清理，失败保留原行并指数退避重试。容量统计纳入 SQLite/WAL/SHM、快照、对象存储和运行日志，高水位按最老且不受保护数据循环回收并保留每路最新帧。云端新增资产分类、删除墓碑、失败重试、COS 分页清单、按存储后端隔离的孤儿对账、运维 dry-run 和迁移 `013_media_lifecycle.sql`。
**保留规则**：家庭记忆和用户上传由用户管理；未解决严重事件持续保护；严重事件证据 180 天、普通事件证据 90 天、验证证据 30 天、未引用临时上传 7 天、COS/本地孤儿宽限 2 天，均可由生产环境变量调整。
**本地验证**：云端全量 `101` 项中 `100` 通过、`1` 项因无 PostgreSQL 集成地址跳过；边缘媒体生命周期、运行历史、环形留存、外键完整性、容量口径和失败重试回归通过。
**盒子验证**：保留真实数据部署后发现历史物理删除摄像头造成 `10,476` 条子表外键违规。根因修复为所有连接强制启用 SQLite 外键、摄像头改为清除凭据的软归档、启动时恢复历史摄像头锚点，生命周期目标外键使用 `ON DELETE SET NULL`。迁移后外键违规为 `0`、`integrity_check=ok`，30 条历史摄像头均无凭据且产品列表只显示 2 路活动摄像头。数据库从 `735,203,328` 字节压缩到 `159,166,464` 字节，回收 `576,036,864` 字节；重启后服务 active、`NRestarts=0`。
**生产只读审计**：自动删除保持关闭。生产数据库共有 1033 个活动资产、470 个事件和 21 个家庭记忆媒体引用；COS 两个受管前缀共 133 个对象且孤儿为 0。本地媒体目录 1102 个文件、约 109.9 MiB。首轮 dry-run 报告 73 个到期资产和 227 个本地孤儿候选。逐项引用审计确认 73 个到期资产全部位于 `care-cards/`，其中 52 个仍由当前 `care_cards.image_url` 精确引用；原引用图只保护事件和家庭记忆，会误删仍在产品中展示的康养卡片图片。引用保护修复部署后，82 个当前卡片图片全部进入保护，保护数由 191 增至 273，删除计划由 73 降至 22，删除、失败和 COS 孤儿均为 0。进一步审计 46 个 `family_memory` 资产，只有 21 个仍由正式记忆引用；其余 25 个已无上传意图和正式引用，占约 13.6 MiB，当前仅凭 purpose 永久保护会形成持续泄漏。
**结构修复**：生命周期不再读取服务启动时的 PostgreSQL 快照，而是每次直接读取最新资产、事件、`family_memory_media`、卡片、账户头像和上传意图。正式记忆只按真实关联保护；完成上传但未发布、或删除记忆后失去关联的媒体回到 7 天临时资产保留规则。分类字段使用专用 PostgreSQL 批量更新，不再为每个资产触发全 Store 序列化和多表差异扫描。新增 `classification_only` 阶段：允许在真实删除关闭时持久化分类和保留期限，但所有正式资产与孤儿仅盘点、不删除。227 个本地孤儿候选中包含 11 个历史 `memories/` 文件，真实删除前继续按目录、数据库引用、年龄和容量分组审计，不按文件名或目录直接回收。
**分类阶段生产验收**：最新版本首次自动执行 `classification_only`，扫描 1036 个资产、分类 1036 个、识别 47 个到期项、保护 251 个；删除和失败均为 0，COS 136 个对象仍无孤儿，本地 227 个孤儿候选仅盘点。紧接着第二次 dry-run 的 `classified=0`、`planned_deletions=47`，证明分类已稳定持久化且不会每周期反复全量写库。正式记忆和当前卡片继续保护，25 个未绑定记忆上传已按临时资产进入计划。
**回收限流**：正式资产和 COS/本地孤儿均按最老时间和稳定键排序；每周期分别受数量和总字节双重限制。默认正式资产最多 10 个/64 MiB，孤儿最多 25 个/64 MiB；结果公开计划数量、计划字节、当期选择数量、选择字节、限流剩余和超大对象数量。限流回归确认 3 个到期资产只删除最老 2 个，3 个孤儿只删除最老 1 个，其余留待后续周期。
**待验收**：部署回收限流后再次执行 dry-run，核对 47 个到期资产和 227 个孤儿候选的当期选择数量与容量。随后补充孤儿删除的持久审计/失败重试边界，并形成最终清单；确认前不得启用真实删除。盒子仍需持续高水位、断电恢复和长时循环删除验收。

## GH-014 至 GH-016 产品闭环复验

- 推送：一个事件只通知一次，记录 APNs 请求、响应、设备回执和端到端延迟。
- 定位：绑定或首次设置时由手机确认家庭固定位置；首页使用手机位置计算回家距离；社区始终使用家庭固定位置。
- 模型质量：人物、猫、狗及困难负样本建立版本化标注集，报告 precision、recall、F1、混淆矩阵和场景分层结果。

## GH-017 至 GH-019 工程质量

- 管理端只展示真实测量指标，不展示公式生成的“准确率”。
- 拆分 iOS 网络协议、仓储和兼容上传装配，降低 `AppEnvironment` 的职责。
- 清理前先生成本地、生产云端和树莓派文件清单及哈希；确认没有独有代码或数据后再删除备份、旧入口、旧权重和构建垃圾。

## GH-020 最终交付验收

必须产出带时间戳和版本号的报告，至少覆盖：

1. 双摄持续运行 30 分钟，原画、人物模糊、纯骨架分别测试。
2. 摄像头断线、盒子重启、App 前后台、网络切换和流自动恢复。
3. 同一风险事件只生成一条事件和一次通知，排除异常只留事件不通知。
4. 本地 SQLite、磁盘和 COS 留存、循环删除、失败重试和孤儿对账。
5. 正式 `ios-shell` 工程生成 TestFlight，确认显示名 `GoHome`、Bundle ID `com.gohome.family`。

Build 8 不满足本台账的骨架产品契约，不作为最终交付版本。

## GH-021 摄像头连接配置原位更新

**现象**：摄像头地址由 `192.168.1.3` 变化到 `192.168.1.7` 后，App 编辑页只允许修改名称、位置和启用状态，没有 IP、端口、路径和凭证更新入口。
**根因**：正式 iOS 表单在编辑模式隐藏连接区，更新请求不包含连接字段；云端虽接受字段，但连接变化后没有统一重置为待盒子同步状态。
**处理**：保留云端与盒子摄像头 ID，云端返回不含密码的结构化连接摘要；App 使用 `URLComponents` 校验并生成 RTSP 地址，用户名和新密码留空时保留原值；连接字段变化后云端统一进入 `pending_edge_sync`，盒子配置同步原位更新。
**实机结果**：生产云摄像头 `29` 与盒子本地摄像头 `32` 保持不变，地址已原位更新为 `192.168.1.7`，盒子与云端均恢复 `online / synced`；云端实时接收约 `12.5 FPS`、stale reject 为 `0`。
**剩余验收**：下一 TestFlight 在正式 App 编辑页修改一次地址，确认密码留空、等待状态、盒子同步和守护页自动恢复；路由器为摄像头 MAC 设置 DHCP 地址保留，减少再次漂移。

## GH-022 摄像头源生命周期分裂

**现象**：云端和盒子数据库已将摄像头从 `192.168.1.3` 更新为 `192.168.1.7`，状态显示同步或在线，但长时进程仍持续连接旧地址；必须重启服务后新地址才真正生效。
**根因**：配置同步只修改 SQLite；托管读取器按启动时的旧配置长期存活。直播中继虽能按新签名重建消费线程，却不会销毁旧托管读取器，也不会原子清除姿态、分割、背景和渲染状态。
**处理**：`CameraAgent` 成为唯一采集生命周期所有者；连接 URL、凭据或启用状态变化时原子停止旧读取器、清除该摄像头旧缓存、提升独立流代次、通知所有视觉状态重置，再启动新读取器。配置同步写完所有摄像头后立即调用一次统一 reconcile。
**验收**：运行中执行 `.3 -> .7` 后无需重启服务；旧地址不再出现连接尝试；新源首个真实解码帧之前不得显示在线；旧缓存、Pose、分割、背景和隐私成品帧均不可返回。

## GH-023 重复帧伪造实时状态

**现象**：录屏中摄像头时间戳一直停在 `2026/08/02 20:01:17`，管理端仍显示约 `12-15 FPS`，并在冻结空画面上保留或生成“人物”状态。
**根因**：读取器把每次 `cap.read()` 成功都视为新帧；没有区分解码调用、像素真正推进和成品输出，重复像素会刷新缓存时间、帧号和姿态跟踪。
**处理**：唯一共享读取器对尺寸和每 8 像素覆盖样本计算 BLAKE2s 内容指纹，分别统计 decoded FPS 与 unique/effective FPS；相同内容只增加 decoded/repeated 计数，不创建新 `frame_id`、不刷新有效帧时间、不进入 Tracker。连续 3 秒无唯一像素后释放捕获、清空缓存、提升流代并通过统一 source transition 清除 Worker Pose、分割、背景运行态和隐私缓存，再按有界退避重连。
**补充根因**：读取线程先提交中央最新帧缓存，再提升独立通知序号。消费者可能在通知序号仍对应上一帧时读到已经提前更新的缓存；下一次通知到达后又读取同一缓存，造成相同 `frame_id` 被发布两次。该竞态会虚增客户端解码次数，并使回归偶发失败。
**本地验证**：共享采集回归确认冻结源多次成功解码但 unique frame 恒为 1、effective FPS 为 0、缓存被清除、流代提升且旧 Pose listener 被调用；新增局部 5x5 变化位于旧 16 像素采样空隙时仍能产生不同指纹。消费者现在以中央缓存的正式 `frame_id` 去重，通知序号只用于唤醒；确定性 cache-ahead 回归强制连续返回 `9-1 / 9-1 / 9-2`，正式输出必须为 `9-1 / 9-2`。完整边缘回归通过后进入实机验收。
**验收**：重复帧输入时有效 FPS 为 0、`frame_id` 不增长、姿态失效且画面状态为 stale；新像素恢复后自动恢复，所有页面仅展示有效输出 FPS。

## GH-024 算法诊断流与隐私模式耦合

**现象**：管理端算法页在全局模糊模式不画骨架，在骨架模式又切到隐私成品流；Tracker 停滞时诊断视频本身会冻结，无法判断摄像头是否仍在推进。
**根因**：诊断路由根据用户隐私模式选择三套不同来源，并把 Tracker 保留的旧像素当作视频源。
**处理**：算法诊断页始终以 `CameraAgent` 当前唯一帧为底图，只读取与该 `camera_id + source_key + frame_id` 完全一致的 Pose 元数据叠加一套诊断骨架；全局隐私模式不改变算法诊断语义。用户直播仍严格遵循三种隐私模式。
**验收**：原画、模糊和骨架任一全局模式下，算法页都能显示当前视频和同帧诊断骨架；源冻结时视频和检测同时明确失效，不得显示旧人物。

## GH-025 追踪补偿状态进入正式显示

**现象**：`20260802213732.mp4` 的管理端视觉算法页中，人物约在 32.6 秒向右移动后，旧骨架仍在沙发附近保留约 0.9 秒；页面同时显示 `短暂补偿`，最大帧龄约 `906 ms`。
**根因**：连续追踪器的 `coasting` 是供调度器快速请求新 Hailo 锚点的内部恢复状态，不是当前帧观测结果；诊断流、纯骨架合成和管理元数据却仍把该状态当成可绘制人物，导致旧几何覆盖在更新的视频帧上。
**处理**：显示契约只接受当前帧有效的 `observed / tracked`。`coasting` 与 `display_only_stale` 不得绘制骨架、不得辅助人物掩码、不得向管理端声明人物可用；Worker 发现这些状态时通过统一推理调度器请求当前摄像头立即刷新，不另建推理线程或第二套状态。当前分割仍负责移除当前人物像素，Pose 暂时无效时宁可短暂不画骨架，也不能展示错误旧骨架。
**本地验证**：连续追踪、同步诊断流、纯骨架隐私流、调度器和管理元数据专项回归通过；边缘完整回归 `48/48`、Python 编译和差异检查通过。
**验收**：部署后两路分别重复进入、横向快速移动、遮挡、离开和再次进入；视觉算法页与纯骨架成品流均不得出现滞留骨架。服务运行指标、校准状态和用户三模式不能回退；通过前保持“待实机验收”。

## GH-026 至 GH-029 校准、模式和实时帧契约

### GH-026 持久校准被短暂场景变化销毁

**根因**：`PrivacyBackgroundReconstructor` 将运行时 `scene_changed` 与永久校准失效视为同一状态，清空内存并删除持久化 `.npz`。电视内容、灯光或局部物品变化因此会破坏重启恢复能力。
**处理**：拆分持久基线、运行时复核和明确重校准三类状态；短暂变化只暂停纯骨架输出并持续复核，绝不删除基线；按逐路摄像头独立管理。
**验收**：电视、开关灯、局部遮挡后可自动恢复；服务重启仍加载同一校准；只有明确换源、分辨率变化、摄像头移动确认或用户操作才要求重校准。

### GH-027 隐私模式不是事务

**根因**：云端和盒子只持有一个全局模式字符串，模式期望值、逐路有效模式和逐路校准状态没有共同提交。任一路未就绪时可能出现云端仍为 `skeleton`、盒子无成品帧。
**处理**：保存 `desired_mode`，逐路计算 `effective_mode/status/reason`；切换前做逐路预检，未就绪摄像头保持原有效模式并明确返回失败，不得拖黑其他摄像头。
**验收**：两路不同校准状态下分别切换，管理端、云端和 App 显示同一逐路状态；失败切换不改变原有效画面。

### GH-028 空 MJPEG 和静默异常

**根因**：`PrivacyMjpegStream.mjpeg_frames()` 捕获所有异常后继续循环，形成状态码为 200 但永远无帧的响应，客户端只能报“无法识别的响应”或持续连接。
**处理**：打开流前执行就绪预检；渲染错误进入逐路结构化状态和指标；流建立后发生不可恢复错误必须结束连接并允许客户端按明确原因重试。
**验收**：未校准、源过期、分割失败和身份不一致均有确定的 API 状态与错误码；不存在 200 空流和无限静默循环。

### GH-029 旧像素使用新帧身份

**根因**：近黑帧出现时 `CameraAgent.raw_frames()` 使用 `last_good_frame` 替换像素，但保留新采集的帧号和时间信息，造成旧画面、Pose 和 FPS 被误认为当前结果。
**处理**：旧像素不得进入实时帧管线；源进入 `stale/reconnecting` 并停止推进有效帧号。若产品需要最后画面，只能作为标明时间的静态占位，不参与算法和实时 FPS。
**本地验证**：近黑判定已移入每路唯一共享读取器；近黑帧只增加 decoded/near-black 诊断计数，首帧即清除中央实时缓存、有效 FPS 窗口和内容连续性并进入 stale，持续 5 帧且至少 0.75 秒后提升流代并统一重连。预览层的 `last_good_frame`、黑帧计数和旧像素替换分支已删除。专项回归确认连续 5 张近黑帧没有写入中央缓存、没有推进有效帧身份、没有作为预览输出，恢复帧使用新流代；共享采集、冻结失效、实时分析同帧和同步骨架回归通过。
**验收**：近黑、冻结和断流时有效 FPS 归零、Pose 失效、帧号不推进；恢复后从新真实像素继续。

## GH-030 至 GH-033 算力、指标、视觉和传输

### GH-030 Hailo 调度未充分利用

**根因**：整套 `VisionPipeline` 被一个全局锁串行执行，双路摄像头共用单个低频 Worker 调度；Pose、Object、CPU 后处理和事件逻辑绑在同一周期，KLT 长期代替新 Pose。
**处理**：按模型职责建立有界最新帧调度器：活跃人物 Pose 目标每路 `10-15 Hz`，隐私需要时分割目标每路 `5-10 Hz`，物体/场景 `0.5-1 Hz`；每种 HEF 保持单 runtime，并强制摄像头、源代次和帧身份。最终频率以 Pi 5 + Hailo 分阶段实测为准。
**阶段进展**：Pi 旧版现场实测 Pose 中位 `22.86 ms`、P95 `35.24 ms`、整管线中位 `28.76 ms`、P95 `71.62 ms`，Hailo 失败为 0，CPU 约 106%、温度 65°C；空房调度为两路 Pose 各 2 Hz、Object 各约 0.8 Hz、分割各约 4.67 Hz，KLT 展示约 11-13 FPS。确认 idle 周期已经执行 Pose HEF，却因 `pose_detection_enabled=false` 丢弃结果，遮挡或 Object 漏检时可能无法进入 active。现已复用已执行的 Hailo idle Pose 结果作为人物/风险首帧，不增加设备推理；CPU/未知后端仍使用轻量人物探测。GH-030 保持处理中，下一步必须实测人物进入后的双路 active cadence 和 Hailo 利用率，再决定是否拆分全局管线锁。
**验收**：报告解码、预处理、Hailo 推理、后处理、合成和编码各阶段 P50/P95；双路无饿死、无跨路状态，当前帧 Pose 锚点达到实测目标，KLT 仅用于短时插值。

### GH-031 FPS 指标语义错误

**根因**：部分指标用固定 10 秒除法或源读取速率代替真实样本跨度和用户收到的完成帧速率。
**处理**：独立报告 `source_decoded_fps`、`source_unique_fps`、`edge_composed_fps`、`cloud_accepted_fps`、`client_displayed_fps` 与帧龄；全部按实际时间跨度计算。
**验收**：无新成品帧时输出 FPS 必须为 0；管理端和 App 标签明确且不能以重复帧或 UI 刷新伪造实时性。

### GH-032 骨架视觉不统一

**根因**：隐私成品流和算法诊断流使用不同 BGR 色值、线宽和绘制实现。
**处理**：共享一套蓝色骨架样式常量和绘制函数；用户成品流只在 `skeleton` 模式显示，算法诊断页始终显示同帧诊断骨架。
**验收**：管理端算法页、管理首页和 App 对相同成品帧不重复绘制；颜色、线宽、关键点和骨架数量符合各页面契约。

### GH-033 逐帧 JPEG 正式链路

**根因**：当前正式路径仍经过 OpenCV BGR、CPU 合成、JPEG 编码、逐帧 HTTP 上传和 MJPEG 播放，带宽、延迟、背压和设备能耗无法达到最终产品标准。
**实测证据**：Build 9 验收时两路源流约 `15.1 / 13.3 FPS`、端侧成品约 `14.8 / 12.7 FPS`，但云端仅约 `9.8 / 8.6 FPS`，逐帧上传覆盖约 `19%-20%` 的待发送新帧，最大上传/接收间隔约 `1.1-1.5s`。没有 RTSP 重连、上传失败或 Hailo 推理失败，瓶颈明确位于 JPEG 编码、逐帧 HTTP 请求/JSON 响应、云端 JPEG 缓存/MJPEG 分发和 iOS `UIImage` 解码链。
**处理**：迁移为单解码所有者、单次隐私合成、常驻 FFmpeg/libx264、RTSP/TCP 上行、MediaMTX 转发和 WHEP/WebRTC 下行。MediaMTX 固定版本并启用外部 HTTP/JWT 鉴权、路径级发布/读取权限、WebRTC 指标和 TURN；Node 只签发短时媒体权限并校验家庭、盒子、摄像头、模式和有效期。迁移完成后删除 `_LatestFrameUploader`、`/api/v1/device/live-frames/upload`、云端 JPEG 帧缓存/MJPEG 产品路由和 iOS `MJPEGStreamClient`；盒子 LAN 管理诊断 MJPEG 独立标识且不被正式 App 使用。
**阶段进展**：隐私渲染器已拆分“像素合成”和“传输编码”职责，新增未经 JPEG 的 BGR 成品帧接口与独立合成耗时指标；三种隐私像素契约保持不变。边缘正式发布器已改为每路唯一常驻 FFmpeg 进程，直接接收 BGR 成品，以 `libx264 / ultrafast / zerolatency / yuv420p / no B-frame / 约 1 秒 GOP` 编码并通过 RTSPS/TCP 发布；待编码帧只保留最新一张，管道写入有界超时，模式或源代次变化会中断旧帧并重启编码器，进程异常退出采用有界退避。状态只报告真实提交和编码器输入 FPS，不把写入管道声称为云端送达；发布 URL、错误和 FFmpeg stderr 均移除设备凭据。旧 `_LatestFrameUploader`、逐帧 JPEG 编码和 HTTP/JSON 上行已从边缘正式中继实现删除，LAN 管理 MJPEG 不变。
**阶段验证**：持久发布专项覆盖最新帧替换、非连续像素规范化、模式重启、异常退出、阻塞管道超时、校准暂停、凭据脱敏和无 JPEG 中继；完整边缘确定性回归 `58/58` 通过。Pi 上 FFmpeg `7.1.5` 确认包含 `libx264`、GnuTLS 和 `fps_mode`，实际 BGR 到 H.264 参数在奇数输入尺寸和 15 FPS 节奏下编码通过，RTSPS 输出参数解析通过并进入连接阶段。生产 MediaMTX/Coturn/TLS、真实盒子发布和 TestFlight 长时验收尚未完成，因此该边缘变更不得单独部署，GH-033 继续保持“处理中”。该项只有在全链路部署和 TestFlight 长时验收全部完成后才能关闭。
**云端与 iOS 进展**：生产云 `/api/v1/video/sessions` 已统一通过 `MediaAccessService` 签发短时 WHEP 会话；正式 iOS 已迁移为原生 Unified Plan WebRTC 接收、WHEP `OPTIONS/POST/DELETE`、完整 ICE 收集和 `RTCMTLVideoView` 渲染，显示 FPS 只统计当前渲染器实际收到的新视频帧。摄像头、隐私模式和前后台变化会关闭旧 PeerConnection、终止旧帧事件并释放 WHEP 资源；WHEP 只接受 HTTPS 且资源 `Location` 必须同源。云端逐帧上传 API、JPEG 帧缓存、产品 MJPEG 路由、播放票据和 iOS `MJPEGStreamClient` 已删除，盒子 LAN 管理 MJPEG 保留为独立诊断链路。云端完整回归 `99` 项中 `98` 通过、`1` 项因未配置 PostgreSQL 集成地址跳过；iOS `122/122` 单元测试和 `25/25` UI 测试通过，其中 WHEP 信令 `6/6`、守护页 ViewModel `13/13`，真实 M137 WebRTC framework 模拟器构建通过。生产 MediaMTX/Coturn/TLS、防火墙、真实双摄发布读取和 TestFlight 仍未完成，因此不得部署当前迁移，也不得关闭 GH-033。
**验收**：双路 30 分钟端到端帧龄不增长，网络切换自动恢复，客户端解码 FPS 和画质达到验收基线。

## GH-034 至 GH-036 架构与真实产品边界

### GH-034 盒子运行时单体化

**根因**：`main.py` 同时装配配网、身份、家庭、消息、升级、推送、云端事件、视频和算法路由；`worker.py` 同时负责推理、追踪、规则、事件、活动日志和留存；`storage.py` 集中全部表和迁移。
**处理**：先锁定调用消费者，再拆分运行生命周期、采集/合成、推理协调、活动持久化、风险事件和维护任务；移除仅属云端或已无消费者的盒子代码。
**验收**：每个生产职责只有一个所有者；入口只装配模块和路由；删除项有调用清单、迁移策略和回归证明。

### GH-035 真实验收覆盖不足

**根因**：现有通过项主要是合成数据和接口回归，没有覆盖校准后重启、电视变化、两路不同状态、生产云、TestFlight 和长时运行完整路径。
**处理**：建立版本化实机验收矩阵和采样报告，自动回归只能作为进入实机验收的门槛。
**验收**：完成 GH-020 全部场景并保存端侧、云端、App 同时段指标和证据。

### GH-036 云端职责残留在盒子

**根因**：盒子入口仍提供用户注册、家庭、消息、推送和模拟天气等历史原型功能，容易形成双数据源和假产品数据。
**处理**：确认正式 iOS 和生产云调用后，将用户、家庭、消息、天气和 APNs 业务统一归生产云；盒子只保留设备、摄像头、算法、事件上行、配置同步和本地管理职责。
**验收**：正式 App 不直接依赖盒子业务 API；盒子断网时本地安全算法继续运行，联网后幂等补传；生产环境不返回模拟数据。

## GH-037 至 GH-040 安全与部署边界

### GH-037 本地管理身份不满足产品安全基线

**根因**：`BoxInitService` 将 `admin / 123456` 写成固定初始凭据，且 `GOHOME_ADMIN_MUST_CHANGE_PASSWORD` 默认关闭；登录没有失败次数、指数退避或临时锁定，文件会话也没有跨进程并发保护。
**处理**：首次启动生成每台盒子唯一的一次性设置凭据或由实体配对流程建立管理员密码；生产环境强制首次修改，增加账号/IP 组合限流和安全审计，密码修改不得通过创建额外会话来验证旧密码。
**验收**：全新数据目录不存在通用默认密码；连续失败触发有界锁定且成功登录后恢复；会话创建、修改密码和并发写入回归通过，管理端不泄露凭据状态细节。
**本地验证**：新盒子生成随机一次性凭据并以 `0600` 文件保存，状态 API 不返回凭据、哈希或凭据状态；历史 `admin / 123456` 启动时自动轮换；第三次连续失败开始指数锁定，锁定上限 5 分钟；认证、会话、改密和安全审计使用同一跨进程文件事务，改密直接核验哈希且撤销全部会话。并发 9 会话、失败锁定、成功恢复、旧凭据迁移、改密和审计专项回归通过。待树莓派部署后读取一次性凭据、完成首次改密并复验重启与并发登录。

### GH-038 升级供应链可执行任意未验证内容

**根因**：包发布只把 SHA-256 放入元数据，安装时没有重新计算比对，也没有受信任公钥签名；ZIP/TAR 使用无边界的 `extractall`，`entry_path` 可解析到版本目录外，随后运行时守护可直接执行 `.py`、`.sh` 或可执行文件。
**处理**：升级控制面归生产云；盒子只接受带版本、设备范围、内容哈希和 Ed25519 签名的不可变清单。下载到临时文件后先验大小、哈希和签名，再使用逐成员安全解压并验证最终入口位于版本目录内；原子切换版本并保留经过同样验证的上一版本。
**验收**：篡改包、错误签名、ZIP Slip、Tar Slip、绝对路径、符号链接逃逸和越界入口全部被拒绝；合法包安装、启动失败回滚和断电恢复通过。
**本地验证**：新增版本化签名清单，签名覆盖包类型、版本、家庭/设备范围、字节数、SHA-256、公钥标识、文件名、入口类型、入口路径和安装策略。发布记录与盒子安装均重新验签和校验包体；ZIP/TAR 改为逐成员复制并拒绝穿越、绝对路径、重复成员、符号/硬链接、设备节点、加密 ZIP、成员数和展开体积越界。版本目录只允许同签名内容幂等复用，安装后入口、文件索引和原始包在每次启动前复验；运行方式由签名 `entry_type` 明确声明，不再按后缀或权限猜测。新 App 通过启动健康检查后才原子切换当前清单，启动或激活失败只回滚到已验证版本。专项攻击回归和边缘完整回归 `51/51` 通过；仍需在树莓派配置正式公钥、部署依赖，并完成真实安装、进程崩溃、断电和回滚验收。

### GH-039 盒子媒体上传服务与生产云重复且当前不可用

**根因**：盒子同时暴露家庭媒体上传、公开媒体下载和设备媒体接收，`create_upload_session()` 还读取未定义的 `event_id`；事件接收后会把本地快照再复制到对象目录，和正式上传队列、生产云资产、COS 对象形成多份来源。盒子上行与云端接收又分别使用 `read_bytes()`、`await request.body()`，媒体越大，进程瞬时内存越高。
**处理**：删除边缘 `ObjectStorageService`、全部 `/api/v1/media/*`、`/api/public/media/*` 与边缘 `/api/v1/device/media-assets*` 接收路由；本地事件不再提升第二份媒体资产。盒子只由持久上传队列将原始证据文件流式发送到生产云，生产云按设备和家庭鉴权后直接流式写 COS，未启用 COS 时流式写临时文件并原子提交。签名升级包独立为 `PackageArtifactService` 和 `/api/v1/package-artifacts/*`，使用声明大小、临时文件、原子替换与 `package_artifact` 留存类别，不再复用家庭媒体语义。
**本地验证**：AST 所有权契约确认旧媒体路由和实现符号均已消失；事件证据上传测试确认传递文件对象而非整包字节；COS 测试确认请求流直接进入对象存储且字节数匹配；升级包流式上传、超限拒绝、临时文件清理和签名供应链回归通过。边缘完整回归 `53/53`；云端 `101` 项中 `100` 通过、`1` 项仅因未配置 PostgreSQL 集成地址跳过。
**实机验收**：部署树莓派和生产云后，在断网、恢复、重复任务、接近大小上限和 COS 短暂失败场景确认队列幂等、内存稳定、对象唯一、事件可鉴权查看且生命周期可对账；完成前保持“待实机验收”。升级控制面迁移生产云继续由 GH-034/GH-038 跟踪，不得恢复通用媒体接口。

### GH-040 非树莓派和旧 Web 试点职责仍进入盒子运行时

**根因**：`EdgeBootstrapService` 只实现 macOS `launchd`，却和 Pi 生产设置一起装配；`PublicPilotService` 继续生成 `app-shell.html`、`watch.html` 等旧 WebView 链接；盒子还持有 APNs 私钥配置和直接发送实现，扩大攻击面并形成多条通知路径。
**处理**：Pi 盒子只由版本化 `systemd` 单元管理；macOS 开发启动工具迁出生产包。删除旧 Web 试点链接生成和盒子直连 APNs，事件只通过持久上行提交生产云，由云端完成单次推送、回执和失效 Token 管理。
**验收**：Pi 部署清单不含 LaunchAgent、旧 Web App 路由或 APNs 私钥；本地算法断网继续工作，恢复后同一事件只由云端通知一次。
**本地验证**：已删除盒子端 LaunchAgent、旧 Web 试点、公网链接增强、通用通知器、APNs relay、App push 服务及测试通知脚本，共净删除约 1,700 行运行时代码。`/ui`、盒子通知/APNs/运行时安装路由和对应 Schema、环境变量、SQLite 表、规则字段均已移除；旧数据库启动时幂等删除 `notification_enabled`、`app_push_tokens` 和 `notification_deliveries`，保留其余算法规则值。新增生产运行时边界回归，确认 `systemd` 是盒子唯一运行时、云端仍独占 APNs。边缘完整回归 `54/54`；云端 `101` 项中 `100` 通过、`1` 项仅因未配置 PostgreSQL 集成地址跳过。
**实机验收**：部署树莓派后确认旧模块、旧脚本、旧表和 APNs 环境均不存在，服务由同一个 `gohome-edge-agent.service` 启动且重启恢复；断网制造一条风险事件，恢复后只上传一次，生产云只生成一次消息和一次 APNs 投递。完成前状态为“本地根因已消除，待实机验收”。

## GH-041 至 GH-043 算法语义与质量证明

### GH-041 骨架可视性错误依赖跌倒证据资格

**根因**：`RtmposeAnalyzer._result_from_raw_poses()` 对每组 Pose 执行跌倒证据质量门槛，核心点或可见点不足时不但禁止告警，还将整组 Pose 从 `poses` 删除。吃饭、桌面遮挡、半身和侧身等常见场景因此在算法页和骨架模式完全无骨架。
**处理**：保留唯一 `poses` 当前帧骨架流，每组 Pose 分别声明 `display_evidence_eligible / person_evidence_eligible / fall_evidence_eligible` 及对应质量明细。最低显示质量只决定是否绘制；更严格的核心点和跌倒质量只决定能否进入正式风险计算。跌倒质量不足时保留当前骨架与姿态事实，但移除 `fall_candidate`、正式跌倒分数归零。场景聚合和 `PoseFactorGraphEngine` 只消费 `fall_evidence_eligible=true` 的 Pose，缓存和光流结果继续仅用于有界显示。
**验收**：站立、坐姿进餐、桌面遮挡、半身、侧身和多人场景中，当前可见关键点按真实置信度显示；不完整 Pose 不触发跌倒事件；无效几何仍拒绝；完整 Pose 保持原有跌倒能力；旧 Pose 和补偿状态仍不得成为正式证据。
**本地验证**：新增独立证据边界回归，覆盖半身骨架可见但跌倒资格为零、完整骨架同时具备显示和风险资格、无效几何拒绝、场景聚合不重建风险、因子图拒绝 display-only Pose。同步更新视觉管线、跌倒规则和因子图断言；连续跟踪、同步骨架流、纯骨架隐私流专项及边缘完整回归 `55/55` 通过。状态为“本地根因已消除，待双摄真人进餐、遮挡、侧身和跌倒实机验收”。

### GH-042 EACP 尚无可复现的模型质量依据

**根因**：当前 `PoseFactorGraphEngine`、`PostureClassifier` 和 `FallAnalyzer` 主要由固定权重、阈值和空间规则组成；`EacpAcceptanceService` 只记录人工单次场景是否产生事件，并不计算样本级 Precision、Recall、F1、ROC、混淆矩阵或分层误报率。
**处理**：评估报告升级为 v2 唯一公共实现，帧评估和序列评估统一输出 TP/FP/TN/FN、Precision、Recall/Sensitivity、Specificity、F1、Accuracy、Balanced Accuracy、FPR/FNR、95% Wilson 区间、P50/P95 延迟、按数据集/动作类别/受试者/视角分层结果及完整误报漏报清单。每份报告绑定 Git revision 与 dirty 状态、算法源文件哈希、模型哈希、完整配置哈希、清单及媒体内容指纹、命令与运行平台。质量声明门禁要求显式独立 `test` split、完整标签和可读样本、train/validation/test 齐全、序列和受试者不泄漏、至少 30 个测试正序列和 50 个测试负序列；不满足时报告固定声明“仅工程回归，不可用于产品准确率或论文级结论”。
**数据审计**：数据审计升级为 v2，检查缺失文件、缺失标签、缺失/重复帧顺序、序列元数据冲突、同序列跨 split、同受试者跨 split、相同内容跨 split 和未分配 split；稀疏/密集抽帧不再重复计为两套独立来源。退役火灾数据门禁和可伪造生产事件的公开样本注入脚本已删除，单场景 `EacpAcceptanceService` 明确只做运行闭环验收。
**当前结论**：现有样本可支持规则回归，但只有 25 个独立跌倒/ADL 时序来源、12 个正例、13 个负例、2 个公开数据集、6 张真实家庭负样本，且无 train/validation/test 划分和完整姿态序列。`fall_rule_regression=ready`，真实家庭试点、时序模型实验和产品质量声明均为 `insufficient`；当前不得宣称论文级、最优或产品准确率。
**本地验证**：新增评估完整性回归，验证统一指标、Wilson 区间、分层统计、复制目录后数据指纹稳定、内容变化后指纹变化、序列/受试者泄漏拦截和严格质量门禁；数据审计回归、EACP 服务/API 回归、真实 person 帧报告和真实 GMDCSA24 序列报告均通过。完成完整边缘回归后提交；实机数据采集和独立测试集建设作为下一阶段质量资产继续跟踪。
**验收**：每个发布算法包含数据版本、模型哈希、完整阈值、总体及分场景指标、混淆矩阵、置信区间、P95 延迟和失败样本清单；双摄实机长时误报/漏报与独立测试集一致。在门禁变为 ready 前，产品页、比赛材料和论文不得引用准确率结论。

### GH-043 暖色启发式不能承担火灾告警

**根因**：`FireAnalyzer` 只根据 RGB 暖色比例、黄色区域、纹理、连通域和相邻帧变化计算分数，电视、灯光、阳光、红色物体和厨房场景均可能误报；当前标签和事件语义却是“火灾应急报警”。
**处理**：产品交付前将该能力降为不通知的实验候选，或接入经过独立数据验证的火焰/烟雾模型并结合多帧持续性和传感器证据；未通过验证不得发送紧急通知。
**验收**：真实火焰/烟雾、电视画面、暖灯、夕阳、红衣、厨房蒸汽等分层测试有明确指标；只有达到发布阈值且连续确认的危险事件通知一次。

### GH-044 管理端保留三套失效运行时和七个跳转页面

**根因**：正式管理端已经收敛为 `login.js + console.js`，但生产静态目录仍包含旧版 `app.js`、`detection.js`、`vision.js`。三者重复实现摄像头选择、抓帧轮询、检测框、规则保存和盒子直发测试通知，其中 `app.js` 还直接访问当前页面不存在的 DOM。`detection.html`、`rules.html`、`stream.html` 和三个 `algorithm-*.html` 仅加载 `redirect.js` 跳转到新页面，形成无业务价值的历史入口。
**处理**：删除三套旧运行时、六个跳转 HTML 和 `redirect.js`；管理端生产静态资源只保留四个正式页面、登录页面、`login.js`、`console.js` 与两份当前样式。建立静态入口清单测试，校验每个 HTML 的本地脚本依赖存在且只能属于正式集合，生产包不能重新引入历史入口。
**验收**：管理首页、摄像头配置、视觉算法、事件日志和登录页均能加载；静态入口测试与边缘回归通过；部署清单不再包含旧页面和旧脚本，代码行数与职责重复可量化下降。
**本地验证**：已删除 2,009 行旧 JavaScript、104 行跳转 HTML 和 2 行跳转脚本。静态入口契约确认五个正式页面只依赖 `console.js / login.js`；两份脚本 Node 语法、Python 编译、差异检查和边缘完整回归 `48/48` 通过。状态为“本地根因已消除，待盒子部署页面验收”。

### GH-045 管理端保留失效算法剧场并伪造百分比指标

**根因**：正式算法页已经取消算法示意容器和模式选择器，但 `console.js` 仍保留完整的八模式演示档案、WebM 加载、模式切换、演示文案和指标换算，部署目录仍包含 8 个演示视频。画面质量、用餐、夜间、跌倒和火灾等内部规则量被重新组合成貌似准确率的百分比，其中火灾分数被乘以 `2800` 并封顶 `98%`；这些数值没有训练集、测试集或统计口径。
**处理**：删除不可达的算法剧场运行时、演示素材和模式分支；管理页只呈现当前帧后端实际返回的原始信号、状态机阶段、真实模型置信度和可测运行指标。规则内部量必须保留原单位和字段名，未经独立测试集验证不得包装成准确率、通过率、健康度或可信度。
**验收**：生产静态目录不含算法演示视频；管理端脚本不存在人为组合百分比或火灾倍数换算；算法页所有数值均能映射到后端响应字段或明确的计数/时间计算；静态契约、脚本语法和完整边缘回归通过。
**本地验证**：已删除 643 行不可达模式/演示/伪指标运行时和 8 个 WebM；算法页固定为统一感知，只显示当前目标计数、后端模型名、帧龄/分析延迟及现有状态机结果。静态资源契约、Node 语法、差异检查和边缘完整回归 `48/48` 通过。状态为“本地根因已消除，待盒子部署页面验收”。

### GH-046 暖色启发式仍进入正式安全事件与通知链路

**根因**：GH-043 已确认 `FireAnalyzer` 只是暖色、纹理、连通域和帧间变化启发式，但 `RuleEngine` 仍将连续命中生成为 `critical fire_candidate`，归类为 `safety_alert` 并交给正式上传、云端复核和通知链路；管理页仍提供“火灾应急报警”开关并显示“火灾事件已触发”。
**处理**：删除暖色启发式实现及其逐帧 CPU 分析，从推理结果、规则状态机、事件、证据持久化、云端复核资格、通知、管理端、配置 Schema 和评估脚本中完整移除。SQLite `rules` 表幂等删除五个废弃列，历史 `events/event_candidates` 保持只读可审计。后续若重新上线，必须以新的经过验证能力版本和独立迁移方案接入，不能复用实验字段伪装正式能力。
**验收**：任意暖灯、电视、夕阳、红色物体或启发式连续命中均不会创建事件和通知；正式管理页无火灾应急开关、事件状态或准确率表述；历史事件仍可审计但不再产生新记录；规则引擎与通知去重回归通过。
**本地验证**：删除 `FireAnalyzer` 191 行实现及正式链路全部调用；旧规则表五列迁移后归零，非火灾配置和 `updated_at` 原值保留；连续十帧注入旧 fire 字段不会产生候选、状态或持久化摘要，历史火灾事件仍可读取。聚焦迁移、视觉管线、持久化、事件去重、管理端静态契约和完整边缘回归通过。状态为“本地根因已消除，待盒子部署后确认运行指标与页面”。

## 2026-08-02 本地修复与验证记录

- GH-002/GH-003：骨架模式改为显式空房校准背景替换，不再调用人物模糊；删除自动背景学习、中性色填充和 safe-scene 兼容输出。流代次复验完成前一律返回 `stream_revalidation_required`，不输出当前帧。
- GH-004：渲染身份固定为 `camera_id + source_key + stream_generation + frame_id`；新增相邻较新 Pose 拒绝回归，证明错误帧姿态不会绘制到当前帧。
- GH-005/GH-006：唯一正式协议为 `edge-composed-mjpeg-v1`。云端删除 Pose 上传、scene 上传、Pose SSE 和 scene MJPEG；会话 JSON 与实际 MJPEG 响应头同时声明协议版本和 `composition-owner=edge`；iOS 同时校验两项。
- GH-007：每路上传改为一个在途请求和一个可覆盖最新帧。20 帧快速输入回归中最大 HTTP 并发为 1、上传序号单调、最终上传第 20 帧；校准等待不会重启摄像头采集。
- GH-009：人体分割同一 HEF 只创建一个共享 runtime，设备推理串行占有；两路并发回归只创建一个 runtime，重置单路摄像头不关闭共享 runtime。Pose 和 Object 后端原本已分别使用单实例 runtime。
- GH-010：共享读取器不再保留第二份 BGR 帧；真实帧只由中央最新帧缓存持有，订阅者按序号等待后取得一份私有副本。GH-029 已进一步删除黑帧保持与旧像素替换。正式云中继路径为一次 RTSP 解码、按需一次缩放、一次隐私合成和一次 JPEG 编码；`render_jpeg()` 不是正式直播入口。
- GH-011：云端滚动直播指标从 `server.js` 提取为独立 `RollingStreamMetrics`。有效 FPS 改为按真实接收帧间隔计算，不再在启动阶段固定除以 10 秒；同时报告 10 秒样本数、帧间隔 P95/最大值、传输延迟 P95/最大值、最后帧龄和 stale reject。新增确定性单元测试覆盖启动、窗口裁剪和卡死后帧龄。
- GH-010 实机续查：离线摄像头原来由共享读取器和 `capture_frame()` 旁路同时反复建连。现统一为每路一个托管采集所有者；分析、直播和抓图只能消费中央最新帧。断线重试按 `0.5/1/2/4/8` 秒有界退避，成功解出真实帧才清零，并报告连接尝试、连续失败和下次重试。`192.168.1.3:554` 当前真实拒绝连接，但不再阻塞或重复解码第一路。
- GH-011 实机续查：在线摄像头 `/1/2` 实测 `640x360@15`，`/1/1` 实测 `2880x1620@15`，两路均由 RTSP 声明 15 FPS；主码流只增加像素，不增加帧率。修复离线阻塞后在线解码约 `14.98 FPS`、隐私输出约 `14.92 FPS`、公网逐帧 JPEG 接收约 `13 FPS`。Pose 元数据新增每路 64 帧有界同帧历史，渲染可在 Tracker 推进后仍按 `camera_id + source_key + frame_id` 取得正确骨架，不保存第二份图像。
- GH-010/GH-011 自动回归：18 项边缘核心验证全部通过，覆盖 Hailo 分割 runtime、共享采集、断线退避、节拍、Pose/KLT、同帧同步、三种隐私、单在途上传、跌倒规则、推理调度、事件去重、配置同步、媒体生命周期、环形留存、关停和路由所有权。
- GH-012：完成跨端路由所有权审计并删除边缘端重复视频服务、播放会话、视频档位、节点调度、旧 App 路由、废弃 Schema/配置和数据库方法；媒体资产提升迁入 `ObjectStorageService`。新增 AST 契约检查，锁定盒子管理流和设备流共用 `PrivacyMjpegStream`，生产云端拥有正式播放会话，正式 iOS 只连接生产云端。
- 自动验证：盒子核心回归全部通过；云端 Node 测试 `101` 项中 `100` 通过、`1` 项因未配置 PostgreSQL 连接跳过；正式 `ios-shell` 单元测试 `127/127`、UI 测试 `25/25` 通过，`xcodebuild` 返回 `TEST SUCCEEDED`。
- GH-010/GH-011 尚需树莓派逐阶段剖析、生产云端部署和 TestFlight 解码实测；持久 H.264/H.265/WebRTC 仍是正式目标，当前 MJPEG 只能作为过渡/降级链路，因此两项保持“处理中”。
- GH-013：盒子真实数据迁移、历史外键修复和 SQLite 压缩已通过，`10,476` 条违规归零并回收约 `576 MB`；生产 PostgreSQL/COS dry-run、真实删除和盒子长时循环仍未验收，因此保持“待实机验收”。
- 上述其余条目仍需部署到树莓派和生产云端，并由 TestFlight 双摄真人长时验收，因此状态只能是 `待实机验收`，不能标记 `已关闭`。

## 2026-08-02 源生命周期与唯一成品流部署记录

- 边缘端 `48/48` 项回归通过。新增冻结流确定性回归：重复像素不增加有效 FPS 或帧号；持续冻结会清除中央帧缓存、提升流代次并向 Worker、隐私渲染器和中继发布统一失效事件，旧 Pose、跟踪、分割和成品帧不得继续显示。
- 生产云只合并视频协议所需的最小差异，没有覆盖生产独有代码：所有播放会话统一声明 `edge-composed-mjpeg-v1`，不再向正式 iOS 返回 Pose/scene 路径；云端缓存流、空流、静态回退和盒子代理统一返回 `X-GoHome-Display-Transport: edge-composed-mjpeg-v1` 与 `X-GoHome-Composition-Owner: edge`。
- 生产鉴权实测通过：真实家庭摄像头播放会话不含旧 Pose/scene 字段；实际 MJPEG 返回 `200` 和持续帧数据，协议头与会话 JSON 一致；临时验收会话已删除，服务 `active`、`NRestarts=0`。
- 盒子已部署并单次重启。摄像头 31 为 `192.168.1.11`，摄像头 32 为 `192.168.1.7`，两路均 `online`；新进程没有访问旧 `192.168.1.3`。有效输入约 `14.6 / 13.6 FPS`，云端完成接收约 `14.3 / 13.6 FPS`，上传失败均为 `0`。
- 原画与人物模糊的盒子成品流实测返回持续 JPEG、统一协议头和 `composition-owner=edge`。纯骨架未标记通过：源身份规则升级后历史空房校准不再匹配，两路必须在无人画面重新执行显式空房校准，再完成管理首页、算法诊断页和 TestFlight 三端验收。
- 正式部署目录只保留 `verify-vision-runtime.py` 一项运行时自检；其他 `verify-*.py`、历史备份、字节码和旧视频模块均不再部署。HailoRT 自身生成的运行日志仍需纳入日志轮转与容量统计。

## 2026-08-02 双摄空房校准与成品流复验

- 通过受鉴权管理端链路抓取当前原画，确认摄像头 31（电视柜上，`192.168.1.11`）和摄像头 32（冰箱上，`192.168.1.7`）画面均无人后，分别执行显式空房校准。
- 两路均一次完成 8 帧稳定空房确认和 3 帧当前流代复验，人物拒绝数为 `0`，状态为 `ready`；校准按摄像头、配置源身份、分辨率持久化，没有删除、重绑或修改摄像头连接配置。
- 六条用户成品流（两路摄像头乘原画、人物模糊、纯骨架）均返回持续 JPEG，响应统一声明 `edge-composed-mjpeg-v1` 和 `composition-owner=edge`。12 帧短测约 `8.4-12.1 FPS`，除一路人物模糊样本有 1 个重复 JPEG 外，其余样本均为新帧。
- 本次只关闭了“未校准导致纯骨架零帧”的运行阻塞，不能关闭 GH-002 至 GH-008。仍需真人进入、横向行走、静止、坐下、离开，并在管理首页、视觉算法页和 TestFlight 同时确认无人物像素、无残影、无双骨架、无串流和无客户端二次绘制；随后执行 30 分钟双摄稳定性测试。

### GH-047 安全升级后管理登录仍显示失效默认密码

**根因**：GH-037 部署时正确地将历史通用凭据 `admin / 123456` 轮换成逐设备一次性凭据，但旧登录 HTML 没有缓存约束，浏览器继续展示“开发阶段初始账号”；用户按错误提示重复登录后触发既有指数限流。一次性凭据只保存在盒子 `0600` 文件中，迁移时没有同步说明当前访问方式。
**处理**：受保护页面重定向统一携带认证静态版本；登录 HTML、脚本、样式和认证 API 统一返回 `no-store`；登录页只说明使用盒子初始化凭据。未认证状态继续隐藏一次性凭据和待改密细节，真实一次性状态只在成功登录响应和受认证状态中返回。
**验收**：旧缓存访问受保护页面后进入当前版本登录页；页面不再出现 `123456`；一次性凭据登录成功后必须改为至少 10 位的新密码；改密撤销全部旧会话并删除一次性凭据文件；失败限流、审计和重启登录继续有效。
**实机进展**：树莓派已部署并单次重启，服务 `active`、`NRestarts=0`、无 warning。管理页 GET 返回版本化 303 登录地址，登录页与认证 API 均返回 `no-store`；首次用户操作发现 8 位新密码被后端正确拒绝，但前端将 Pydantic 错误数组显示为 `[object Object]`，`auth-5` 已增加 10-128 位提交前校验、结构化错误格式化和不可解析响应处理。用户已完成合规密码设置并以新凭据进入管理端；盒子认证元数据为 `credential_kind=user_defined`、`must_change_password=false`，一次性凭据文件已删除。GH-047 已关闭。

### GH-048 算法诊断画面与用户隐私模式语义混合

**根因**：管理端在所有页面动态插入同一“画面隐私”选择器，但视觉算法页固定连接诊断 Pose 流。用户在该页选择“骨架”后，中间仍显示原画和黄色骨架，界面错误地暗示诊断画面就是用户隐私成品流。当前两路持久空房基线文件又已为空，云端期望模式仍为 `skeleton`，导致真实首页/App 成品流被阻断但管理页没有明确呈现。
**处理**：视觉算法页固定为不可切换的“原画 + 黄色骨架”诊断语义；隐私模式选择只属于管理首页和摄像头页，明确命名为“App / 首页画面”。用户纯骨架流在逐路校准未就绪时，前端不建立连接，后端在 MJPEG 建立前返回结构化 409，禁止 200 空流和任何原画降级。校准错误统一提取结构化中文 `message`。
**验收**：算法页没有可操作的隐私分段按钮且始终显示黄色诊断骨架；首页/App 的纯骨架画面只包含背景与蓝色骨架；未校准时显示明确状态、真实输出 FPS 为 0 且无空连接；两路重新空房校准后持久文件存在，重启后复验恢复，不需再次校准。
**实机进展**：完整边缘回归 `58/58` 通过并已部署。Pi 服务 `active`、`NRestarts=0`、warning 日志为空；盒子实际静态资源已是 `privacy-contract-1`。两路源流稳定约 15 FPS，Hailo Pose 与人物分割均为 `ready`、推理失败为 0。摄像头 32 已完成空房校准及当前流代复验并进入 `ready`；摄像头 31 的持久基线已恢复发现，但因真实人物在场保持 `revalidating / person_present / 0/3`。待 31 自动复验完成后，执行双路纯骨架与 TestFlight 同帧验收，再做单次重启持久化验收。

### GH-049 未校准纯骨架仍持续消耗人物分割算力

**根因**：纯骨架渲染先等待同步 Pose、执行 Hailo 人物分割和掩码传播，再由背景重建阶段发现空房基线不存在并抛出 `calibration_required`。两路最终零输出、零上传，但仍持续占用 Hailo、CPU 光流和内存带宽；Pi 现场达到约 `74°C`、进程约 270% CPU，Pose 模型锚点只有约 `2.25 / 3.20 Hz`。
**处理**：在背景重建器增加按摄像头、配置源身份和分辨率检查的正式基线前置门禁；纯骨架在任何 Pose 同步等待、人物分割、背景重建和 JPEG 编码前先验证持久基线。缺失基线或正在校准时立即阻断；持久基线存在但流代待复验时仍进入原有分割与场景复验流程，不能错误跳过安全复验。
**验收**：未校准纯骨架连续运行时分割调用、编码和上传均为 0；原画、人物模糊、显式校准、持久基线复验和已校准纯骨架行为不变。部署后以分割成功次数差分、CPU、温度、Pose 锚点 FPS 和服务告警为实机指标。
**实机结果**：完整边缘回归 `58/58` 通过后部署并单次重启。两路未校准持续运行期间分割成功次数、分割锚点和传播帧均保持 `0`，隐私提交/完成仍为 `0`；服务 `active`、`NRestarts=0`、warning 日志为空。Pi 温度由约 `74°C` 回落到 `65°C`，进程 CPU 由约 270% 回落到约 90%；有人画面的 Pose 模型锚点达到约 `14 Hz`，无人画面按调度策略约 `2 Hz`，两路展示约 `13-15 FPS`。GH-049 已关闭。

### GH-050 摄像头切换期间复用旧 MJPEG 像素

**现象**：管理首页或算法页从一路摄像头切换到另一路后，摄像头名称和请求地址已更新，但新流首帧到达前仍保留上一条流的最后画面，看起来像后端串流或跨摄像头污染。流不可用时旧画面也可能继续显示。
**根因**：页面跨摄像头复用同一个 `<img>` 元素；旧 `src` 的解码像素不会在赋新地址时自动清空。前端又在 900 毫秒后仅凭 `src` 存在隐藏加载层，没有等待真实首帧；旧会话的延迟 `load/error` 事件也没有摄像头和会话代次约束。首次替换节点的实现仍未在脱离 DOM 前移除旧 `src`，快速切换后旧 MJPEG HTTP 请求继续占用订阅和渲染资源，最终使新流长期停在加载中。
**处理**：每次建立或重连视频先提升 `streamGeneration`，再隐藏旧节点、解除旧 `load/error` 并移除旧 `src` 终止请求，然后创建新图像元素。新元素默认不可见，只有当前摄像头、当前代次的真实 `load` 事件才能显示。移除定时揭示逻辑，错误和加载期间始终显示明确占位状态；校准请求同时按摄像头持有唯一 pending 状态，轮询重绘不能制造重复提交或串按钮状态。
**验收**：管理首页与算法页连续切换 31、32 两路各 20 次；加载、失败和重连阶段不得出现上一摄像头像素，新首帧出现前占位层持续可见，名称、请求 URL 和实际场景始终一致。断开任一路后必须显示不可用而非旧画面；校准按钮单次点击只产生一次对应摄像头请求。
**当前进展**：第一轮本地实现、Node 语法、静态运行契约和完整边缘回归 `57/57` 通过并部署；实机 20 次切换在中途发现订阅者由正常每路约 2 个累积到 `4 / 5`，新首帧饿死。第二轮补齐连接终止、事件解除、代次顺序和独立缓存版本，完整边缘回归再次 `57/57` 通过并部署。真实 20 次交替切换中旧画面全部立即隐藏，首帧 `494-1312 ms`、中位 `751 ms`；订阅数稳定在测试前后的 `31:2 / 32:4`，源流约 `15 FPS`、重连为 0、Hailo 推理失败为 0。待断开一路的不可用状态验收后关闭。

### GH-051 公网媒体平面缺少正式鉴权与部署边界

**根因**：边缘持久 H.264 发布器已完成，但云端尚无固定版本 MediaMTX、路径级发布/读取鉴权、WHEP/ICE/TURN 边界和可审计部署配置。如果直接开放 RTSP 和 WHEP，任意设备可覆盖流，过期家庭成员仍可读取，隐私模式变化后旧会话也可继续使用。
**处理**：新增独立 `MediaAccessService`。盒子发布必须使用已签发设备令牌，且 `live/{device}/{camera}` 路径必须与设备、家庭、启用摄像头完全对应。App 读取会话使用最长 5 分钟、默认 120 秒的 HMAC 令牌，绑定用户、家庭、设备、摄像头、路径、动作、隐私模式和时间；每次 MediaMTX 建连再核对当前成员资格、归属和隐私模式。内部鉴权路由同时要求本机回环来源和独立共享密钥，只返回 HTTP 决策，不泄露令牌或家庭数据。
**部署边界**：固定 MediaMTX `v1.19.3`，仅允许 RTSPS/TCP 发布和 WHEP/WebRTC 读取；管理 API、指标和 WHEP 监听仅回环，nginx 只暴露 `/media/`；公网仅开放 `8322/tcp`、`8189/tcp+udp`、`3478/tcp` 和 Coturn `49160-49200/udp` 中继范围。禁用 RTMP、HLS、SRT、MoQ、录像和任意路径，禁止新发布者踢掉现有发布者。Coturn 使用独立 systemd 单元和 `root:turnserver / 0640` 配置文件，密钥不得进入进程参数；Let’s Encrypt 部署钩子验证证书/私钥匹配后原子更新 MediaMTX 证书并重启服务。
**本地验证**：鉴权专项 `3/3` 通过，覆盖设备令牌哈希、路径篡改、归属变化、成员撤销、隐私变化、到期、内部调用者密钥和健康信息脱敏。完整云端回归 `104` 项中 `103` 通过、`1` 项因本机未配置 PostgreSQL 集成地址跳过；MediaMTX YAML 已独立解析通过。
**WHEP 接入进展**：正式播放会话已改为调用 `MediaAccessService`，返回短时 WHEP URL、Bearer 权限和会话元数据，不再签发播放票据或暴露 MJPEG 产品地址。iOS 原生客户端在会话创建后用同一 Bearer 权限完成 WHEP 能力发现和 SDP 交换，并拒绝非 HTTPS、跨源资源地址和过期会话。旧云端/App 正式 MJPEG 路径已删除；当前剩余边界是生产媒体服务和 TURN 的真实部署、盒子 H.264 发布以及 TestFlight 实机验收。
**生产部署进展**：云服务器已安装并校验官方 MediaMTX `v1.19.3` 与 Coturn `4.6.1`。首次部署发现远程脚本把三组密钥写成字面量占位符，同时 `/etc/gohome` 的目录权限阻断专用服务账号读取配置；占位符已全部替换为三组独立随机密钥。Coturn 已改为读取 `/etc/gohome/coturn/turnserver.conf`，MediaMTX 独占 `/etc/gohome/media`，两个目录分别由 `turnserver` 与 `mediamtx` 组管理。账号下实际资源已核实为轻量应用服务器 `lhins-rf6o2tns`（`118.25.151.101`），而非 CVM；轻量防火墙已最小放通 `8322/tcp`、`8189/tcp+udp`、`3478/tcp` 和 `49160-49200/udp`，未修改域名、证书、`80/443` 或原有规则。外网 TCP 建连、`8322` TLS 1.3 证书校验和 UDP 抵达 `8189/49160` 均通过；`gohome-mediamtx` 与 `gohome-coturn` 稳定运行。尚需新云端 API 原子切换、盒子 H.264 真实发布、直连 ICE、强制 TURN 和 TestFlight 双摄 30 分钟验收，GH-051 保持处理中。
**验收**：匿名发布/读取拒绝，跨设备、跨摄像头和过期成员拒绝，隐私模式变化后旧会话立即失效；直连 ICE 和强制 TURN 均通过双摄 30 分钟 TestFlight 验收后才能关闭。

### GH-052 云端原子发布在 unit 安装失败后留下错误指针

**根因**：安装器进入发布 staging 目录执行 `npm ci` 后，继续使用调用方传入的相对 `gohome-app.service` 路径。文件因当前目录变化而不可见，但此时 `/opt/gohome/current` 已经切到新发布；原 trap 只清理 staging 和临时链接，不恢复流量指针。
**处理**：安装开始时将归档和 unit 文件解析为绝对路径；完整发布目录就位后先安装 unit 并 `daemon-reload`，然后才原子切换 `current`。切换后的重启、健康检查或任何命令失败统一触发 EXIT 事务：恢复旧指针、重启旧版本、清理失败发布目录。
**生产验证**：首次失败未重启生产进程，旧 API 持续 `active`，`current` 已显式恢复为 `/opt/gohome/app`。修正脚本通过 `sh -n` 与路径顺序检查；随后从干净提交构建并成功切换到不可变发布 `/opt/gohome/releases/20260804035908-aac1a9d2333d`。API 健康检查通过、`NRestarts=0`，MediaMTX 与 Coturn 保持 `active`，GH-052 已关闭。

### GH-053 云端发布遗漏 PostgreSQL 迁移

**现象**：新云端 API 已成功切换并通过健康检查，但盒子同步摄像头配置时返回 `HTTP 500`，生产日志显示 `media_assets.retention_class` 不存在。
**根因**：发布包包含 `local-app-server/migrations/*.sql`，却遗漏唯一迁移执行器 `scripts/apply-postgres-migrations.js`；安装器只安装 Node 依赖、切换版本并重启服务，没有在切流前执行迁移。应用健康检查没有覆盖当前完整数据库结构，因此代码版本和 PostgreSQL Schema 发生漂移。
**处理**：发布清单显式包含迁移执行器；安装器在切流前以 `gohome` 服务账号读取正式环境并执行所有按版本排序、校验和保护的迁移。迁移失败时禁止安装 unit、切换 `/opt/gohome/current` 或重启服务。数据库迁移不随应用文件回滚，因此每个迁移必须与上一应用版本向后兼容，禁止破坏性字段删除和语义替换。
**验收**：新发布归档同时包含迁移执行器和完整迁移目录；生产 `schema_migrations` 登记至 `013_media_lifecycle.sql` 且校验和一致；`media_assets.retention_class` 等生命周期字段存在；盒子配置同步恢复 2xx；发布后 API、MediaMTX、Coturn 均无重启和新增 5xx。重复部署或重复执行迁移必须幂等，不产生重复字段、索引或种子数据。
**生产验证**：修复已由提交 `aac1a9d` 独立推送。新归档包含迁移执行器、完整迁移目录及云端入口，共 125 个受控文件；安装阶段 `001` 至 `012` 校验和一致并跳过，`013_media_lifecycle.sql` 成功应用。生产 `schema_migrations` 共 13 条且最新版本为 `013_media_lifecycle.sql`，8 个媒体生命周期字段全部存在。切流后盒子 `/api/v1/device/sync` 从连续 500 恢复为连续 200，云端三个服务均为 `active`、API `NRestarts=0`。GH-053 已关闭。

### GH-054 COS 证据前缀与最小权限策略不一致

**现象**：数据库迁移恢复后，盒子历史证据上传持续返回 403；云端媒体生命周期任务每五分钟报告 `Access Denied`。
**根因**：生产 CAM 子账号只被授权访问 `memory-media/*` 与 `event-evidence/*`，云端代码却将盒子证据写入 `edge-evidence/*`。同一密钥在 `memory-media/*` 的 Put/Head/Delete 实测全部成功，证明密钥、存储桶和地域配置有效；`edge-evidence/*` 的 Put/Head/Delete 全部被策略正确拒绝。生命周期孤儿对账还调用桶级 `GetBucket`，现有策略只含对象操作，因而两个受管前缀的分页清单均被拒绝。
**处理**：删除失配的 `edge-evidence/` 运行时语义，上传、生命周期、测试与产品文档统一使用 `event-evidence/`；由单一共享常量约束证据和家庭记忆前缀。进一步核对正式 SDK 后确认 `getBucket` 会把请求的 `Prefix` 作为授权 `ResourceKey`，因此把 `GetBucket` 单独授权给不带路径的桶资源无法命中。正确最小策略是将 `GetBucket` 与 Put/Get/Head/Delete 放在同一条语句中，共同限定为 `memory-media/*` 和 `event-evidence/*`，禁止裸桶列举、`cos:*` 和公共访问。
**验收**：生产密钥对 `memory-media/*` 与 `event-evidence/*` 可 Put/Head/Delete，对其它前缀保持拒绝；两个受管前缀可完整分页列举。盒子失败队列恢复 200 并幂等排空，数据库对象键均使用正式前缀；生命周期 dry-run 成功返回盘点结果且不删除对象，确认清单后才启用真实删除。
**生产验收**：策略版本 4 已设为当前版本，`GetBucket` 与对象 Put/Get/Head/Delete 已共同限定到两个受管前缀。生产分页列举成功：`event-evidence/` 119 个对象、`memory-media/` 16 个对象；未授权 `unmanaged-probe/` 列举和写入均保持 403。两个受管前缀的临时 Put/Head/Delete 均成功，删除后 Head 返回 404。两项历史权限测试对象已清理。生命周期 dry-run 手动和自动周期均成功：资产 1033、COS 对象 133、COS 孤儿 0、失败 0、删除 0；自动周期时间为 `2026-08-04T05:58:34Z`。策略更新后生产日志无新增 `AccessDenied`，API、MediaMTX、Coturn 均 active，API `NRestarts=0`。GH-054 已关闭。
