# GoHome 产品交付问题台账

更新时间：2026-08-07
适用范围：树莓派 5 + Hailo-8 HAT+ 盒子、生产云端、盒子管理端、正式 GoHome iOS App
正式 iOS 工程：`ios-shell`
正式 Bundle ID：`com.gohome.family`

## 处理规则

- 状态只使用：`待处理`、`处理中`、`待实机验收`、`已关闭`、`阻塞`。
- `已关闭` 必须同时具备：根因消除、自动回归通过、运行指标达标、产品文档同步、真机验收通过。
- 禁止用 UI 隐藏、异常兜底、重复管线或降低标准代替根因修复。
- 盒子是隐私画面的唯一合成方；云端、管理端和 App 只传输和显示完整成品帧。
- 旧 WebView “回家” iOS 工程不参与开发、构建、测试或 TestFlight。

## 隐私模式产品契约

| 模式 | 唯一正确行为 |
| --- | --- |
| `original` | 显示未经隐私处理的当前摄像头画面，不绘制骨架。 |
| `person_blur` | 保留当前场景，只模糊当前帧中的人物，不绘制骨架。 |
| `skeleton` | 保留当前场景，完全移除当前帧中的全部人物像素，只绘制一套与该帧同步的实时骨架。 |

骨架模式禁止出现：模糊人物、黑底、中性色块、矩形替换、修复扭曲、历史人物像素、人物残影、双重骨架、跨摄像头像素、客户端二次绘制、旧帧倒退。

## 当前交付门槛

| 编号 | 优先级 | 问题 | 状态 |
| --- | --- | --- | --- |
| GH-001 | P0 | PRD、Plan、Implement 将“模糊 + 骨架”错误记录为正式骨架模式 | 已关闭 |
| GH-002 | P0 | 盒子骨架渲染直接调用人物模糊，违反产品契约 | 待实机验收 |
| GH-003 | P0 | 背景校准、持久化与场景变化状态机不完整 | 待实机验收 |
| GH-004 | P0 | Pose、人物掩码、视频帧缺少不可分割的同帧完成契约 | 待实机验收 |
| GH-005 | P0 | iOS 骨架流出现“服务器返回无法识别的响应” | 已关闭 |
| GH-006 | P0 | 云端同时保留 Pose SSE、safe-scene 和合成 MJPEG 三条骨架路径 | 待实机验收 |
| GH-007 | P0 | 多路并发 JPEG 上传产生乱序、忙丢帧和大量云端 stale reject | 待实机验收 |
| GH-008 | P0 | 双摄真人骨架模式尚未通过无残影、无泄漏、无串流验收 | 待处理 |
| GH-009 | P1 | 每个摄像头独占分割 HEF runtime，Hailo 运行时所有权重复 | 待实机验收 |
| GH-010 | P1 | OpenCV/FFmpeg 解码、CPU 合成和 JPEG 链路存在重复复制与编码 | 处理中 |
| GH-011 | P1 | 当前端到端仅约 6.5-8.5 FPS，明显低于约 12-15 FPS 的实际源流 | 处理中 |
| GH-012 | P1 | 独立旧视频服务与现行云中继重叠，并暴露已失效的丢帧控制 | 待实机验收 |
| GH-013 | P1 | SQLite、快照、上传队列、COS 对象没有统一留存清单和对账任务 | 处理中 |
| GH-014 | P1 | 推送需要事件级幂等、单次通知、回执和延迟监控的生产复验 | 待实机验收 |
| GH-015 | P1 | 家庭固定位置、手机当前位置、回家距离、社区家庭位置需闭环复验 | 待实机验收 |
| GH-016 | P1 | 宠物类别和人物误检没有正式标注集、混淆矩阵及阈值验收 | 待处理 |
| GH-017 | P2 | 管理端展示由公式推导的“准确率”，容易被误认为模型实测指标 | 待实机验收 |
| GH-018 | P2 | iOS `AppEnvironment` 集中装配过多接口和兼容上传逻辑 | 待实机验收 |
| GH-019 | P2 | 本地与盒子部署包含备份、旧入口、旧权重和生成物，缺少清理清单 | 待实机验收 |
| GH-020 | P0 | 尚未形成可重复的双摄长时、断线、隐私、跌倒、推送和 TestFlight 交付报告 | 待处理 |
| GH-021 | P1 | 摄像头 DHCP 地址变化后 App 无法原位编辑连接配置，只能误导用户删除重建 | 待实机验收 |
| GH-022 | P0 | 摄像头连接配置更新后旧读取器继续运行，数据库状态与实际采集源分裂 | 待实机验收 |
| GH-023 | P0 | 冻结和重复画面仍被计入实时 FPS，旧姿态结果没有随源停滞失效 | 待实机验收 |
| GH-024 | P0 | 管理端算法诊断骨架错误依赖全局隐私模式，并可能用 Tracker 旧像素替代当前视频 | 处理中 |
| GH-025 | P0 | Tracker 短暂丢失目标时把旧 `coasting` 骨架绘制到后续新视频帧，形成残留骨架 | 待实机验收 |
| GH-026 | P0 | 电视、灯光等短暂变化会删除持久空房校准，重启后骨架流失效 | 处理中 |
| GH-027 | P0 | 隐私模式只有全局期望值，没有逐路就绪校验和原子生效状态 | 处理中 |
| GH-028 | P0 | 隐私渲染异常被静默吞掉，HTTP 保持成功但没有任何成品帧 | 处理中 |
| GH-029 | P0 | 近黑新帧会被旧画面替换，却沿用新帧身份和时间戳 | 待实机验收 |
| GH-030 | P0 | Hailo Pose、分割和物体模型共用低频串行整管线，真实 Pose 锚点仅约 1-2 Hz | 待实机验收 |
| GH-031 | P1 | 源解码、有效输入、端侧合成、云端接收和客户端显示 FPS 口径混用 | 处理中 |
| GH-032 | P1 | 管理诊断流与隐私成品流骨架颜色、粗细和语义不统一 | 处理中 |
| GH-033 | P1 | 正式实时链路仍是逐帧 JPEG/MJPEG，缺少持久编码视频传输 | 处理中 |
| GH-034 | P1 | 盒子入口、Worker 和 Storage 职责过度集中，生产与历史云业务混杂 | 处理中 |
| GH-035 | P0 | 自动回归未覆盖重启、电视变化、逐路校准差异和 TestFlight 长时真实链路 | 处理中 |
| GH-036 | P1 | 盒子生产接口仍提供模拟天气和本地用户/家庭/消息等云端职责 | 处理中 |
| GH-037 | P0 | 盒子管理端保留固定默认密码且默认不强制修改，登录缺少失败限流 | 待实机验收 |
| GH-038 | P0 | 应用和模型升级包缺少签名/校验强制验证，归档解压存在目录穿越与任意执行风险 | 待实机验收 |
| GH-039 | P1 | 盒子残留云端媒体上传接口，存在未定义变量、整包入内存和双数据源问题 | 待实机验收 |
| GH-040 | P1 | 盒子残留 macOS LaunchAgent、旧 Web App 链接和直连 APNs 等非生产职责 | 处理中 |
| GH-041 | P0 | 当前骨架显示错误复用跌倒证据门槛，遮挡或半身场景直接丢弃可用 Pose | 处理中 |
| GH-042 | P1 | EACP 风险图和姿态规则由手工阈值堆叠，缺少版本化数据集与量化验证 | 处理中 |
| GH-043 | P0 | 火灾功能仅使用暖色纹理启发式，却以“火灾应急报警”产品语义暴露 | 处理中 |
| GH-044 | P1 | 管理端保留三套失效运行时和历史跳转页面 | 待实机验收 |
| GH-045 | P1 | 管理端保留失效算法剧场和无依据百分比指标 | 待实机验收 |
| GH-046 | P0 | 暖色启发式仍进入正式安全事件与通知链路 | 待实机验收 |
| GH-047 | P0 | 安全升级后管理登录仍显示失效默认密码 | 已关闭 |
| GH-048 | P0 | 算法诊断画面与用户隐私模式语义混合 | 待实机验收 |
| GH-049 | P1 | 未校准纯骨架仍持续消耗人物分割算力 | 已关闭 |
| GH-050 | P0 | 切换摄像头时旧 MJPEG 像素会显示在新摄像头名称下 | 待实机验收 |
| GH-051 | P0 | 公网媒体平面缺少正式鉴权与部署边界 | 处理中 |
| GH-052 | P0 | 云端发布安装器使用相对 unit 路径且中途失败不回滚 `current` | 已关闭 |
| GH-053 | P0 | 云端发布遗漏 PostgreSQL 迁移，代码与数据库结构漂移 | 已关闭 |
| GH-054 | P0 | COS 证据前缀与最小权限策略不一致 | 已关闭 |
| GH-055 | P0 | 事件证据关系存在 JSON 双数据源，历史资产无法可靠归属和留存 | 处理中 |
| GH-056 | P0 | 无法确定归属的历史媒体没有不可删除的审计状态 | 已关闭 |
| GH-057 | P0 | 历史数字媒体 ID 复用导致 227 个本地文件失去可靠数据库身份 | 已关闭 |
| GH-058 | P0 | 持续 Pose Tracker 将仍有效的晚到 Hailo 结果丢弃，确认无人也不能及时清除旧骨架 | 待实机验收 |
| GH-059 | P1 | H.264 发布器使用墙钟生成重复 DTS，重启原因、错误恢复和字节指标语义不正确 | 已关闭 |
| GH-060 | P0 | 服务重启后单次几何证据不足被误判为摄像头移动，两路持久骨架基线永久阻塞 | 已关闭 |
| GH-061 | P0 | TestFlight Build 9 仍使用旧 MJPEG 会话契约，生产云切换 WHEP 后三种模式均无法解码 | 已关闭 |
| GH-062 | P0 | iOS WHEP 错误等待 ICE gathering complete，Build 10 在 SDP POST 前超时 | 已关闭 |
| GH-063 | P0 | MediaMTX systemd 缺少 `AF_NETLINK`，全部 WHEP SDP 协商返回 400 | 已关闭 |
| GH-064 | P1 | 本地闭环校验器把合法空老人资料和干净开发拓扑当成产品故障 | 已关闭 |
| GH-065 | P1 | 采集层丢弃未冻结的重复解码帧，导致 H.264 发布低于摄像头实际解码节奏 | 待实机验收 |
| GH-066 | P0 | 摄像头断流恢复后空房隐私复验卡死，成品发布无法自动恢复 | 待实机验收 |
| GH-067 | P0 | App WHEP 会话失败后没有持续自动恢复 | 处理中 |
| GH-068 | P0 | 可转动摄像头只有单一空房基线，已确认机位变化后仍被错误阻断 | 处理中 |
| GH-069 | P1 | 正式 App 首页 Hero 布局被图片比例撑高，社区推荐图片失败时显示空白占位 | 待实机验收 |
| GH-070 | P1 | 共享 Web 客户端用旧用户接口 fallback 掩盖正式 API 契约漂移 | 已关闭 |

## GH-001 文档产品契约错误

**现象**：产品文档把人物模糊底图叠加骨架记录为正式完成方案。
**根因**：骨架隐私的视觉定义没有成为跨端唯一契约。
**处理**：同步修正 PRD、Plan、Implement，废止 Build 8 的错误完成结论。
**验收**：三份文档对三种模式的定义完全一致，并明确盒子是唯一合成方。

## GH-002 骨架实现调用人物模糊

**现象**：骨架模式与模糊模式视觉相同，只多一套骨架。
**根因**：`PrivacyFrameRenderer._render_skeleton()` 直接调用 `_render_person_blur_for_camera()`。
**处理**：建立独立的纯骨架合成器，不复用模糊模式输出。
**验收**：人物区域没有任何原始或模糊人物像素，场景非人物区域保持当前帧，且只有一套骨架。

## GH-003 背景模型污染和伪造区域

**现象**：人物残影、旧人物矩形、家具纹理扭曲、黑场或中性色块。
**根因**：早期自动学习和中性色填充虽已删除，但当前显著场景不匹配仍调用 `_invalidate()` 清空内存背景并删除持久文件；用户重校准一开始也先删旧基线，超时或磁盘失败后校准会长期停在 active。持久资产、流代复验、场景变化和校准事务仍被混成一个布尔状态。
**处理**：按 `camera_id + configured_source_key + resolution` 保存持久空房基线，流代只作为运行复验状态。新校准使用候选平均帧，先压缩、fsync、原子替换磁盘文件，成功后才切换内存基线；重复校准直接拒绝，超时、人物进入、分割不可用和磁盘失败统一取消候选并保留旧基线。电视、灯光和局部物品变化通过全局色偏抵消和可见区域残差判断继续使用；摄像头移动或强场景变化只进入 `scene_review_required`、阻断骨架并保留基线，不自动删除。状态公开基线 SHA-256、进程内 revision、场景匹配率、残差、复验进度和场景复核次数。
**本地验证**：专项覆盖未校准、纯骨架、近期人物掩码、相邻 Pose 拒绝、新流代复验、服务重启、电视局部变化、整体灯光变化、摄像头移动、移动后旧基线恢复、失败重校准、并发重校准、磁盘原子提交失败和成功替换新基线。API 专项确认成功唤醒中继，超时和持久化失败返回结构化状态且取消 active；云中继能区分 calibrating、revalidating、scene_review_required 和 calibration_required。完整边缘回归通过后保持“待实机验收”。
**启动恢复修复**：背景重建器现在会按当前摄像头配置主动发现持久 `.npz`，服务重启后公开 `calibrated=true`、`baseline_retained=true`、`ready=false`、`status=revalidating`，不再把已存基线误报为“未校准”。复验独立于当前隐私模式运行，每个源帧只允许计入一次；等待复验的纯骨架流可以明确返回复验状态，不再因状态未实例化形成死锁。
**首轮实机反证**：最初复验只要求 Pose 同帧且以背景局部变化判断人物，摄像头 31 的有人画面被错误接受为 `local_change`。现已改为每次复验强制执行当前帧 Hailo 人物分割锚点；Pose 或分割任一发现人物都将进度重置为 `0/3` 并返回 `person_present`，禁止使用传播掩码替代真实锚点。
**负载修复与现场状态**：强制锚点首版挂在每个实时帧入口，有人持续停留时会逐帧重试并把该路成品输出压低。现按 `camera_id + source_key + resolution` 使用线程安全调度，每路首次立即检查、之后每 1 秒最多一次，每次仍为独立 Hailo 锚点；调度间隔、活动流和下次检查时间进入 `/health`。提交 `5642c9d` 完整边缘回归 `58/58` 后部署 Pi，服务 `active`、`NRestarts=0`，两路源流约 `15 FPS`、重连与 Hailo 失败均为 0。摄像头 31 的 `.npz` 文件 SHA-256 仍为 `291639114ffe4064d3e11e503a150ccb7b8b4f5dbd56bc992bf070a970256bd8`，基线像素 SHA-256 为 `cc8ff649c3611f51ae063ba1c089758140415c48c56d12d4566750ffcab5f969`；20:51 现场证据清楚显示两个人，因此状态正确保持 `revalidating / person_present / 0/3`。摄像头 32 随后在真实空房画面完成 `8/8` 校准与 `3/3` 独立 Hailo 复验，拒绝数为 0，状态为 `ready`；持久文件大小 `369281` 字节，文件 SHA-256 为 `8a2cda89ea9412841142193ef7ea9114c4ccb8c9ca51d7ec271ddd2a4fd63344`，基线像素 SHA-256 为 `05eae00d1c0faa0fc818d05e68a70a707130d91e9e3647e1e63e456918a49aab`。当前仍只有一路 ready，不能开始双路纯骨架交付验收。
**自动复验与纯骨架现场结果**：摄像头 31 在真实空房后自动完成 `3/3`，调度活动流清零且两份 `.npz` 文件哈希均未变化；两路同时 `ready` 后管理端正式切换为 `skeleton`。摄像头 32 的当前成品帧只显示一套蓝色骨架，人物像素由持久空房背景替换，没有模糊人物、双骨架或跨路像素；端侧约 `12.8-13.6 FPS`，云端接收约 `10.5-11.4 FPS`，上传失败为 0。随后人物重新进入摄像头 31 画面，该路正确回到 `revalidating / person_present / 0/3`，端侧和云端输出 FPS 均为 0，未回退原画或输出旧帧；摄像头 32 保持 `ready`。因此只能确认单路纯骨架视觉与阻断契约，GH-003 和双路交付门槛仍不关闭。
**验收**：两路分别完成空房校准，执行人物进入/停留/离开、电视内容变化、开关灯、移动局部物品、服务重启、断流重连和摄像头真实移动。前六类不得丢失持久基线或产生残影；真实移动必须立即阻断骨架、管理端显示“场景变化，需重校准”，重校准成功后原子替换并恢复。原画和模糊模式始终可用。

## GH-004 同帧身份和完成帧契约缺失

**现象**：骨架错位、旧结果覆盖新画面、跨摄像头内容和画面倒退。
**根因**：视频、Pose、分割和背景结果可以由不同线程、不同代次和不同时间完成。
**处理**：所有输入与输出强制携带并校验 `camera_id`、`source_key`、`stream_generation`、`frame_id`、单调时钟和合成版本。
**验收**：任一身份不一致的结果不得进入成品帧；两路摄像头连续运行时零串流、零旧帧回写。

## GH-005 iOS 与生产云端协议不一致

**现象**：骨架模式提示“服务器返回无法识别的响应”。
**根因**：手机当前安装的 TestFlight `1.0.0 (8)` 归档于 2026-08-01；其骨架分支要求已经废止的 `safe-scene-pose-v1` 分拆协议，由 App 分别取得安全场景和 Pose 后二次绘制。2026-08-03 的 `2678233` 已将正式链路收敛为盒子唯一合成的 `edge-composed-mjpeg-v1`，生产云会话和实际 MJPEG 响应均正确声明 `composition-owner=edge`。Build 8 在骨架分支主动拒绝新协议并抛出 `APIError.invalidResponse`；原画和模糊没有进入旧分拆分支，因此仍可播放。
**处理**：不恢复旧 Pose/scene 路由，不放宽协议校验，不增加兼容兜底。正式 iOS 已删除 Pose SSE、scene MJPEG 和客户端骨架绘制，只消费盒子生成的一条成品 MJPEG，并同时校验会话 `display_transport=edge-composed-mjpeg-v1` 与响应头 `X-GoHome-Composition-Owner=edge`。版本提升至 `1.0.0 (9)`，从唯一正式工程 `ios-shell/GoHomeShell.xcodeproj` 重新归档上传。
**发布前验证**：Build 9 单元测试 `128/128`、UI 测试 `25/25` 全部通过，`xcodebuild` 返回 `TEST SUCCEEDED`；版本源 `project.yml` 与生成工程均为 Build 9。纯骨架双摄连续监控 `1804.6` 秒、`360` 次采样，读取错误、模式不一致、服务故障、重连和上传失败均为 `0`；摄像头 31/32 的云端帧龄 P95 分别为 `217.5 / 180.3 ms`。Build 9 已于 2026-08-03 23:21 上传 App Store Connect，Delivery UUID 为 `d067a363-40e2-4723-93e6-e32282de6bba`，上传无错误或警告。
**TestFlight 验收**：用户已从 TestFlight 安装 Build 9，并在真实账户、生产云和双摄盒子环境进入骨架模式；画面可见、显示约 `7-13 FPS`，不再出现“服务器返回无法识别的响应”。同时段生产云存在 `1` 个正式视频客户端、Pose/scene 客户端均为 `0`，证明 App 使用唯一成品流而非旧分拆协议。GH-005 协议故障关闭；FPS 波动和持久视频传输继续由 GH-010、GH-011、GH-033 处理，不能用协议通过替代性能达标。
**验收**：TestFlight 从前台、后台恢复、换摄像头和换隐私模式均不会触发协议错误。

## GH-006 多条骨架展示路径

**现象**：管理端黄色骨架、App 蓝色骨架、粗细骨架重叠。
**根因**：服务端烘焙、Pose SSE 和客户端覆盖层同时存在。
**处理**：正式链路只保留盒子完成帧；删除云端兼容 Pose/scene 正式入口和所有客户端骨架绘制。
**验收**：管理端和 App 对同一 JPEG/视频帧显示完全一致的颜色、位置和数量。

## GH-007 实时上传调度错误

**现象**：流越看越慢、偶发卡死，云端累计大量 stale reject。
**根因**：每路允许多个 HTTP POST 并发，完成顺序不同于源帧顺序；槽位占满时丢弃当前新帧。
**处理**：每路一个在途请求和一个可覆盖的最新待发送帧，不建立积压队列。
**验收**：持续 30 分钟帧龄不增长，发送序号单调，stale reject 接近零，断网恢复后不补发旧直播帧。

## GH-008 双摄纯骨架验收缺失

**验收动作**：两路分别执行空场校准、进入、横向行走、静止、坐下、躺下、快速跌倒模拟、离开、遮挡、摄像头重连。
**通过标准**：无人物像素、无残影、无重复骨架、无串流；跌倒事件只通知一次；原画和模糊模式不受骨架链路影响。

## GH-009 Hailo 运行时重复

**现象**：共享 VDevice 上同时存在 Pose、Object 及两路 Segmentation runtime。
**处理**：每种 HEF 只实例化一个 runtime，使用跨摄像头的有界最新帧调度器。
**验收**：runtime 数量与 HEF 类型一致；双摄长时运行无内存增长、无饿死、无跨路状态。

## GH-010 至 GH-012 视频性能与旧链路

**处理**：测量 RTSP 实际码流；评估 GStreamer 硬件解码与 Hailo 管线；减少 BGR/JPEG 往返；最终使用持久 H.264/H.265/WebRTC，MJPEG 仅保留明确降级用途；移除重叠视频服务和失效参数。
**正式架构定稿（2026-08-03）**：盒子继续作为唯一隐私合成方，每路摄像头只有一个采集所有者和一个成品帧发布者。`PrivacyFrameRenderer` 直接输出 BGR 成品帧，常驻 FFmpeg 进程使用 `libx264 + ultrafast + zerolatency + yuv420p + 无 B 帧 + 约 1 秒关键帧` 编码，并通过 RTSP/TCP 持久发布到云端 MediaMTX。云端不解码、不重新合成、不缓存 JPEG，只做鉴权、媒体转发和可观测性；iOS 通过 WHEP/WebRTC 接收 H.264 并使用原生 WebRTC 视频渲染。正式 App 不允许自动回退 MJPEG，MJPEG 只保留在盒子局域网管理诊断接口，不能成为第二条公网产品链路。
**设备结论**：当前 Pi 5 系统虽列出 `h264_v4l2m2m`，真实编码探测返回无可用设备，不能把不存在的硬件编码能力写进方案。运行中使用 `libx264` 对 `640x360@15 FPS`、450 帧基准达到约 `36.1x` 实时、CPU 时间约 `1.4s`、峰值内存约 `63 MB`、码率约 `1.26 Mbps`；双路软件 H.264 编码具备明确余量。Hailo 只负责 Pose、分割和目标模型，不承担视频编码。
**验收**：所有 FPS 都标明源流、端侧合成、云端接收和客户端解码口径；不得以重复帧或 UI 刷新伪造 FPS。

GH-012 的本地结构已收口：边缘端删除独立 `video_app.py`、视频节点调度、旧视频档位、播放票据和全部 `/api/app/*`、`/api/v1/video/*` 路由；管理端仅保留 `/api/cameras/*` 局域网管理与诊断 MJPEG。旧 `/api/v1/device/cameras/*/stream.mjpg` 公网设备入口及宽松的设置令牌兼容已删除；正式公网视频只允许盒子通过 RTSPS 发布 H.264，云端通过短效鉴权 WHEP 会话向 App 转发。事件证据和受鉴权媒体读取统一归属云端；正式 App 的 `/api/v1/video/*` 始终归生产云端，不由盒子实现。尚需树莓派部署、生产云端和 TestFlight 实机确认后关闭。

## GH-013 存储与 COS 生命周期

**现象**：盒子数据库曾膨胀到约 1.10 GB，本轮检查仍有约 735 MB 已分配空间和大量可复用页；旧清理会先删 SQLite 行再删物理文件，COS、快照、对象目录和运行日志没有同一容量口径。
**根因**：媒体没有保留类别、删除状态、失败重试和删除审计；盒子只按表龄删除，云端只回收过期上传意图，没有正式资产保留和 COS/本地孤儿对账。首轮云端生命周期虽然补齐正式资产重试，但 COS/本地孤儿失败仍只依赖下个周期隐式重试，尝试次数、下次重试、对象消失和每次运行结果都没有持久记录。
**处理**：盒子新增持久化媒体生命周期队列，统一快照、对象、上传会话和事件留存；在同一写事务中先删除物理字节，再提交引用清理，失败保留原行并指数退避重试。容量统计纳入 SQLite/WAL/SHM、快照、对象存储和运行日志，高水位按最老且不受保护数据循环回收并保留每路最新帧。云端新增资产分类、删除墓碑、失败重试、COS 分页清单、按存储后端隔离的孤儿对账、运维 dry-run，以及迁移 `013_media_lifecycle.sql`、`014_media_orphan_cleanup.sql`。
**保留规则**：家庭记忆和用户上传由用户管理；未解决严重事件持续保护；严重事件证据 180 天、普通事件证据 90 天、验证证据 30 天、未引用临时上传 7 天、COS/本地孤儿宽限 2 天，均可由生产环境变量调整。
**本地验证**：云端全量 `101` 项中 `100` 通过、`1` 项因无 PostgreSQL 集成地址跳过；边缘媒体生命周期、运行历史、环形留存、外键完整性、容量口径和失败重试回归通过。
**盒子验证**：保留真实数据部署后发现历史物理删除摄像头造成 `10,476` 条子表外键违规。根因修复为所有连接强制启用 SQLite 外键、摄像头改为清除凭据的软归档、启动时恢复历史摄像头锚点，生命周期目标外键使用 `ON DELETE SET NULL`。迁移后外键违规为 `0`、`integrity_check=ok`，30 条历史摄像头均无凭据且产品列表只显示 2 路活动摄像头。数据库从 `735,203,328` 字节压缩到 `159,166,464` 字节，回收 `576,036,864` 字节；重启后服务 active、`NRestarts=0`。
**生产只读审计**：自动删除保持关闭。生产数据库共有 1033 个活动资产、470 个事件和 21 个家庭记忆媒体引用；COS 两个受管前缀共 133 个对象且孤儿为 0。本地媒体目录 1102 个文件、约 109.9 MiB。首轮 dry-run 报告 73 个到期资产和 227 个本地孤儿候选。逐项引用审计确认 73 个到期资产全部位于 `care-cards/`，其中 52 个仍由当前 `care_cards.image_url` 精确引用；原引用图只保护事件和家庭记忆，会误删仍在产品中展示的康养卡片图片。引用保护修复部署后，82 个当前卡片图片全部进入保护，保护数由 191 增至 273，删除计划由 73 降至 22，删除、失败和 COS 孤儿均为 0。进一步审计 46 个 `family_memory` 资产，只有 21 个仍由正式记忆引用；其余 25 个已无上传意图和正式引用，占约 13.6 MiB，当前仅凭 purpose 永久保护会形成持续泄漏。
**结构修复**：生命周期不再读取服务启动时的 PostgreSQL 快照，而是每次直接读取最新资产、事件、`family_memory_media`、卡片、账户头像和上传意图。正式记忆只按真实关联保护；完成上传但未发布、或删除记忆后失去关联的媒体回到 7 天临时资产保留规则。分类字段使用专用 PostgreSQL 批量更新，不再为每个资产触发全 Store 序列化和多表差异扫描。新增 `classification_only` 阶段：允许在真实删除关闭时持久化分类和保留期限，但所有正式资产与孤儿仅盘点、不删除。227 个本地孤儿候选中包含 11 个历史 `memories/` 文件，真实删除前继续按目录、数据库引用、年龄和容量分组审计，不按文件名或目录直接回收。
**分类阶段生产验收**：最新版本首次自动执行 `classification_only`，扫描 1036 个资产、分类 1036 个、识别 47 个到期项、保护 251 个；删除和失败均为 0，COS 136 个对象仍无孤儿，本地 227 个孤儿候选仅盘点。紧接着第二次 dry-run 的 `classified=0`、`planned_deletions=47`，证明分类已稳定持久化且不会每周期反复全量写库。正式记忆和当前卡片继续保护，25 个未绑定记忆上传已按临时资产进入计划。
**回收限流**：正式资产和 COS/本地孤儿均按最老时间和稳定键排序；每周期分别受数量和总字节双重限制。默认正式资产最多 10 个/64 MiB，孤儿最多 25 个/64 MiB；结果公开计划数量、计划字节、当期选择数量、选择字节、限流剩余和超大对象数量。限流回归确认 3 个到期资产只删除最老 2 个，3 个孤儿只删除最老 1 个，其余留待后续周期。
**孤儿持久审计**：新增 `media_orphan_cleanup`，仅保存 `cos/local`、相对对象键、大小、源修改时间、首次/最近发现、状态、尝试次数、脱敏错误、下次重试、删除和解决时间。真实删除前先批量提交 `deleting + attempts`，进程中断后按失败任务恢复；失败使用 1 分钟起、最长 6 小时的确定性指数退避。对象被业务重新引用、被外部删除或不再满足宽限条件时记为 `resolved`。任务摘要复用 `scheduler_runs(job_type=media_lifecycle)`，服务重启后可恢复最近结果，不建立重复调度表。dry-run 和 `classification_only` 只盘点、不改写孤儿状态、不删除对象；运行摘要仍持久保存。错误记录会移除 URL、凭据样式和绝对路径。
**孤儿审计本地验证**：专项覆盖 COS 与本地删除失败、60 秒首次退避、未到期不重试、到期后成功、对象提前消失转 `resolved`、本地绝对路径脱敏、dry-run 无孤儿状态写入、运行摘要重启恢复、PostgreSQL 批量 upsert 和迁移字段。完整云端 Node 回归 `107` 项中 `106` 通过、`1` 项仅因本机未配置 PostgreSQL 集成地址跳过；前端缓存、App Server 和 iOS 发布元数据验证全部通过。
**孤儿审计生产验证**：提交 `d2eba48` 已发布到不可变版本 `/opt/gohome/releases/20260804083111-d2eba48d8915`，迁移 `014_media_orphan_cleanup.sql` 成功应用。API、MediaMTX、Coturn 均为 active，API `NRestarts=0` 且发布后 warning 日志为空。生产删除开关保持 `false`，`media_orphan_cleanup` 为 0 行。手动 dry-run 扫描 1036 个资产，计划 47 个/29,334,837 字节，当期选择 10 个/13,770,195 字节；COS 扫描 136、孤儿 0；本地扫描 1102、孤儿 227 个/21,530,829 字节，当期选择 25 个/5,403,814 字节；删除和失败均为 0。该结果已持久写入 `scheduler_runs(job_type=media_lifecycle)`，状态接口同步返回相同 `last_run` 和 `delete_enabled=false`。
**历史对象身份审计**：227 个本地候选不能直接删除。226 个文件名携带的旧资产 ID 已被当前另一条资产记录复用，其中 207 个可通过原始快照文件名在事件 JSON 中唯一匹配到具体事件且无歧义；其余 19 个是旧关怀图和记忆网格衍生图。典型样本的物理文件写入时间早于当前同 ID 资产创建时间数小时，证明旧自增媒体 ID 在数据库重置、恢复导入或并行实例边界发生过复用，后写资产覆盖数据库行而旧物理对象失去身份。当前设备上传改用 `设备 ID + 上传幂等键` 的确定性 SHA-256 资产 ID，迁移 `015_media_asset_identity.sql` 同时约束物理对象键和设备幂等键唯一；删除状态的上传不会被历史重试重新创建。历史 207 个事件文件将在独立迁移中恢复为一物一资产，并只把每个事件、角色、内容的规范副本写回事件证据引用；重复物理副本保留明确的规范资产关系。无法证明业务归属的对象只进入受保护审计状态，不按文件名前缀进入产品逻辑。
**删除候选实物审计**：GH-056/GH-057 收口后，47 个到期资产由 22 张失去当前卡片引用的旧关怀图和 25 个未发布或已移除的家庭记忆上传组成；全部业务引用为 0。逐物核验发现 22 张关怀图实体存在且大小一致，但 18 个 COS 与 7 个本地记忆对象已经不存在，数据库资产仍为 active。根因是记忆编辑/删除入口先同步删除物理对象，再硬删数据库资产，两套存储无法原子提交，中断时会留下活动元数据。处理改为先持久化 `deleting + deletion_requested_at`，立即停止播放，再由唯一生命周期状态机执行物理删除、成功墓碑和失败退避；入口先读最新 PostgreSQL 记忆关系，仍被其它记忆引用的共享资产不入队；执行时再统一检查记忆、事件、卡片和头像引用。专项覆盖请求持久化、立即停止播放、生命周期成功墓碑及共享资产持续可用；全部 Node 测试 `118` 通过、`1` 项因本机未配置 PostgreSQL 集成地址跳过、`0` 失败，完整 `npm test` 与差异检查通过。生产部署与历史 25 条状态对账通过前仍不得启用真实删除。
**删除一致性生产验证**：提交 `37c8a2f` 已原子部署为 `/opt/gohome/releases/20260804112852-37c8a2fc88df`。发布后 dry-run 仍为 47 个到期资产、当期最多选择 10 个但实际删除 0；20 个受保护本地对象保持 `protected=20、selected=0`，COS 孤儿为 0，运行时删除能力为关闭。最终只读候选报告保存为 `/var/lib/gohome/reconciliation/media-deletion-candidates-20260804T113101Z.json`，权限 0600，SHA-256 为 `fad938add9ca24fe7a05840af6f995d5a1a8df8035ba95613ff3d0588e3c7112`：22 条 `physical_delete_candidate` 共 15,060,775 字节，25 条 `metadata_tombstone_required` 共 14,274,062 字节；业务引用和实体大小失配均为 0。报告人工确认前继续禁止真实删除。
**批准清单执行**：用户确认后按固定报告与哈希执行。先在可串行化事务中为 25 条实体已缺失资产补 `deleted` 墓碑，物理删除为 0；dry-run 从 47 精确降为 22。随后严格按默认上限分三批回收实体存在资产：`10 + 10 + 2`，分别删除 `13,770,195 + 1,188,856 + 101,724` 字节，每批执行前重新核对当前引用、路径和大小，执行后逐项核对墓碑与文件消失。最终 dry-run 为到期资产 0、选择 0、失败 0、COS 孤儿 0；本地文件数从 1102 降为 1080。报告内 47 条资产全部为 deleted 墓碑且 `size_bytes=0`，failed 资产为 0；20 个无法确定归属的历史本地对象继续保持 `protected=20、selected=0`。报告标准校验通过，三项服务 active、API `NRestarts=0` 且无 warning，生产删除开关仍保持关闭。
**盒子数据库受控压缩**：30 分钟实时验收结束后，`agent.db` 预检为物理 `693,161,984` 字节，其中 `581,120,000` 字节为 SQLite freelist 可复用页，估算有效页约 `112 MB`，`quick_check=ok`。停止唯一服务写入后执行 WAL checkpoint 和 `VACUUM INTO`，对新库独立执行 `quick_check` 与 `integrity_check`，均为 `ok` 且初始 freelist 为 0；原子切换、服务健康成功后才删除旧库。最终物理大小 `110,723,072` 字节，服务写入后 freelist 为 1。重启后行数据持续可读并新增，双路 streaming、H.264 ready、Pose/Object/读取/重连/发布失败均为 0，`NRestarts=0`、warning 为 0，临时库和备份无残留。本次只回收历史空闲页；持续容量仍由现有高水位环形留存、批量删除和页复用负责，GH-013 的断电与高水位长时验收继续保持未完成。
**待验收**：GH-013 云端 PostgreSQL、本地媒体与 COS 生命周期已完成真实受控回收。整项仍需盒子持续高水位、断电恢复和长时循环删除验收；在该验收完成前保持“处理中”。

## GH-014 至 GH-016 产品闭环复验

- 推送：一个事件只通知一次，记录 APNs 请求、响应、设备回执和端到端延迟。
- 定位：绑定或首次设置时由手机确认家庭固定位置；首页使用手机位置计算回家距离；社区始终使用家庭固定位置。
- 模型质量：人物、猫、狗及困难负样本建立版本化标注集，报告 precision、recall、F1、混淆矩阵和场景分层结果。
- 宠物诊断进展：现场健康状态此前只公开全局宠物轨迹总数，无法判断每路是猫、狗还是未确认，也无法解释“同一只猫在一路被识别成狗”。`PetTemporalStabilizer` 现以锁保护更新、重置和健康读取，并公开不含图像及框坐标的逐路轨迹：确认类别、类别命中数、时序平均置信度和距最后命中的秒数。专项与完整边缘回归 `59/59` 通过。提交 `a109eef` 精确部署后，两路当前 `track_count=0`，证明旧 `presence.pet_types=cat/dog` 是历史观察而非空房缓存泄漏；Pose/Object 均 ready、失败为 0，视频与发布稳定。该项只建立可观测性，不修改阈值、不按当前家庭硬编码类别；必须让真实猫重新经过两路取得当前证据，再用已标注猫/狗样本决定模型或时序分类修复，GH-016 保持待处理。
- 宠物现场反证：当日结构化检测记录显示，摄像头 31 在 `15:19:50-15:19:53Z` 对家庭猫连续输出 4 个原始 `cat` 结果，置信度 `0.4628-0.5827`；摄像头 32 在 `15:59:15-15:59:22Z` 对同一家庭宠物连续输出 8 个原始 `dog` 结果，置信度 `0.5277-0.6564`。错误类别已经存在于 Hailo COCO Object 原始输出，时序层只是稳定了原始类别，不是 App 翻译、跨摄像头缓存或旧轨迹污染。随后连续 `6` 分钟健康采样中 Object 成功推理约增加 `600` 次、失败为 0、温度约 `61-64°C`、两路发布无重连，但没有新的宠物候选。禁止通过提高统一阈值掩盖该问题：错误 `dog` 的置信度与正确 `cat` 重叠，提高阈值会同时制造漏检。下一步先建立经用户授权的版本化宠物裁剪标注集，比较专用轻量猫狗分类器与“只展示宠物、不臆测类别”产品态；在混淆矩阵达标前不得向用户输出未经验证的猫/狗结论，GH-016 保持待处理。
- 宠物产品隔离：CPU YOLO 与 Hailo Object 现在统一输出 `type=pet / label_zh=宠物 / species_status=unverified`；原始 COCO 类别、类别 ID、标签和置信度只保存在 `raw_model_*` 诊断字段。时序稳定器继续按原始类别累计受控诊断证据，但正式家庭首页、活动轨迹和盒子管理端只显示“宠物活动”，历史 `cat/dog` 数据也不再被界面翻译为物种结论。专项覆盖 CPU、Hailo、时序保持和两套界面契约，完整边缘回归 `59/59` 通过。提交 `f965f7f` 已推送并精确部署 Pi；边缘检测与管理端文件哈希和主线一致，双路 streaming、H.264 ready，重连、读取、Pose/Object 和发布失败均为 0。生产云不可变版本 `/opt/gohome/releases/20260805165225-f965f7f66a18` 已健康切流，公网家庭首页脚本哈希与主线一致，云端和 Pi 均 `NRestarts=0`、无新增 warning。该隔离消除了错误产品表述，但没有提高物种准确率；标注授权、数据集、专用分类器和独立混淆矩阵仍未完成，GH-016 保持待处理。

## GH-017 至 GH-019 工程质量

- 管理端只展示真实测量指标，不展示公式生成的“准确率”。
- 拆分 iOS 网络协议、仓储和兼容上传装配，降低 `AppEnvironment` 的职责。
- 清理前先生成本地、生产云端和树莓派文件清单及哈希；确认没有独有代码或数据后再删除备份、旧入口、旧权重和构建垃圾。

### GH-017 当前处理记录（2026-08-07）

**根因确认**：旧管理端曾把规则分数、窗口比例或演示数据换算成百分比，容易被误认为模型实测准确率。该运行时已在提交 `1440828` 中删除；当前正式 `algorithms.html + console.js` 只展示后端实际返回的目标计数、模型置信度、规则原值、状态机阶段、帧龄、分析延迟、输出 FPS、Hailo Hz 和设备资源指标。`confidence` 仅标记当前检测结果的模型置信度，不等同于准确率；没有标注集和独立测试集时不显示准确率。

**本次处理**：在 `verify-admin-static-contract.py` 增加指标契约门禁，正式管理端源码一旦重新出现“准确率”、`accuracy`、`balanced_accuracy`、旧伪指标函数、旧算法选择器或百分比格式化器，静态检查立即失败；算法演示素材也继续由正式静态资源清单拒绝。

**自动验证**：静态入口、脚本依赖、实时流代次契约和伪指标禁用契约通过；当前管理端源码未命中禁用项。该项已完成本地根因修复，仍需部署后的管理端页面验收，确认页面只显示真实运行指标，因此状态保持“待实机验收”。

### GH-018 当前处理记录（2026-08-07）

**根因确认**：正式 App 的 `AppEnvironment` 同时承担启动依赖创建、全部网络 endpoint 装配、媒体上传协议选择和旧接口兼容回退，导致启动入口难以审查，协议演进时容易把废弃路径重新带回产品。

**本次处理**：新增 `AppRepositoryLiveFactory`，将生产 endpoint 装配从启动环境移入仓储装配层；`AppEnvironment` 只负责认证存储、会话存储、缓存、API 客户端、仓储和推送协调器的生命周期。删除旧 `/api/v2/memory-media-batch` 兼容上传和 404/503 自动回退，正式媒体批量上传只走上传意图、直传、完成/中止这一条已定义协议。异常仍会中止未完成上传并向上报告，不静默改走旧接口。

**自动验证**：Xcode 全量测试通过，单元 `130/130`、UI `25/25`，返回 `TEST SUCCEEDED`；`git diff --check` 通过。`AppEnvironment.swift` 从 410 行降为 53 行，新增工厂文件 345 行，职责移动没有复制第二套网络管线。已生成工程并确认新文件只被正式 App target 引用。该项本地根因已消除，需用新的 TestFlight 构建验证登录、首页、社区、设备/摄像头、记忆上传和实时守护，状态为“待实机验收”。

### GH-019 当前处理记录（2026-08-07）

**根因确认**：此前树莓派部署用工作区目录直接 `rsync`，依赖一长串排除项且没有删除式收敛；旧版本遗留的生产代码可能继续运行，测试脚本和当前提交之间也没有一个可追溯的设备输入。首次正式 dry-run 还发现排除规则没有锚定设备根目录，旧快照中的验证脚本未完全删除，且同步后没有重启 systemd，代码和实际运行进程可能不一致。云端虽然使用 `git archive`，但只输出归档 checksum，没有逐文件输入清单。

**本次处理**：`deploy/edge-agent/build-production-payload.sh` 成为唯一设备包生成入口，要求 tracked worktree 干净，从当前 `HEAD` 生成临时设备包，再删除评估、验证（保留唯一运行时预检）、演示、研究和工具目录。唯一允许追加的本机运行时模型是 `yolo11n.pt`、`yolov8n.pt`；`.env`、数据库、日志、`data/`、虚拟环境和媒体数据不进入包。`deploy-to-pi.sh` 只负责把该包以根目录锚定的 `rsync --delete-delay` 收敛到设备，受保护的运行数据和配置目录不被删除，传输后重启唯一 systemd 服务并等待 `/health` 成功；旧生产代码会从设备移除。包内写入提交号与逐文件 SHA-256 清单。`build-release.sh` 同步输出云端归档逐文件 SHA-256 清单。

**自动验证**：独立干净 Git 副本中实际生成云端归档和 Pi 设备包，云端逐文件清单 `120` 项、Pi 逐文件清单 `93` 项；归档内容门禁、模型白名单、开发目录排除均通过。`npm test`、生产边界预检、四个发布脚本 `bash -n` 和 `git diff --check` 均通过。禁止把未跟踪参考资料、模型研究目录或盒子运行数据纳入清理。

**Pi 实机部署证据（2026-08-07）**：提交 `2eec984` 已同步并重启 `gohome-edge-agent.service`，远端 `.release-commit` 与逐文件清单校验通过，旧 `hyperframes`、旧部署快照和接受测试备份已清除；`.env`、`data/`、虚拟环境、两个 YOLO 权重和媒体运行数据保持不变。服务 `active`、`NRestarts=0`，双路源有效帧约 `14.07/14.17 FPS`，H.264 编码输入约 `14.55/14.76 FPS`，Pose、Object、分割失败均为 `0`，两路发布器均 `publish_ready=true`。首次同步发现并修复了嵌套快照排除过宽和同步后未重启服务两个边界问题；后续发现生产 Python 会在源码目录重新生成 `.pyc`，现已在 systemd、手动启动、部署预检和已安装 unit drop-in 入口统一禁止字节码写入。生产云端仍需用同一提交构建清单并记录安装版本，因此本项状态为“待实机验收”。

## GH-020 最终交付验收

必须产出带时间戳和版本号的报告，至少覆盖：

1. 双摄持续运行 30 分钟，原画、人物模糊、纯骨架分别测试。
2. 摄像头断线、盒子重启、App 前后台、网络切换和流自动恢复。
3. 同一风险事件只生成一条事件和一次通知，排除异常只留事件不通知。
4. 本地 SQLite、磁盘和 COS 留存、循环删除、失败重试和孤儿对账。
5. 正式 `ios-shell` 工程生成 TestFlight，确认显示名 `GoHome`、Bundle ID `com.gohome.family`。

Build 8 不满足本台账的骨架产品契约，不作为最终交付版本。

**30 分钟混合负载证据（2026-08-05）**：Pi 本机完成 `0-60` 共 61 次、每 30 秒一次的连续采样。服务全程运行，双路读取失败、重连、Pose/Object/分割失败、正式分析超时和发布失败均为 0；温度约 `55.1-66.1°C`。人物活动期间两路正式模型锚点曾达到约 `15.67 / 16.89 Hz`，发布输入接近 `15 FPS`；一次隐私模式切换使两路发布器各受控重建一次，之后无失败。该窗口实际只覆盖人物模糊与原画，监控中的 `privacy_mode` 在后半程始终为 `original`，没有进入 `skeleton`。因此本证据只通过基础视频、Hailo 调度、受控模式重建和温控稳定性，不能关闭 GH-008/GH-020 的纯骨架、残影、断线、推送和 TestFlight 联合验收。

## GH-021 摄像头连接配置原位更新

**现象**：摄像头地址由 `192.168.1.3` 变化到 `192.168.1.7` 后，App 编辑页只允许修改名称、位置和启用状态，没有 IP、端口、路径和凭证更新入口。
**根因**：正式 iOS 表单在编辑模式隐藏连接区，更新请求不包含连接字段；云端虽接受字段，但连接变化后没有统一重置为待盒子同步状态。
**处理**：保留云端与盒子摄像头 ID，云端返回不含密码的结构化连接摘要；App 使用 `URLComponents` 校验并生成 RTSP 地址，用户名和新密码留空时保留原值；连接字段变化后云端统一进入 `pending_edge_sync`，盒子配置同步原位更新。
**实机结果**：生产云摄像头 `29` 与盒子本地摄像头 `32` 保持不变，地址已原位更新为 `192.168.1.7`，盒子与云端均恢复 `online / synced`；云端实时接收约 `12.5 FPS`、stale reject 为 `0`。
**剩余验收**：下一 TestFlight 在正式 App 编辑页修改一次地址，确认密码留空、等待状态、盒子同步和守护页自动恢复；路由器为摄像头 MAC 设置 DHCP 地址保留，减少再次漂移。

## GH-022 摄像头源生命周期分裂

**现象**：云端和盒子数据库已将摄像头从 `192.168.1.3` 更新为 `192.168.1.7`，状态显示同步或在线，但长时进程仍持续连接旧地址；必须重启服务后新地址才真正生效。
**根因**：配置同步只修改 SQLite；托管读取器按启动时的旧配置长期存活。直播中继虽能按新签名重建消费线程，却不会销毁旧托管读取器，也不会原子清除姿态、分割、背景和渲染状态。
**处理**：`CameraAgent` 成为唯一采集生命周期所有者；连接 URL、凭据或启用状态变化时原子停止旧读取器、清除该摄像头旧缓存、提升独立流代次、通知所有视觉状态重置，再启动新读取器。配置同步写完所有摄像头后立即调用一次统一 reconcile。停止现在有明确成功/失败结果；旧 reader 未在关停期限内退出时，保留所有权、阻断替代 reader 和旁路抓帧，配置同步失败并等待下一次对账。云端摄像头进入 `setup_required` 时，旧本地源先禁用并从托管集合移除，完整新连接配置到达后使用原摄像头 ID 恢复。
**自动验证**：共享流回归覆盖旧 reader 停止失败、禁止替代 reader、禁止新配置旁路抓帧、旧缓存隔离和正常 `.3 -> .7` 切源；配置同步回归覆盖 `setup_required -> 完整连接配置` 的禁用、统一 reconcile 和原位恢复。关联断流、H.264 发布、编译和差异检查通过。
**待实机验收**：运行中执行 `.3 -> .7` 后无需重启服务；旧地址不再出现连接尝试；新源首个真实解码帧之前不得显示在线；旧缓存、Pose、分割、背景和隐私成品帧均不可返回。另需现场验证旧源拔出/插回、切换 DHCP 地址、App/管理端状态同步；自动回归通过不等同于实机关闭。

## GH-023 重复帧伪造实时状态

**现象**：录屏中摄像头时间戳一直停在 `2026/08/02 20:01:17`，管理端仍显示约 `12-15 FPS`，并在冻结空画面上保留或生成“人物”状态。
**根因**：读取器把每次 `cap.read()` 成功都视为新帧；没有区分解码调用、像素真正推进和成品输出，重复像素会刷新缓存时间、帧号和姿态跟踪。
**处理**：唯一共享读取器对尺寸和每 8 像素覆盖样本计算 BLAKE2s 内容指纹，分别统计 decoded FPS 与 unique/effective FPS；相同内容只增加 decoded/repeated 计数，不创建新 `frame_id`、不刷新有效帧时间、不进入 Tracker。连续 3 秒无唯一像素后释放捕获、清空缓存、提升流代并通过统一 source transition 清除 Worker Pose、分割、背景运行态和隐私缓存，再按有界退避重连。
**补充根因**：读取线程先提交中央最新帧缓存，再提升独立通知序号。消费者可能在通知序号仍对应上一帧时读到已经提前更新的缓存；下一次通知到达后又读取同一缓存，造成相同 `frame_id` 被发布两次。该竞态会虚增客户端解码次数，并使回归偶发失败。
**本地验证**：共享采集回归确认冻结源多次成功解码但 unique frame 恒为 1、effective FPS 为 0、缓存被清除、流代提升且旧 Pose listener 被调用；新增局部 5x5 变化位于旧 16 像素采样空隙时仍能产生不同指纹。消费者现在以中央缓存的正式 `frame_id` 去重，通知序号只用于唤醒；确定性 cache-ahead 回归强制连续返回 `9-1 / 9-1 / 9-2`，正式输出必须为 `9-1 / 9-2`。完整边缘回归通过后进入实机验收。
**验收**：重复帧输入时有效 FPS 为 0、`frame_id` 不增长、姿态失效且画面状态为 stale；新像素恢复后自动恢复，所有页面仅展示有效输出 FPS。

## GH-024 算法诊断流与隐私模式耦合

**现象**：管理端算法页在全局模糊模式不画骨架，在骨架模式又切到隐私成品流；Tracker 停滞时诊断视频本身会冻结，无法判断摄像头是否仍在推进。
**根因**：诊断路由根据用户隐私模式选择三套不同来源，并把 Tracker 保留的旧像素当作视频源。
**处理**：算法诊断页始终以 `CameraAgent` 当前唯一帧为底图，只读取与该 `camera_id + source_key + frame_id` 完全一致的 Pose 元数据叠加一套诊断骨架；全局隐私模式不改变算法诊断语义。用户直播仍严格遵循三种隐私模式。
**验收**：原画、模糊和骨架任一全局模式下，算法页都能显示当前视频和同帧诊断骨架；源冻结时视频和检测同时明确失效，不得显示旧人物。

## GH-025 追踪补偿状态进入正式显示

**现象**：`20260802213732.mp4` 的管理端视觉算法页中，人物约在 32.6 秒向右移动后，旧骨架仍在沙发附近保留约 0.9 秒；页面同时显示 `短暂补偿`，最大帧龄约 `906 ms`。
**根因**：连续追踪器的 `coasting` 是供调度器快速请求新 Hailo 锚点的内部恢复状态，不是当前帧观测结果；诊断流、纯骨架合成和管理元数据却仍把该状态当成可绘制人物，导致旧几何覆盖在更新的视频帧上。
**处理**：显示契约只接受当前帧有效的 `observed / tracked`。`coasting` 与 `display_only_stale` 不得绘制骨架、不得辅助人物掩码、不得向管理端声明人物可用；Worker 发现这些状态时通过统一推理调度器请求当前摄像头立即刷新，不另建推理线程或第二套状态。当前分割仍负责移除当前人物像素，Pose 暂时无效时宁可短暂不画骨架，也不能展示错误旧骨架。
**本地验证**：连续追踪、同步诊断流、纯骨架隐私流、调度器和管理元数据专项回归通过；边缘完整回归 `48/48`、Python 编译和差异检查通过。
**验收**：部署后两路分别重复进入、横向快速移动、遮挡、离开和再次进入；视觉算法页与纯骨架成品流均不得出现滞留骨架。服务运行指标、校准状态和用户三模式不能回退；通过前保持“待实机验收”。

## GH-026 至 GH-029 校准、模式和实时帧契约

### GH-026 持久校准被短暂场景变化销毁

**根因**：`PrivacyBackgroundReconstructor` 将运行时 `scene_changed` 与永久校准失效视为同一状态，清空内存并删除持久化 `.npz`。电视内容、灯光或局部物品变化因此会破坏重启恢复能力。
**处理**：拆分持久基线、运行时复核和明确重校准三类状态；短暂变化只暂停纯骨架输出并持续复核，绝不删除基线；按逐路摄像头独立管理。
**实机续查与本地修复**：2026-08-04 重启后两份 `.npz` 基线及 SHA-256 均完整保留，但摄像头 31/32 分别因 `person_present` 与 `stream_revalidation_failed` 未就绪。逐帧抓取基线和当前画面确认两路镜头视角没有改变，实际变化是靠垫、茶几、晾衣架、宠物位置和整体照明；旧复核仅用全画面像素残差，因此把正常家庭布局变化误判为镜头移动。现已提取独立 `SceneGeometryVerifier`：快速像素检测失败时，按每路有界频率使用 ORB 静态特征、描述子比率筛选、RANSAC 单应性、内点比例和四角归一化位移验证视角。真实两路分别得到 `15/21` 与 `77/87` 个内点/匹配，最大角点位移约 `0.94% / 0.66%`，均判定 `same_view`；合成回归确认大面积家具变化和重启复验可通过，52 像素镜头位移仍进入 `scene_review_required`。完整边缘回归 `58/58` 通过，待部署 Pi 验证两路自动恢复且基线文件哈希不变。
**验收**：电视、开关灯、局部遮挡后可自动恢复；服务重启仍加载同一校准；只有明确换源、分辨率变化、摄像头移动确认或用户操作才要求重校准。

### GH-027 隐私模式不是事务

**根因**：云端和盒子只持有一个全局模式字符串，模式期望值、逐路有效模式和逐路校准状态没有共同提交。任一路未就绪时可能出现云端仍为 `skeleton`、盒子无成品帧。
**处理**：保存 `desired_mode`，逐路计算 `effective_mode/status/reason`；切换前做逐路预检，未就绪摄像头保持原有效模式并明确返回失败，不得拖黑其他摄像头。
**验收**：两路不同校准状态下分别切换，管理端、云端和 App 显示同一逐路状态；失败切换不改变原有效画面。

### GH-028 空 MJPEG 和静默异常

**根因**：`PrivacyMjpegStream.mjpeg_frames()` 捕获所有异常后继续循环，形成状态码为 200 但永远无帧的响应，客户端只能报“无法识别的响应”或持续连接。
**处理**：打开流前执行就绪预检；渲染错误进入逐路结构化状态和指标；流建立后发生不可恢复错误必须结束连接并允许客户端按明确原因重试。
**验收**：未校准、源过期、分割失败和身份不一致均有确定的 API 状态与错误码；不存在 200 空流和无限静默循环。

### GH-029 旧像素使用新帧身份

**根因**：近黑帧出现时 `CameraAgent.raw_frames()` 使用 `last_good_frame` 替换像素，但保留新采集的帧号和时间信息，造成旧画面、Pose 和 FPS 被误认为当前结果。
**处理**：旧像素不得进入实时帧管线；源进入 `stale/reconnecting` 并停止推进有效帧号。若产品需要最后画面，只能作为标明时间的静态占位，不参与算法和实时 FPS。
**本地验证**：近黑判定已移入每路唯一共享读取器；近黑帧只增加 decoded/near-black 诊断计数，首帧即清除中央实时缓存、有效 FPS 窗口和内容连续性并进入 stale，持续 5 帧且至少 0.75 秒后提升流代并统一重连。预览层的 `last_good_frame`、黑帧计数和旧像素替换分支已删除。专项回归确认连续 5 张近黑帧没有写入中央缓存、没有推进有效帧身份、没有作为预览输出，恢复帧使用新流代；共享采集、冻结失效、实时分析同帧和同步骨架回归通过。
**验收**：近黑、冻结和断流时有效 FPS 归零、Pose 失效、帧号不推进；恢复后从新真实像素继续。

## GH-030 至 GH-033 算力、指标、视觉和传输

### GH-030 Hailo 调度未充分利用

**历史根因**：整套 `VisionPipeline` 曾被一个全局锁串行执行，双路摄像头共用单个低频 Worker 调度；Pose、Object、CPU 后处理和事件逻辑绑在同一周期，KLT 曾长期代替新 Pose。当前实现已不再以该历史结构作为运行方案。
**处理**：按模型职责建立有界最新帧调度器：活跃人物 Pose 目标每路 `10-15 Hz`，隐私需要时分割目标每路 `5-10 Hz`，物体/场景 `0.5-1 Hz`；每种 HEF 保持单 runtime，并强制摄像头、源代次和帧身份。最终频率以 Pi 5 + Hailo 分阶段实测为准。
**阶段进展**：Pi 旧版现场实测 Pose 中位 `22.86 ms`、P95 `35.24 ms`、整管线中位 `28.76 ms`、P95 `71.62 ms`，Hailo 失败为 0，CPU 约 106%、温度 65°C；空房调度为两路 Pose 各 2 Hz、Object 各约 0.8 Hz、分割各约 4.67 Hz，KLT 展示约 11-13 FPS。确认 idle 周期已经执行 Pose HEF，却因 `pose_detection_enabled=false` 丢弃结果，遮挡或 Object 漏检时可能无法进入 active。现已复用已执行的 Hailo idle Pose 结果作为人物/风险首帧，不增加设备推理；CPU/未知后端仍使用轻量人物探测。该历史问题已由共享 Pose owner、最新帧协调器和自适应调度实现替代，后续不再通过拆第二套 runtime 解决。
**2026-08-04 性能续查**：部署当前 H.264 正式链路后，两路源采集稳定在约 `15 FPS`，空画面隐私成品约 `14.4 FPS`，但有人路仅约 `10.8 FPS`。分阶段指标确认有人路每帧对整张 `640x360` 图像执行约 `73x73` 高斯模糊并进行三份全帧浮点 alpha 合成，`person_blur_composition` 中位约 `50.7 ms`；分割/光流另占中位约 `23.7 ms`。现已将人物模糊改为只处理掩码包围区域，使用低分辨率不可逆强模糊和平滑放大，再以 `uint16` 羽化合成，掩码缺失时仍保持整帧保护。相同 `640x360` 合成基准由约 `5.12 ms` 降至 `0.84 ms`，约快 `6.1` 倍；无 JPEG 回归确认远离人体区域逐像素不变，人体区域隐私变化达标。待 Pi 部署确认有人路成品 FPS 和阶段 P50/P95 后，再处理当前约 `19 ms` 的分割光流与仅约 `3 Hz` 的真实 Pose 锚点。
**2026-08-05 分割传播修复**：完整审阅确认非锚点分割帧仍在 Pi CPU 上运行 `320x180` Farneback 稠密光流，并逐帧重新分配坐标网格、remap 坐标和形态学核；现场长期指标中位约 `20.3 ms`。使用同一 Pi、真实家庭截图、真实录屏相邻帧和受控 `8x3` 像素人物掩码平移比较：Farneback 在 320 宽中位约 `14.6-19.9 ms`，DIS UltraFast 约 `0.42-1.24 ms`；真实相邻帧重投影平均误差仅增加约 `0.03-0.08` 灰度级，受控掩码平移 IoU 为 `0.9992`。实现改为每路复用一个 DIS UltraFast 实例，按尺寸缓存只读坐标网格，复用形态学核，并直接在 flow 两通道上原位转换为 remap 坐标；保持 `flow_width=320`、锚点频率、传播时限、场景变化拒绝和人物证据门槛不变，不以降低隐私分辨率换性能。专项增加真实位移方向、掩码 IoU、每路 estimator 生命周期和缓存复用契约，完整边缘回归 `58/58` 通过。待部署 Pi 核对传播 P50/P95、成品 FPS、CPU、Hailo 锚点频率和长时隐私效果后，再决定是否进入 Pose 调度拆分。
**2026-08-05 实机收口与下一结构边界**：DIS 版本已部署，Pi 长时运行的传播中位约 `2.26 ms`、P95 `4.63 ms`，此前 Farneback 中位约 `15.78 ms`、P95 `20.96 ms`，隐私分辨率仍为 320，没有以降画质换性能。完整读取 `EdgeWorker`、`DetectAgent`、`VisionPipeline`、Hailo Pose/Object 和持续 Tracker 后确认，下一瓶颈不是 Hailo 单次推理：现场 Pose 中位约 `22.9 ms`、P95 `37.46 ms`、失败为 0；真正限制来自单个 Worker 串行选择摄像头、`DetectAgent` 全局锁包住整条 Pipeline，以及 Pose、Object、场景、活动、规则、持久化共同占用一次全分析周期。下一阶段只允许建立一个共享有界 Pose 调度所有者：高频当前帧 Pose 锚点与低频完整产品分析分离，模型结果仍携带完整帧身份，KLT 只做短时显示插值；不得简单删除全局锁、复制第二套 Hailo runtime 或让显示 Pose 直接成为正式风险证据。
**2026-08-05 共享所有权第一步**：新增 `PoseInferenceService`，由 `DetectAgent` 创建并注入 `VisionPipeline`，统一拥有唯一 `HailoPoseBackend` 和匹配的 `RtmposeAnalyzer`；直接构造 `VisionPipeline` 的测试与独立调用仍只创建一项自有服务。运行状态明确报告 `shared-pose-inference-v1 / single_shared_service / runtime_count=1`，关闭职责按所有权执行，不复制 runtime。现阶段故意保留原推理顺序、阈值、输出字段和 `DetectAgent` 全局串行锁，只建立后续有界调度所需的唯一所有者边界；它本身不宣称提高 Pose Hz。专项、Python 编译和完整边缘回归 `58/58` 通过。首次 Pi 部署确认服务、双路源流、Hailo 与 H.264 发布均正常，但 `/health` 的白名单投影遗漏所有权摘要；修复后运行中接口已确认 `shared-pose-inference-v1 / single_shared_service / runtime_count=1`。服务 `active`、`NRestarts=0`，Pose/Object ready 且失败为 0，两路流 streaming、重连为 0，两个发布器均单次启动、publish ready、无 stderr；四个部署源码 SHA-256 与主线一致。第一结构步骤完成，下一步进入最新帧 Pose 协调器。
**2026-08-05 最新帧协调器实现与首轮反证**：新增单线程 `PoseInferenceCoordinator`，每路最多保留一个最新显示请求，正式分析请求可与同一帧显示请求合并并复用原始 Hailo 结果；全局只允许一个 Pose 推理在途，双路按提交时序持续前进，源切换通过摄像头 revision 同时废止待处理、缓存和晚到结果。完整管线新增显式同帧预计算入口，协调器存在时禁止 Pipeline 再发起第二次 Pose 推理；Hailo 设备锁与 CPU 解释器锁分离，允许下一帧设备推理和上一帧 CPU 后处理安全重叠。显示边界不信任原始新目标：协调器结果只可刷新同源、空间连续且已经过完整管线验证的现有骨架；新人物、人物消失、远距离跳变和 formal 合并帧只唤醒完整分析，不能绕过人物一致性、电视画面、场景和风险证据门槛。首轮 Pi 部署中服务、Hailo、双路源流和 H.264 均正常，协调器队列最大深度 1、失败/超时为 0，但活跃路只有约 `8.62 Hz`。根因是源帧成对到达时，早于 `67 ms` 截止的帧被直接丢弃，下一帧又等待完整周期；现改为首帧立即执行，后续早到帧保留到截止时间，并且连续早到帧只替换待处理内容，不丢失整个 cadence。专项新增“首帧不受 cadence 延迟、截止前不执行、到期只运行最新帧”，完整回归再次 `59/59`。待重新部署确认活跃路接近源帧上限后继续。
**2026-08-05 cadence 二次实机结果**：提交 `7f29827` 已按单文件 SHA-256 `8bce3269...89a343c` 精确部署。稳定运行后共享 Pose owner 仍为 1；活跃摄像头 31 协调器完成频率 `10.04 Hz`、Tracker 真实模型锚点 `9.85 Hz`，达到本阶段 `10 Hz` 下限，相比首轮约 `8.62 Hz` 不再因早到帧丢失整个周期；空闲摄像头 32 保持 `2.26 Hz`，没有以无人物空转抢占算力。两路每路最多一个待处理槽，全局队列峰值 2，正式请求超时 0、推理失败 0、Hailo Pose 中位 `22.82 ms`、P95 `43.65 ms`。源流分别约 `13.24 / 10.40 FPS`，隐私成品和 H.264 编码输入与各自唯一源帧一致；两个发布器均 `process_starts=1`、publish ready、失败为 0。服务 `NRestarts=0`、journal warning 为 0，短时主进程约 `82.9% CPU / 337344 KiB RSS`、温度 `64.5°C`。协调器部署项完成，GH-030 继续等待双摄真人、遮挡、快速移动、离开、宠物、电视内容和长时温控验收。
**2026-08-05 调度可观测性缺口**：两分钟空场结构化采样确认双路无超时、无推理失败、无跨路状态，但摄像头 31 在 `2 Hz` 与 `10-13 Hz` 间周期性升频且没有生成 Pose。Worker 已维护每路调度模式、刷新请求、最近刷新原因、active/risk 保持时间和资源状态，公开 `/health` 却遗漏该唯一状态源，无法区分人物、运动门、Tracker 失锚或温控导致的频率变化。现将已有 `inference_scheduler` 原样加入健康白名单并增加生产边界契约；不复制状态、不改变算法行为。部署后以明确原因判断空场升频是否来自电视内容或原始 Pose 候选，再决定状态机调整。
**2026-08-05 空场升频根因与结构修复**：调度健康投影部署后直接确认摄像头 31 空场累计 9 次 `coordinated_pose_person_entered`，摄像头 32 的首个空结果也被错误记录为 `coordinated_pose_person_cleared`。根因是 Worker 用单帧原始 Hailo Pose 的布尔变化直接调用 `request_refresh()`；该 API 同时代表 Tracker 失锚恢复和人物候选验证，会把尚未正式确认的候选提前延长 active 8 秒。现拆分职责：新增 `PoseCandidateValidationGate`，同源候选至少连续两帧且空间连续才申请正式验证；初始空帧和候选清空不触发；等待正式结果期间去重，正式拒绝按同源连续拒绝次数执行 `10/20/40/60 秒` 上限退避，正式分析错误则释放等待但不伪判无人。Scheduler 新增 `request_validation()`，只安排一次立即正式分析，不进入 active；只有正式分析确认人物才按原逻辑进入 active，风险规则仍只读取正式证据。健康接口公开候选采样、验证、确认、拒绝、错误、拒绝连击和冷却指标。专项覆盖初始空帧、单帧候选、连续候选、验证期间空帧、重复请求、递增退避、正式错误和 idle 模式保持；待完整回归与 Pi 空场/真人验证。
**2026-08-05 空场 Pi 反证通过**：提交 `5d294d3` 和退避提交 `0228751` 已精确部署。电视柜路一分钟内持续产生 131 个可显示质量的原始 Pose 样本，但正式管线始终判定空场；Scheduler 全程 `idle`，旧 `refresh_request_count=0`、`last_refresh_reason` 为空，协调器保持约 `2.0-2.3 Hz`，不再进入 10-13 Hz active。候选正式验证由修复前约 12 次/分钟降至 3 次，并依次进入约 `10 / 20 / 40 秒` 冷却；冰箱路保持 idle 且没有候选验证。两路正式超时 0、推理失败 0、服务自动重启 0。空场电视假候选的升频和验证风暴根因关闭，GH-030 继续等待真人进入延迟、遮挡、快速移动、离开、宠物和长时温控验收。
**2026-08-05 真人动作首轮结构化证据**：用户完成进入、停留、遮挡、快速移动和离开后，SQLite 正式检测记录显示摄像头 32 于 `23:18:58`、摄像头 31 于 `23:19:03` 首次确认真人；中途短暂离开或遮挡后可重新确认，最终分别在 `23:19:54` 和 `23:19:53` 后清空。动作期间单帧姿态风险分最高为 `0.90`，但没有形成满足时序门槛的跌倒候选、正式事件或通知，符合“指标异常但排除风险不通知”的产品边界。测试后两路 Tracker 均为 `empty / pose_count=0`，源有效帧约 `14.95 / 17.47 FPS`，H.264 发布约 `14.53 / 14.27 FPS`，超时、推理失败、发布失败、服务重启和 journal warning 均为 0。本轮采样发生在用户离开后，无法反推活跃阶段实际 Pose Hz 和进入延迟，因此只确认真人识别、离开清空和无误报，不关闭 GH-030。候选健康指标此前只有累计计数，无法区分当前真人确认与历史电视假候选；现补充最近一次验证请求、正式确认、正式拒绝和正式错误的单调时间及距今秒数，完整边缘回归 `59/59` 通过，待部署后执行同步真人采样。
**2026-08-05 空房五分钟稳定性**：候选时间指标与宠物逐路状态部署后连续采样 `300.8` 秒、30 次，健康读取错误为 0，温度 `59.5-62.8°C`。摄像头 31 源有效帧 `13.56-14.86 FPS`、H.264 输入 `11.91-14.75 FPS`；摄像头 32 分别为 `11.51-14.48 / 11.23-14.41 FPS`。两路 Scheduler 全程 idle，Pose/Object 失败增量均为 0，宠物轨迹最终均为 0。电视柜路原始假候选新增 5 次正式拒绝、正式确认 0、错误 0；冰箱路三项均为 0。空场隔离、60 秒上限退避和温控短时通过，但这不是有人负载或 30 分钟交付验收。
**2026-08-05 同步真人 active 实测**：在健康采样运行期间完成进入、停留、遮挡、快速横移和离开，持续 `90.3` 秒取得 173 个样本、读取错误 0。摄像头 31 新增 1 次正式真人确认，活跃模型锚点峰值 `11.11 Hz`、协调器 Pose `14.43 Hz`、显示 `14.99 FPS`，源有效帧 `13.47-15.07 FPS`、H.264 输入 `13.23-15.14 FPS`；最后一次非空骨架后 `0.54` 秒清空。摄像头 32 在采样开始前约 `3.4` 秒已完成真人确认，active 期间模型锚点峰值 `10.46 Hz`、协调器 Pose `12.70 Hz`、显示 `14.94 FPS`，源有效帧 `12.63-15.06 FPS`、H.264 输入 `12.51-15.01 FPS`。两路最终均为 `empty / pose_count=0 / 约 2 Hz idle`；正式超时、推理失败、Object 失败和事件/通知均为 0。温度峰值 `72.15°C` 短暂进入 warm，随后回落到 `62.25°C / normal`；Pose 中位 `22.77 ms`、P95 `44.75 ms`。真人 cadence、遮挡恢复和离开清空达到本阶段目标；进入延迟因用户在采样进程建立前已经入画不能精确计算，GH-030 继续等待真实宠物分类与 30 分钟混合负载。
**2026-08-07 实现收口复核**：当前主线代码、盒子部署版本和昨天现场验收结果均与上述架构一致。盒子当前两路 `skeleton` 成品均 `ready`，隐私输出约 `13.5/13.6 FPS`，H.264 输入约 `14.5/14.7 FPS`，Pose/Object/分割失败为 0，协调器超时为 0，服务无重启。空房时调度约 `2 Hz` 是明确的 idle 策略；当前 Tracker 两路均为 `empty`，不能把空房锚点频率误当作有人时的 Pose 频率。GH-030 的代码实施项已收口，状态调整为“待实机验收”；未完成的只是最终交付报告所需的双摄长时混合负载、温控和异常恢复证据，不再安排新的算法重构。
**验收**：报告解码、预处理、Hailo 推理、后处理、合成和编码各阶段 P50/P95；双路无饿死、无跨路状态，当前帧 Pose 锚点达到实测目标，KLT 仅用于短时插值。

### GH-031 FPS 指标语义错误

**根因**：部分指标用固定 10 秒除法或源读取速率代替真实样本跨度和用户收到的完成帧速率。
**处理**：独立报告 `source_decoded_fps`、`source_unique_fps`、`edge_composed_fps`、`cloud_accepted_fps`、`client_displayed_fps` 与帧龄；全部按实际时间跨度计算。
**验收**：无新成品帧时输出 FPS 必须为 0；管理端和 App 标签明确且不能以重复帧或 UI 刷新伪造实时性。

### GH-032 骨架视觉不统一

**根因**：隐私成品流和算法诊断流使用不同 BGR 色值、线宽和绘制实现。
**处理**：共享 `app/vision/skeleton_style.py` 中的一套蓝色骨架样式常量、关键点边定义、描边和线宽；用户成品流只在 `skeleton` 模式显示，算法诊断页始终显示同帧诊断骨架，不再按风险状态切换为黄色或红色。
**验收**：管理端算法页、管理首页和 App 对相同成品帧不重复绘制；颜色、线宽、关键点和骨架数量符合各页面契约。

**部署证据（2026-08-08）**：提交 `a2aabe9` 已通过共享视觉契约专项、隐私渲染专项、连续姿态流专项、管理端静态契约、完整盒子 `62/62` 回归、Python 编译和 `git diff --check`，并由唯一部署入口安装到 `192.168.1.12`。远端 `.release-commit` 为 `a2aabe943dde9c14514d478d73fe3bcbb4f7d717`；服务 `active`、`SubState=running`、`NRestarts=0`、主进程退出码 `0`，共享样式和几何绘制文件已存在。部署后 Pose/Object 均 `ready` 且失败 `0`；探测时没有管理端或 App 订阅者，流管理器保持 `0` 个活动流，未伪造视频 FPS；温度和日志无异常，最近十分钟 warning 为空。两路摄像头配置、历史表、校准基线、COS 资产和用户资料均未修改。仍需管理端和正式 App 实机确认同一骨架颜色、线宽和无二次绘制后再关闭 GH-032。GitHub 已推送至 `origin/main`。

### GH-033 逐帧 JPEG 正式链路

**根因**：当前正式路径仍经过 OpenCV BGR、CPU 合成、JPEG 编码、逐帧 HTTP 上传和 MJPEG 播放，带宽、延迟、背压和设备能耗无法达到最终产品标准。
**实测证据**：Build 9 验收时两路源流约 `15.1 / 13.3 FPS`、端侧成品约 `14.8 / 12.7 FPS`，但云端仅约 `9.8 / 8.6 FPS`，逐帧上传覆盖约 `19%-20%` 的待发送新帧，最大上传/接收间隔约 `1.1-1.5s`。没有 RTSP 重连、上传失败或 Hailo 推理失败，瓶颈明确位于 JPEG 编码、逐帧 HTTP 请求/JSON 响应、云端 JPEG 缓存/MJPEG 分发和 iOS `UIImage` 解码链。
**处理**：迁移为单解码所有者、单次隐私合成、常驻 FFmpeg/libx264、RTSP/TCP 上行、MediaMTX 转发和 WHEP/WebRTC 下行。MediaMTX 固定版本并启用外部 HTTP/JWT 鉴权、路径级发布/读取权限、WebRTC 指标和 TURN；Node 只签发短时媒体权限并校验家庭、盒子、摄像头、模式和有效期。迁移完成后删除 `_LatestFrameUploader`、`/api/v1/device/live-frames/upload`、云端 JPEG 帧缓存/MJPEG 产品路由和 iOS `MJPEGStreamClient`；盒子 LAN 管理诊断 MJPEG 独立标识且不被正式 App 使用。
**阶段进展**：隐私渲染器已拆分“像素合成”和“传输编码”职责，新增未经 JPEG 的 BGR 成品帧接口与独立合成耗时指标；三种隐私像素契约保持不变。边缘正式发布器已改为每路唯一常驻 FFmpeg 进程，直接接收 BGR 成品，以 `libx264 / ultrafast / zerolatency / yuv420p / no B-frame / 约 1 秒 GOP` 编码并通过 RTSPS/TCP 发布；待编码帧只保留最新一张，管道写入有界超时，模式或源代次变化会中断旧帧并重启编码器，进程异常退出采用有界退避。状态只报告真实提交和编码器输入 FPS，不把写入管道声称为云端送达；发布 URL、错误和 FFmpeg stderr 均移除设备凭据。旧 `_LatestFrameUploader`、逐帧 JPEG 编码和 HTTP/JSON 上行已从边缘正式中继实现删除，LAN 管理 MJPEG 不变。
**阶段验证**：持久发布专项覆盖最新帧替换、非连续像素规范化、模式重启、异常退出、阻塞管道超时、校准暂停、凭据脱敏和无 JPEG 中继；完整边缘确定性回归 `58/58` 通过。Pi 上 FFmpeg `7.1.5` 确认包含 `libx264`、GnuTLS 和 `fps_mode`，实际 BGR 到 H.264 参数在奇数输入尺寸和 15 FPS 节奏下编码通过。生产媒体面就绪后已按整体迁移顺序部署，后续长时结果见“生产部署进展”；GH-033 只有在完整 TestFlight 网络与三模式矩阵通过后才能关闭。
**云端与 iOS 进展**：生产云 `/api/v1/video/sessions` 已统一通过 `MediaAccessService` 签发短时 WHEP 会话；正式 iOS 已迁移为原生 Unified Plan WebRTC 接收、WHEP `OPTIONS/POST/DELETE`、完整 ICE 收集和 `RTCMTLVideoView` 渲染，显示 FPS 只统计当前渲染器实际收到的新视频帧。摄像头、隐私模式和前后台变化会关闭旧 PeerConnection、终止旧帧事件并释放 WHEP 资源；WHEP 只接受 HTTPS 且资源 `Location` 必须同源。云端逐帧上传 API、JPEG 帧缓存、产品 MJPEG 路由、播放票据和 iOS `MJPEGStreamClient` 已删除，盒子 LAN 管理 MJPEG 保留为独立诊断链路。云端完整回归 `99` 项中 `98` 通过、`1` 项因未配置 PostgreSQL 集成地址跳过；iOS `122/122` 单元测试和 `25/25` UI 测试通过，其中 WHEP 信令 `6/6`、守护页 ViewModel `13/13`。生产 MediaMTX/Coturn/TLS、防火墙和真实双摄发布读取均已完成，剩余边界是 TestFlight 强制 TURN、网络切换和三模式联合长时验收。
**生产部署进展**：固定版本 MediaMTX、Coturn、TLS、nginx、媒体鉴权和腾讯云安全组已投入生产，盒子双路正式发布已切换为持久 RTSPS/H.264，正式 App 使用 WHEP/WebRTC，旧公网 JPEG/MJPEG 产品链路未恢复。2026-08-05 当前精确部署版本连续 30 分钟采样 61 次，服务状态始终 `ok`、`NRestarts=0`；两路摄像头线程与 FFmpeg 进程均只启动 1 次、停止 0 次，`publish_ready=true`，进程失败、半帧中止和 stderr 警告均为 0。摄像头 31 编码输入为 `12.44-14.78 FPS`，摄像头 32 为 `9.75-14.97 FPS`；后者波动与源流唯一帧节拍一致，不能由编码器补造重复帧。GH-033 仍保持“处理中”，剩余门槛是 TestFlight 三模式、前后台、换路、Wi-Fi/蜂窝、直连 ICE、强制 TURN、断网恢复和端到端 30 分钟联合报告。
**验收**：双路 30 分钟端到端帧龄不增长，网络切换自动恢复，客户端解码 FPS 和画质达到验收基线。

## GH-034 至 GH-036 架构与真实产品边界

### GH-034 盒子运行时单体化

**根因**：`main.py` 同时装配配网、身份、家庭、消息、升级、推送、云端事件、视频和算法路由；`worker.py` 同时负责推理、追踪、规则、事件、活动日志和留存；`storage.py` 集中全部表和迁移。
**处理**：先锁定调用消费者，再拆分运行生命周期、采集/合成、推理协调、活动持久化、风险事件和维护任务；移除仅属云端或已无消费者的盒子代码。
**验收**：每个生产职责只有一个所有者；入口只装配模块和路由；删除项有调用清单、迁移策略和回归证明。

### GH-035 真实验收覆盖不足

**根因**：现有通过项主要是合成数据和接口回归，没有覆盖校准后重启、电视变化、两路不同状态、生产云、TestFlight 和长时运行完整路径。
**处理**：建立版本化实机验收矩阵和采样报告，自动回归只能作为进入实机验收的门槛。
**验收**：完成 GH-020 全部场景并保存端侧、云端、App 同时段指标和证据。

### GH-036 云端职责残留在盒子

**根因**：盒子入口仍提供用户注册、家庭、消息、推送和模拟天气等历史原型功能，容易形成双数据源和假产品数据。
**处理**：确认正式 iOS 和生产云调用后，将用户、家庭、消息、天气和 APNs 业务统一归生产云；盒子只保留设备、摄像头、算法、事件上行、配置同步和本地管理职责。
**验收**：正式 App 不直接依赖盒子业务 API；盒子断网时本地安全算法继续运行，联网后幂等补传；生产环境不返回模拟数据。

**当前审计（2026-08-07）**：正式 iOS 和生产云的绑定、家庭、资料、天气和消息调用均指向云端；盒子 `main.py` 仍挂有同名旧 API，并与本地用户/家庭/升级控制表耦合，不能只删单个天气或消息函数，否则会留下半套不可用控制面。先完成独立子项：删除启动时自动注入 `demo:living_room` 的演示摄像头及 `GOHOME_ENABLE_DEMO_CAMERA` 环境开关；验证脚本中的演示 URL 仅作为内存测试夹具保留。正式云业务路由、用户/家庭表和本地升级控制面的整体迁移列入下一阶段联合收口，当前状态保持“处理中”。
**独立子项记录（2026-08-07）**：后端演示摄像头注入已在 `b482076` 中移除；本次同时删除管理端的演示摄像头分支、旧手动/本机模式控制和 `configure-demo-mode.sh`，并增加静态门禁；内存测试夹具仍仅用于回归，不进入生产页面或发布包。

**云业务路由收口记录（2026-08-07）**：已从盒子 `main.py` 删除用户注册/登录、家庭/老人资料、日历、模拟天气、消息生成和消息状态路由，同时删除对应的 Pydantic 请求模型和路由专用辅助函数；正式云端是这些业务的唯一所有者。盒子本地账户/家庭/消息历史表暂不做 DROP，仅作为历史数据保留，待完成迁移对账后再按 GH-013 流程清理。生产边界回归已增加上述路由禁止重新出现。

**设备绑定重复路由收口（2026-08-07）**：盒子不再提供用户侧的绑定、绑定码、本地设备令牌交换和自身心跳 API；保留 `/pair` 局域配对入口，由盒子出站调用生产云完成凭证交换，以及设备令牌上行的 v1 路由。删除对应请求模型和本地会话辅助函数，历史绑定表仍保留待 GH-013 对账；边界门禁已锁定这些路由不得回归。

**控制面矩阵复核（2026-08-07）**：新增 `docs/control-plane-matrix.md`，逐项记录正式 iOS、生产云和盒子管理端/设备出站接口的调用者、唯一所有者和允许边界。复核确认正式 App 只直接访问盒子的四个局域网入口；账户、家庭、摄像头配置、实时会话和通知归生产云；盒子只负责本地管理、采集/分析/合成、设备状态与事件上行。当前生产云尚未提供包发布和 rollout 编排 API，因此盒子签名校验、原子安装和回滚执行器暂不删除，也不把盒子上的用户侧包路由误当成完整生产闭环；该缺口继续由 GH-034/GH-038 跟踪。

## GH-037 至 GH-040 安全与部署边界

### GH-037 本地管理身份不满足产品安全基线

**根因**：`BoxInitService` 将 `admin / 123456` 写成固定初始凭据，且 `GOHOME_ADMIN_MUST_CHANGE_PASSWORD` 默认关闭；登录没有失败次数、指数退避或临时锁定，文件会话也没有跨进程并发保护。
**处理**：首次启动生成每台盒子唯一的一次性设置凭据或由实体配对流程建立管理员密码；生产环境强制首次修改，增加账号/IP 组合限流和安全审计，密码修改不得通过创建额外会话来验证旧密码。
**验收**：全新数据目录不存在通用默认密码；连续失败触发有界锁定且成功登录后恢复；会话创建、修改密码和并发写入回归通过，管理端不泄露凭据状态细节。
**本地验证**：新盒子生成随机一次性凭据并以 `0600` 文件保存，状态 API 不返回凭据、哈希或凭据状态；历史 `admin / 123456` 启动时自动轮换；第三次连续失败开始指数锁定，锁定上限 5 分钟；认证、会话、改密和安全审计使用同一跨进程文件事务，改密直接核验哈希且撤销全部会话。并发 9 会话、失败锁定、成功恢复、旧凭据迁移、改密和审计专项回归通过。待树莓派部署后读取一次性凭据、完成首次改密并复验重启与并发登录。

### GH-038 升级供应链可执行任意未验证内容

**根因**：包发布只把 SHA-256 放入元数据，安装时没有重新计算比对，也没有受信任公钥签名；ZIP/TAR 使用无边界的 `extractall`，`entry_path` 可解析到版本目录外，随后运行时守护可直接执行 `.py`、`.sh` 或可执行文件。
**处理**：升级控制面归生产云；盒子只接受带版本、设备范围、内容哈希和 Ed25519 签名的不可变清单。下载到临时文件后先验大小、哈希和签名，再使用逐成员安全解压并验证最终入口位于版本目录内；原子切换版本并保留经过同样验证的上一版本。
**验收**：篡改包、错误签名、ZIP Slip、Tar Slip、绝对路径、符号链接逃逸和越界入口全部被拒绝；合法包安装、启动失败回滚和断电恢复通过。
**本地验证**：新增版本化签名清单，签名覆盖包类型、版本、家庭/设备范围、字节数、SHA-256、公钥标识、文件名、入口类型、入口路径和安装策略。发布记录与盒子安装均重新验签和校验包体；ZIP/TAR 改为逐成员复制并拒绝穿越、绝对路径、重复成员、符号/硬链接、设备节点、加密 ZIP、成员数和展开体积越界。版本目录只允许同签名内容幂等复用，安装后入口、文件索引和原始包在每次启动前复验；运行方式由签名 `entry_type` 明确声明，不再按后缀或权限猜测。新 App 通过启动健康检查后才原子切换当前清单，启动或激活失败只回滚到已验证版本。专项攻击回归和边缘完整回归 `51/51` 通过；仍需在树莓派配置正式公钥、部署依赖，并完成真实安装、进程崩溃、断电和回滚验收。

### GH-039 盒子媒体上传服务与生产云重复且当前不可用

**根因**：盒子同时暴露家庭媒体上传、公开媒体下载和设备媒体接收，`create_upload_session()` 还读取未定义的 `event_id`；事件接收后会把本地快照再复制到对象目录，和正式上传队列、生产云资产、COS 对象形成多份来源。盒子上行与云端接收又分别使用 `read_bytes()`、`await request.body()`，媒体越大，进程瞬时内存越高。
**处理**：删除边缘 `ObjectStorageService`、全部 `/api/v1/media/*`、`/api/public/media/*` 与边缘 `/api/v1/device/media-assets*` 接收路由；本地事件不再提升第二份媒体资产。盒子只由持久上传队列将原始证据文件流式发送到生产云，生产云按设备和家庭鉴权后直接流式写 COS，未启用 COS 时流式写临时文件并原子提交。签名升级包独立为 `PackageArtifactService` 和 `/api/v1/package-artifacts/*`，使用声明大小、临时文件、原子替换与 `package_artifact` 留存类别，不再复用家庭媒体语义。
**本地验证**：AST 所有权契约确认旧媒体路由和实现符号均已消失；事件证据上传测试确认传递文件对象而非整包字节；COS 测试确认请求流直接进入对象存储且字节数匹配；升级包流式上传、超限拒绝、临时文件清理和签名供应链回归通过。边缘完整回归 `53/53`；云端 `101` 项中 `100` 通过、`1` 项仅因未配置 PostgreSQL 集成地址跳过。
**实机验收**：部署树莓派和生产云后，在断网、恢复、重复任务、接近大小上限和 COS 短暂失败场景确认队列幂等、内存稳定、对象唯一、事件可鉴权查看且生命周期可对账；完成前保持“待实机验收”。升级控制面迁移生产云继续由 GH-034/GH-038 跟踪，不得恢复通用媒体接口。

### GH-040 非树莓派和旧 Web 试点职责仍进入盒子运行时

**根因**：`EdgeBootstrapService` 只实现 macOS `launchd`，却和 Pi 生产设置一起装配；`PublicPilotService` 继续生成 `app-shell.html`、`watch.html` 等旧 WebView 链接；盒子还持有 APNs 私钥配置和直接发送实现，扩大攻击面并形成多条通知路径。
**处理**：Pi 盒子只由版本化 `systemd` 单元管理；macOS 开发启动工具迁出生产包。删除旧 Web 试点链接生成和盒子直连 APNs，事件只通过持久上行提交生产云，由云端完成单次推送、回执和失效 Token 管理。
**验收**：Pi 部署清单不含 LaunchAgent、旧 Web App 路由或 APNs 私钥；本地算法断网继续工作，恢复后同一事件只由云端通知一次。
**本地验证**：已删除盒子端 LaunchAgent、旧 Web 试点、公网链接增强、通用通知器、APNs relay、App push 服务及测试通知脚本，共净删除约 1,700 行运行时代码。`/ui`、盒子通知/APNs/运行时安装路由和对应 Schema、环境变量、SQLite 表、规则字段均已移除；旧数据库启动时幂等删除 `notification_enabled`、`app_push_tokens` 和 `notification_deliveries`，保留其余算法规则值。新增生产运行时边界回归，确认 `systemd` 是盒子唯一运行时、云端仍独占 APNs。边缘完整回归 `54/54`；云端 `101` 项中 `100` 通过、`1` 项仅因未配置 PostgreSQL 集成地址跳过。
**实机验收**：部署树莓派后确认旧模块、旧脚本、旧表和 APNs 环境均不存在，服务由同一个 `gohome-edge-agent.service` 启动且重启恢复；断网制造一条风险事件，恢复后只上传一次，生产云只生成一次消息和一次 APNs 投递。完成前状态为“本地根因已消除，待实机验收”。

## GH-041 至 GH-043 算法语义与质量证明

### GH-041 骨架可视性错误依赖跌倒证据资格

**根因**：`RtmposeAnalyzer._result_from_raw_poses()` 对每组 Pose 执行跌倒证据质量门槛，核心点或可见点不足时不但禁止告警，还将整组 Pose 从 `poses` 删除。吃饭、桌面遮挡、半身和侧身等常见场景因此在算法页和骨架模式完全无骨架。
**处理**：保留唯一 `poses` 当前帧骨架流，每组 Pose 分别声明 `display_evidence_eligible / person_evidence_eligible / fall_evidence_eligible` 及对应质量明细。最低显示质量只决定是否绘制；更严格的核心点和跌倒质量只决定能否进入正式风险计算。跌倒质量不足时保留当前骨架与姿态事实，但移除 `fall_candidate`、正式跌倒分数归零。场景聚合和 `PoseFactorGraphEngine` 只消费 `fall_evidence_eligible=true` 的 Pose，缓存和光流结果继续仅用于有界显示。
**验收**：站立、坐姿进餐、桌面遮挡、半身、侧身和多人场景中，当前可见关键点按真实置信度显示；不完整 Pose 不触发跌倒事件；无效几何仍拒绝；完整 Pose 保持原有跌倒能力；旧 Pose 和补偿状态仍不得成为正式证据。
**本地验证**：新增独立证据边界回归，覆盖半身骨架可见但跌倒资格为零、完整骨架同时具备显示和风险资格、无效几何拒绝、场景聚合不重建风险、因子图拒绝 display-only Pose。同步更新视觉管线、跌倒规则和因子图断言；连续跟踪、同步骨架流、纯骨架隐私流专项及边缘完整回归 `55/55` 通过。状态为“本地根因已消除，待双摄真人进餐、遮挡、侧身和跌倒实机验收”。

### GH-042 EACP 尚无可复现的模型质量依据

**根因**：当前 `PoseFactorGraphEngine`、`PostureClassifier` 和 `FallAnalyzer` 主要由固定权重、阈值和空间规则组成；`EacpAcceptanceService` 只记录人工单次场景是否产生事件，并不计算样本级 Precision、Recall、F1、ROC、混淆矩阵或分层误报率。
**处理**：评估报告升级为 v2 唯一公共实现，帧评估和序列评估统一输出 TP/FP/TN/FN、Precision、Recall/Sensitivity、Specificity、F1、Accuracy、Balanced Accuracy、FPR/FNR、95% Wilson 区间、P50/P95 延迟、按数据集/动作类别/受试者/视角分层结果及完整误报漏报清单。每份报告绑定 Git revision 与 dirty 状态、算法源文件哈希、模型哈希、完整配置哈希、清单及媒体内容指纹、命令与运行平台。质量声明门禁要求显式独立 `test` split、完整标签和可读样本、train/validation/test 齐全、序列和受试者不泄漏、至少 30 个测试正序列和 50 个测试负序列；不满足时报告固定声明“仅工程回归，不可用于产品准确率或论文级结论”。
**数据审计**：数据审计升级为 v2，检查缺失文件、缺失标签、缺失/重复帧顺序、序列元数据冲突、同序列跨 split、同受试者跨 split、相同内容跨 split 和未分配 split；稀疏/密集抽帧不再重复计为两套独立来源。退役火灾数据门禁和可伪造生产事件的公开样本注入脚本已删除，单场景 `EacpAcceptanceService` 明确只做运行闭环验收。
**当前结论**：现有样本可支持规则回归，但只有 25 个独立跌倒/ADL 时序来源、12 个正例、13 个负例、2 个公开数据集、6 张真实家庭负样本，且无 train/validation/test 划分和完整姿态序列。`fall_rule_regression=ready`，真实家庭试点、时序模型实验和产品质量声明均为 `insufficient`；当前不得宣称论文级、最优或产品准确率。
**本地验证**：新增评估完整性回归，验证统一指标、Wilson 区间、分层统计、复制目录后数据指纹稳定、内容变化后指纹变化、序列/受试者泄漏拦截和严格质量门禁；数据审计回归、EACP 服务/API 回归、真实 person 帧报告和真实 GMDCSA24 序列报告均通过。完成完整边缘回归后提交；实机数据采集和独立测试集建设作为下一阶段质量资产继续跟踪。
**验收**：每个发布算法包含数据版本、模型哈希、完整阈值、总体及分场景指标、混淆矩阵、置信区间、P95 延迟和失败样本清单；双摄实机长时误报/漏报与独立测试集一致。在门禁变为 ready 前，产品页、比赛材料和论文不得引用准确率结论。

### GH-043 暖色启发式不能承担火灾告警

**根因**：`FireAnalyzer` 只根据 RGB 暖色比例、黄色区域、纹理、连通域和相邻帧变化计算分数，电视、灯光、阳光、红色物体和厨房场景均可能误报；当前标签和事件语义却是“火灾应急报警”。
**处理**：产品交付前将该能力降为不通知的实验候选，或接入经过独立数据验证的火焰/烟雾模型并结合多帧持续性和传感器证据；未通过验证不得发送紧急通知。
**验收**：真实火焰/烟雾、电视画面、暖灯、夕阳、红衣、厨房蒸汽等分层测试有明确指标；只有达到发布阈值且连续确认的危险事件通知一次。

### GH-044 管理端保留三套失效运行时和七个跳转页面

**根因**：正式管理端已经收敛为 `login.js + console.js`，但生产静态目录仍包含旧版 `app.js`、`detection.js`、`vision.js`。三者重复实现摄像头选择、抓帧轮询、检测框、规则保存和盒子直发测试通知，其中 `app.js` 还直接访问当前页面不存在的 DOM。`detection.html`、`rules.html`、`stream.html` 和三个 `algorithm-*.html` 仅加载 `redirect.js` 跳转到新页面，形成无业务价值的历史入口。
**处理**：删除三套旧运行时、六个跳转 HTML 和 `redirect.js`；管理端生产静态资源只保留四个正式页面、登录页面、`login.js`、`console.js` 与两份当前样式。建立静态入口清单测试，校验每个 HTML 的本地脚本依赖存在且只能属于正式集合，生产包不能重新引入历史入口。
**验收**：管理首页、摄像头配置、视觉算法、事件日志和登录页均能加载；静态入口测试与边缘回归通过；部署清单不再包含旧页面和旧脚本，代码行数与职责重复可量化下降。
**本地验证**：已删除 2,009 行旧 JavaScript、104 行跳转 HTML 和 2 行跳转脚本。静态入口契约确认五个正式页面只依赖 `console.js / login.js`；两份脚本 Node 语法、Python 编译、差异检查和边缘完整回归 `48/48` 通过。状态为“本地根因已消除，待盒子部署页面验收”。

### GH-045 管理端保留失效算法剧场并伪造百分比指标

**根因**：正式算法页已经取消算法示意容器和模式选择器，但 `console.js` 仍保留完整的八模式演示档案、WebM 加载、模式切换、演示文案和指标换算，部署目录仍包含 8 个演示视频。画面质量、用餐、夜间、跌倒和火灾等内部规则量被重新组合成貌似准确率的百分比，其中火灾分数被乘以 `2800` 并封顶 `98%`；这些数值没有训练集、测试集或统计口径。
**处理**：删除不可达的算法剧场运行时、演示素材和模式分支；管理页只呈现当前帧后端实际返回的原始信号、状态机阶段、真实模型置信度和可测运行指标。规则内部量必须保留原单位和字段名，未经独立测试集验证不得包装成准确率、通过率、健康度或可信度。
**验收**：生产静态目录不含算法演示视频；管理端脚本不存在人为组合百分比或火灾倍数换算；算法页所有数值均能映射到后端响应字段或明确的计数/时间计算；静态契约、脚本语法和完整边缘回归通过。
**本地验证**：已删除 643 行不可达模式/演示/伪指标运行时和 8 个 WebM；算法页固定为统一感知，只显示当前目标计数、后端模型名、帧龄/分析延迟及现有状态机结果。静态资源契约、Node 语法、差异检查和边缘完整回归 `48/48` 通过。状态为“本地根因已消除，待盒子部署页面验收”。

### GH-046 暖色启发式仍进入正式安全事件与通知链路

**根因**：GH-043 已确认 `FireAnalyzer` 只是暖色、纹理、连通域和帧间变化启发式，但 `RuleEngine` 仍将连续命中生成为 `critical fire_candidate`，归类为 `safety_alert` 并交给正式上传、云端复核和通知链路；管理页仍提供“火灾应急报警”开关并显示“火灾事件已触发”。
**处理**：删除暖色启发式实现及其逐帧 CPU 分析，从推理结果、规则状态机、事件、证据持久化、云端复核资格、通知、管理端、配置 Schema 和评估脚本中完整移除。SQLite `rules` 表幂等删除五个废弃列，历史 `events/event_candidates` 保持只读可审计。后续若重新上线，必须以新的经过验证能力版本和独立迁移方案接入，不能复用实验字段伪装正式能力。
**验收**：任意暖灯、电视、夕阳、红色物体或启发式连续命中均不会创建事件和通知；正式管理页无火灾应急开关、事件状态或准确率表述；历史事件仍可审计但不再产生新记录；规则引擎与通知去重回归通过。
**本地验证**：删除 `FireAnalyzer` 191 行实现及正式链路全部调用；旧规则表五列迁移后归零，非火灾配置和 `updated_at` 原值保留；连续十帧注入旧 fire 字段不会产生候选、状态或持久化摘要，历史火灾事件仍可读取。聚焦迁移、视觉管线、持久化、事件去重、管理端静态契约和完整边缘回归通过。状态为“本地根因已消除，待盒子部署后确认运行指标与页面”。

## 2026-08-02 本地修复与验证记录

- GH-002/GH-003：骨架模式改为显式空房校准背景替换，不再调用人物模糊；删除自动背景学习、中性色填充和 safe-scene 兼容输出。流代次复验完成前一律返回 `stream_revalidation_required`，不输出当前帧。
- GH-004：渲染身份固定为 `camera_id + source_key + stream_generation + frame_id`；新增相邻较新 Pose 拒绝回归，证明错误帧姿态不会绘制到当前帧。
- GH-005/GH-006：唯一正式协议为 `edge-composed-mjpeg-v1`。云端删除 Pose 上传、scene 上传、Pose SSE 和 scene MJPEG；会话 JSON 与实际 MJPEG 响应头同时声明协议版本和 `composition-owner=edge`；iOS 同时校验两项。
- GH-007：每路上传改为一个在途请求和一个可覆盖最新帧。20 帧快速输入回归中最大 HTTP 并发为 1、上传序号单调、最终上传第 20 帧；校准等待不会重启摄像头采集。
- GH-009：人体分割同一 HEF 只创建一个共享 runtime，设备推理串行占有；两路并发回归只创建一个 runtime，重置单路摄像头不关闭共享 runtime。Pose 和 Object 后端原本已分别使用单实例 runtime。
- GH-010：共享读取器不再保留第二份 BGR 帧；真实帧只由中央最新帧缓存持有，订阅者按序号等待后取得一份私有副本。GH-029 已进一步删除黑帧保持与旧像素替换。正式云中继路径为一次 RTSP 解码、按需一次缩放、一次隐私合成和一次 JPEG 编码；`render_jpeg()` 不是正式直播入口。
- GH-011：云端滚动直播指标从 `server.js` 提取为独立 `RollingStreamMetrics`。有效 FPS 改为按真实接收帧间隔计算，不再在启动阶段固定除以 10 秒；同时报告 10 秒样本数、帧间隔 P95/最大值、传输延迟 P95/最大值、最后帧龄和 stale reject。新增确定性单元测试覆盖启动、窗口裁剪和卡死后帧龄。
- GH-010 实机续查：离线摄像头原来由共享读取器和 `capture_frame()` 旁路同时反复建连。现统一为每路一个托管采集所有者；分析、直播和抓图只能消费中央最新帧。断线重试按 `0.5/1/2/4/8` 秒有界退避，成功解出真实帧才清零，并报告连接尝试、连续失败和下次重试。`192.168.1.3:554` 当前真实拒绝连接，但不再阻塞或重复解码第一路。
- GH-011 实机续查：在线摄像头 `/1/2` 实测 `640x360@15`，`/1/1` 实测 `2880x1620@15`，两路均由 RTSP 声明 15 FPS；主码流只增加像素，不增加帧率。修复离线阻塞后在线解码约 `14.98 FPS`、隐私输出约 `14.92 FPS`、公网逐帧 JPEG 接收约 `13 FPS`。Pose 元数据新增每路 64 帧有界同帧历史，渲染可在 Tracker 推进后仍按 `camera_id + source_key + frame_id` 取得正确骨架，不保存第二份图像。
- GH-010/GH-011 自动回归：18 项边缘核心验证全部通过，覆盖 Hailo 分割 runtime、共享采集、断线退避、节拍、Pose/KLT、同帧同步、三种隐私、单在途上传、跌倒规则、推理调度、事件去重、配置同步、媒体生命周期、环形留存、关停和路由所有权。
- GH-012：完成跨端路由所有权审计并删除边缘端重复视频服务、播放会话、视频档位、节点调度、旧 App 路由、废弃 Schema/配置和数据库方法；媒体资产提升迁入 `ObjectStorageService`。新增 AST 契约检查，锁定盒子管理流和设备流共用 `PrivacyMjpegStream`，生产云端拥有正式播放会话，正式 iOS 只连接生产云端。
- 自动验证：盒子核心回归全部通过；云端 Node 测试 `101` 项中 `100` 通过、`1` 项因未配置 PostgreSQL 连接跳过；正式 `ios-shell` 单元测试 `127/127`、UI 测试 `25/25` 通过，`xcodebuild` 返回 `TEST SUCCEEDED`。
- GH-010/GH-011 尚需树莓派逐阶段剖析、生产云端部署和 TestFlight 解码实测；持久 H.264/H.265/WebRTC 仍是正式目标，当前 MJPEG 只能作为过渡/降级链路，因此两项保持“处理中”。
- GH-013：盒子真实数据迁移、历史外键修复和 SQLite 压缩已通过，`10,476` 条违规归零并回收约 `576 MB`；生产 PostgreSQL/COS dry-run、真实删除和盒子长时循环仍未验收，因此保持“待实机验收”。
- 上述其余条目仍需部署到树莓派和生产云端，并由 TestFlight 双摄真人长时验收，因此状态只能是 `待实机验收`，不能标记 `已关闭`。

## 2026-08-02 源生命周期与唯一成品流部署记录

- 边缘端 `48/48` 项回归通过。新增冻结流确定性回归：重复像素不增加有效 FPS 或帧号；持续冻结会清除中央帧缓存、提升流代次并向 Worker、隐私渲染器和中继发布统一失效事件，旧 Pose、跟踪、分割和成品帧不得继续显示。
- 生产云只合并视频协议所需的最小差异，没有覆盖生产独有代码：所有播放会话统一声明 `edge-composed-mjpeg-v1`，不再向正式 iOS 返回 Pose/scene 路径；云端缓存流、空流、静态回退和盒子代理统一返回 `X-GoHome-Display-Transport: edge-composed-mjpeg-v1` 与 `X-GoHome-Composition-Owner: edge`。
- 生产鉴权实测通过：真实家庭摄像头播放会话不含旧 Pose/scene 字段；实际 MJPEG 返回 `200` 和持续帧数据，协议头与会话 JSON 一致；临时验收会话已删除，服务 `active`、`NRestarts=0`。
- 盒子已部署并单次重启。摄像头 31 为 `192.168.1.11`，摄像头 32 为 `192.168.1.7`，两路均 `online`；新进程没有访问旧 `192.168.1.3`。有效输入约 `14.6 / 13.6 FPS`，云端完成接收约 `14.3 / 13.6 FPS`，上传失败均为 `0`。
- 原画与人物模糊的盒子成品流实测返回持续 JPEG、统一协议头和 `composition-owner=edge`。纯骨架未标记通过：源身份规则升级后历史空房校准不再匹配，两路必须在无人画面重新执行显式空房校准，再完成管理首页、算法诊断页和 TestFlight 三端验收。
- 正式部署目录只保留 `verify-vision-runtime.py` 一项运行时自检；其他 `verify-*.py`、历史备份、字节码和旧视频模块均不再部署。HailoRT 自身生成的运行日志仍需纳入日志轮转与容量统计。

## 2026-08-02 双摄空房校准与成品流复验

- 通过受鉴权管理端链路抓取当前原画，确认摄像头 31（电视柜上，`192.168.1.11`）和摄像头 32（冰箱上，`192.168.1.7`）画面均无人后，分别执行显式空房校准。
- 两路均一次完成 8 帧稳定空房确认和 3 帧当前流代复验，人物拒绝数为 `0`，状态为 `ready`；校准按摄像头、配置源身份、分辨率持久化，没有删除、重绑或修改摄像头连接配置。
- 六条用户成品流（两路摄像头乘原画、人物模糊、纯骨架）均返回持续 JPEG，响应统一声明 `edge-composed-mjpeg-v1` 和 `composition-owner=edge`。12 帧短测约 `8.4-12.1 FPS`，除一路人物模糊样本有 1 个重复 JPEG 外，其余样本均为新帧。
- 本次只关闭了“未校准导致纯骨架零帧”的运行阻塞，不能关闭 GH-002 至 GH-008。仍需真人进入、横向行走、静止、坐下、离开，并在管理首页、视觉算法页和 TestFlight 同时确认无人物像素、无残影、无双骨架、无串流和无客户端二次绘制；随后执行 30 分钟双摄稳定性测试。

### GH-047 安全升级后管理登录仍显示失效默认密码

**根因**：GH-037 部署时正确地将历史通用凭据 `admin / 123456` 轮换成逐设备一次性凭据，但旧登录 HTML 没有缓存约束，浏览器继续展示“开发阶段初始账号”；用户按错误提示重复登录后触发既有指数限流。一次性凭据只保存在盒子 `0600` 文件中，迁移时没有同步说明当前访问方式。
**处理**：受保护页面重定向统一携带认证静态版本；登录 HTML、脚本、样式和认证 API 统一返回 `no-store`；登录页只说明使用盒子初始化凭据。未认证状态继续隐藏一次性凭据和待改密细节，真实一次性状态只在成功登录响应和受认证状态中返回。
**验收**：旧缓存访问受保护页面后进入当前版本登录页；页面不再出现 `123456`；一次性凭据登录成功后必须改为至少 10 位的新密码；改密撤销全部旧会话并删除一次性凭据文件；失败限流、审计和重启登录继续有效。
**实机进展**：树莓派已部署并单次重启，服务 `active`、`NRestarts=0`、无 warning。管理页 GET 返回版本化 303 登录地址，登录页与认证 API 均返回 `no-store`；首次用户操作发现 8 位新密码被后端正确拒绝，但前端将 Pydantic 错误数组显示为 `[object Object]`，`auth-5` 已增加 10-128 位提交前校验、结构化错误格式化和不可解析响应处理。用户已完成合规密码设置并以新凭据进入管理端；盒子认证元数据为 `credential_kind=user_defined`、`must_change_password=false`，一次性凭据文件已删除。GH-047 已关闭。

### GH-048 算法诊断画面与用户隐私模式语义混合

**根因**：管理端在所有页面动态插入同一“画面隐私”选择器，但视觉算法页固定连接诊断 Pose 流。用户在该页选择“骨架”后，中间仍显示原画和黄色骨架，界面错误地暗示诊断画面就是用户隐私成品流。当前两路持久空房基线文件又已为空，云端期望模式仍为 `skeleton`，导致真实首页/App 成品流被阻断但管理页没有明确呈现。
**处理**：视觉算法页固定为不可切换的“原画 + 蓝色骨架”诊断语义；隐私模式选择只属于管理首页和摄像头页，明确命名为“App / 首页画面”。管理诊断流与隐私成品流共用一份骨架边、颜色、描边和线宽契约，算法页不再按风险状态切换为黄色或红色。用户纯骨架流在逐路校准未就绪时，前端不建立连接，后端在 MJPEG 建立前返回结构化 409，禁止 200 空流和任何原画降级。校准错误统一提取结构化中文 `message`。
**验收**：算法页没有可操作的隐私分段按钮且始终显示蓝色诊断骨架；首页/App 的纯骨架画面只包含背景与同色蓝色骨架；未校准时显示明确状态、真实输出 FPS 为 0 且无空连接；两路重新空房校准后持久文件存在，重启后复验恢复，不需再次校准。
**实机进展**：完整边缘回归 `58/58` 通过并已部署。Pi 服务 `active`、`NRestarts=0`、warning 日志为空；盒子实际静态资源已是 `privacy-contract-1`。两路源流稳定约 15 FPS，Hailo Pose 与人物分割均为 `ready`、推理失败为 0。摄像头 32 已完成空房校准及当前流代复验并进入 `ready`；摄像头 31 的持久基线已恢复发现，但因真实人物在场保持 `revalidating / person_present / 0/3`。待 31 自动复验完成后，执行双路纯骨架与 TestFlight 同帧验收，再做单次重启持久化验收。

### GH-049 未校准纯骨架仍持续消耗人物分割算力

**根因**：纯骨架渲染先等待同步 Pose、执行 Hailo 人物分割和掩码传播，再由背景重建阶段发现空房基线不存在并抛出 `calibration_required`。两路最终零输出、零上传，但仍持续占用 Hailo、CPU 光流和内存带宽；Pi 现场达到约 `74°C`、进程约 270% CPU，Pose 模型锚点只有约 `2.25 / 3.20 Hz`。
**处理**：在背景重建器增加按摄像头、配置源身份和分辨率检查的正式基线前置门禁；纯骨架在任何 Pose 同步等待、人物分割、背景重建和 JPEG 编码前先验证持久基线。缺失基线或正在校准时立即阻断；持久基线存在但流代待复验时仍进入原有分割与场景复验流程，不能错误跳过安全复验。
**验收**：未校准纯骨架连续运行时分割调用、编码和上传均为 0；原画、人物模糊、显式校准、持久基线复验和已校准纯骨架行为不变。部署后以分割成功次数差分、CPU、温度、Pose 锚点 FPS 和服务告警为实机指标。
**实机结果**：完整边缘回归 `58/58` 通过后部署并单次重启。两路未校准持续运行期间分割成功次数、分割锚点和传播帧均保持 `0`，隐私提交/完成仍为 `0`；服务 `active`、`NRestarts=0`、warning 日志为空。Pi 温度由约 `74°C` 回落到 `65°C`，进程 CPU 由约 270% 回落到约 90%；有人画面的 Pose 模型锚点达到约 `14 Hz`，无人画面按调度策略约 `2 Hz`，两路展示约 `13-15 FPS`。GH-049 已关闭。

### GH-050 摄像头切换期间复用旧 MJPEG 像素

**现象**：管理首页或算法页从一路摄像头切换到另一路后，摄像头名称和请求地址已更新，但新流首帧到达前仍保留上一条流的最后画面，看起来像后端串流或跨摄像头污染。流不可用时旧画面也可能继续显示。
**根因**：页面跨摄像头复用同一个 `<img>` 元素；旧 `src` 的解码像素不会在赋新地址时自动清空。前端又在 900 毫秒后仅凭 `src` 存在隐藏加载层，没有等待真实首帧；旧会话的延迟 `load/error` 事件也没有摄像头和会话代次约束。首次替换节点的实现仍未在脱离 DOM 前移除旧 `src`，快速切换后旧 MJPEG HTTP 请求继续占用订阅和渲染资源，最终使新流长期停在加载中。
**处理**：每次建立或重连视频先提升 `streamGeneration`，再隐藏旧节点、解除旧 `load/error` 并移除旧 `src` 终止请求，然后创建新图像元素。新元素默认不可见，只有当前摄像头、当前代次的真实 `load` 事件才能显示。移除定时揭示逻辑，错误和加载期间始终显示明确占位状态；校准请求同时按摄像头持有唯一 pending 状态，轮询重绘不能制造重复提交或串按钮状态。
**验收**：管理首页与算法页连续切换 31、32 两路各 20 次；加载、失败和重连阶段不得出现上一摄像头像素，新首帧出现前占位层持续可见，名称、请求 URL 和实际场景始终一致。断开任一路后必须显示不可用而非旧画面；校准按钮单次点击只产生一次对应摄像头请求。
**当前进展**：第一轮本地实现、Node 语法、静态运行契约和完整边缘回归 `57/57` 通过并部署；实机 20 次切换在中途发现订阅者由正常每路约 2 个累积到 `4 / 5`，新首帧饿死。第二轮补齐连接终止、事件解除、代次顺序和独立缓存版本，完整边缘回归再次 `57/57` 通过并部署。真实 20 次交替切换中旧画面全部立即隐藏，首帧 `494-1312 ms`、中位 `751 ms`；订阅数稳定在测试前后的 `31:2 / 32:4`，源流约 `15 FPS`、重连为 0、Hailo 推理失败为 0。待断开一路的不可用状态验收后关闭。

### GH-051 公网媒体平面缺少正式鉴权与部署边界

**根因**：边缘持久 H.264 发布器已完成，但云端尚无固定版本 MediaMTX、路径级发布/读取鉴权、WHEP/ICE/TURN 边界和可审计部署配置。如果直接开放 RTSP 和 WHEP，任意设备可覆盖流，过期家庭成员仍可读取，隐私模式变化后旧会话也可继续使用。
**处理**：新增独立 `MediaAccessService`。盒子发布必须使用已签发设备令牌，且 `live/{device}/{camera}` 路径必须与设备、家庭、启用摄像头完全对应。App 读取会话使用最长 5 分钟、默认 120 秒的 HMAC 令牌，绑定用户、家庭、设备、摄像头、路径、动作、隐私模式和时间；每次 MediaMTX 建连再核对当前成员资格、归属和隐私模式。内部鉴权路由同时要求本机回环来源和独立共享密钥，只返回 HTTP 决策，不泄露令牌或家庭数据。
**部署边界**：固定 MediaMTX `v1.19.3`，仅允许 RTSPS/TCP 发布和 WHEP/WebRTC 读取；管理 API、指标和 WHEP 监听仅回环，nginx 只暴露 `/media/`；公网仅开放 `8322/tcp`、`8189/tcp+udp`、`3478/tcp` 和 Coturn `49160-49200/udp` 中继范围。禁用 RTMP、HLS、SRT、MoQ、录像和任意路径，禁止新发布者踢掉现有发布者。Coturn 使用独立 systemd 单元和 `root:turnserver / 0640` 配置文件，密钥不得进入进程参数；Let’s Encrypt 部署钩子验证证书/私钥匹配后原子更新 MediaMTX 证书并重启服务。
**本地验证**：鉴权专项 `3/3` 通过，覆盖设备令牌哈希、路径篡改、归属变化、成员撤销、隐私变化、到期、内部调用者密钥和健康信息脱敏。完整云端回归 `104` 项中 `103` 通过、`1` 项因本机未配置 PostgreSQL 集成地址跳过；MediaMTX YAML 已独立解析通过。
**WHEP 接入进展**：正式播放会话已改为调用 `MediaAccessService`，返回短时 WHEP URL、Bearer 权限和会话元数据，不再签发播放票据或暴露 MJPEG 产品地址。iOS 原生客户端在会话创建后用同一 Bearer 权限完成 WHEP 能力发现和 SDP 交换，并拒绝非 HTTPS、跨源资源地址和过期会话。旧云端/App 正式 MJPEG 路径已删除；当前剩余边界是生产媒体服务和 TURN 的真实部署、盒子 H.264 发布以及 TestFlight 实机验收。
**生产部署进展**：云服务器已安装并校验官方 MediaMTX `v1.19.3` 与 Coturn `4.6.1`。首次部署发现远程脚本把三组密钥写成字面量占位符，同时 `/etc/gohome` 的目录权限阻断专用服务账号读取配置；占位符已全部替换为三组独立随机密钥。Coturn 已改为读取 `/etc/gohome/coturn/turnserver.conf`，MediaMTX 独占 `/etc/gohome/media`，两个目录分别由 `turnserver` 与 `mediamtx` 组管理。账号下实际资源已核实为轻量应用服务器 `lhins-rf6o2tns`（`118.25.151.101`），而非 CVM；轻量防火墙已最小放通 `8322/tcp`、`8189/tcp+udp`、`3478/tcp` 和 `49160-49200/udp`，未修改域名、证书、`80/443` 或原有规则。外网 TCP 建连、`8322` TLS 1.3 证书校验和 UDP 抵达 `8189/49160` 均通过；`gohome-mediamtx` 与 `gohome-coturn` 稳定运行。尚需新云端 API 原子切换、盒子 H.264 真实发布、直连 ICE、强制 TURN 和 TestFlight 双摄 30 分钟验收，GH-051 保持处理中。
**验收**：匿名发布/读取拒绝，跨设备、跨摄像头和过期成员拒绝，隐私模式变化后旧会话立即失效；直连 ICE 和强制 TURN 均通过双摄 30 分钟 TestFlight 验收后才能关闭。

### GH-052 云端原子发布在 unit 安装失败后留下错误指针

**根因**：安装器进入发布 staging 目录执行 `npm ci` 后，继续使用调用方传入的相对 `gohome-app.service` 路径。文件因当前目录变化而不可见，但此时 `/opt/gohome/current` 已经切到新发布；原 trap 只清理 staging 和临时链接，不恢复流量指针。
**处理**：安装开始时将归档和 unit 文件解析为绝对路径；完整发布目录就位后先安装 unit 并 `daemon-reload`，然后才原子切换 `current`。切换后的重启、健康检查或任何命令失败统一触发 EXIT 事务：恢复旧指针、重启旧版本、清理失败发布目录。
**生产验证**：首次失败未重启生产进程，旧 API 持续 `active`，`current` 已显式恢复为 `/opt/gohome/app`。修正脚本通过 `sh -n` 与路径顺序检查；随后从干净提交构建并成功切换到不可变发布 `/opt/gohome/releases/20260804035908-aac1a9d2333d`。API 健康检查通过、`NRestarts=0`，MediaMTX 与 Coturn 保持 `active`，GH-052 已关闭。

### GH-053 云端发布遗漏 PostgreSQL 迁移

**现象**：新云端 API 已成功切换并通过健康检查，但盒子同步摄像头配置时返回 `HTTP 500`，生产日志显示 `media_assets.retention_class` 不存在。
**根因**：发布包包含 `local-app-server/migrations/*.sql`，却遗漏唯一迁移执行器 `scripts/apply-postgres-migrations.js`；安装器只安装 Node 依赖、切换版本并重启服务，没有在切流前执行迁移。应用健康检查没有覆盖当前完整数据库结构，因此代码版本和 PostgreSQL Schema 发生漂移。
**处理**：发布清单显式包含迁移执行器；安装器在切流前以 `gohome` 服务账号读取正式环境并执行所有按版本排序、校验和保护的迁移。迁移失败时禁止安装 unit、切换 `/opt/gohome/current` 或重启服务。数据库迁移不随应用文件回滚，因此每个迁移必须与上一应用版本向后兼容，禁止破坏性字段删除和语义替换。
**验收**：新发布归档同时包含迁移执行器和完整迁移目录；生产 `schema_migrations` 登记至 `013_media_lifecycle.sql` 且校验和一致；`media_assets.retention_class` 等生命周期字段存在；盒子配置同步恢复 2xx；发布后 API、MediaMTX、Coturn 均无重启和新增 5xx。重复部署或重复执行迁移必须幂等，不产生重复字段、索引或种子数据。
**生产验证**：修复已由提交 `aac1a9d` 独立推送。新归档包含迁移执行器、完整迁移目录及云端入口，共 125 个受控文件；安装阶段 `001` 至 `012` 校验和一致并跳过，`013_media_lifecycle.sql` 成功应用。生产 `schema_migrations` 共 13 条且最新版本为 `013_media_lifecycle.sql`，8 个媒体生命周期字段全部存在。切流后盒子 `/api/v1/device/sync` 从连续 500 恢复为连续 200，云端三个服务均为 `active`、API `NRestarts=0`。GH-053 已关闭。

### GH-054 COS 证据前缀与最小权限策略不一致

**现象**：数据库迁移恢复后，盒子历史证据上传持续返回 403；云端媒体生命周期任务每五分钟报告 `Access Denied`。
**根因**：生产 CAM 子账号只被授权访问 `memory-media/*` 与 `event-evidence/*`，云端代码却将盒子证据写入 `edge-evidence/*`。同一密钥在 `memory-media/*` 的 Put/Head/Delete 实测全部成功，证明密钥、存储桶和地域配置有效；`edge-evidence/*` 的 Put/Head/Delete 全部被策略正确拒绝。生命周期孤儿对账还调用桶级 `GetBucket`，现有策略只含对象操作，因而两个受管前缀的分页清单均被拒绝。
**处理**：删除失配的 `edge-evidence/` 运行时语义，上传、生命周期、测试与产品文档统一使用 `event-evidence/`；由单一共享常量约束证据和家庭记忆前缀。进一步核对正式 SDK 后确认 `getBucket` 会把请求的 `Prefix` 作为授权 `ResourceKey`，因此把 `GetBucket` 单独授权给不带路径的桶资源无法命中。正确最小策略是将 `GetBucket` 与 Put/Get/Head/Delete 放在同一条语句中，共同限定为 `memory-media/*` 和 `event-evidence/*`，禁止裸桶列举、`cos:*` 和公共访问。
**验收**：生产密钥对 `memory-media/*` 与 `event-evidence/*` 可 Put/Head/Delete，对其它前缀保持拒绝；两个受管前缀可完整分页列举。盒子失败队列恢复 200 并幂等排空，数据库对象键均使用正式前缀；生命周期 dry-run 成功返回盘点结果且不删除对象，确认清单后才启用真实删除。
**生产验收**：策略版本 4 已设为当前版本，`GetBucket` 与对象 Put/Get/Head/Delete 已共同限定到两个受管前缀。生产分页列举成功：`event-evidence/` 119 个对象、`memory-media/` 16 个对象；未授权 `unmanaged-probe/` 列举和写入均保持 403。两个受管前缀的临时 Put/Head/Delete 均成功，删除后 Head 返回 404。两项历史权限测试对象已清理。生命周期 dry-run 手动和自动周期均成功：资产 1033、COS 对象 133、COS 孤儿 0、失败 0、删除 0；自动周期时间为 `2026-08-04T05:58:34Z`。策略更新后生产日志无新增 `AccessDenied`，API、MediaMTX、Coturn 均 active，API `NRestarts=0`。GH-054 已关闭。

### GH-055 事件证据关系缺少独立数据模型

**现象**：同一事件的 before、transition、current 等证据关系只嵌在 `events.payload.evidence_media_assets`，生命周期任务、App 投影、视觉复核和账户导出分别解析 JSON。历史数字资产 ID 被复用后，数据库当前行无法表达旧物理文件原本属于哪个事件，也无法为重复但真实存在的证据对象设置非 canonical 关系和事件留存。
**根因**：媒体对象与事件之间没有一等关系表，传输载荷被误用为持久事实来源；角色唯一性、事件-资产唯一性和级联边界均只能依赖应用约定，无法由数据库约束。
**处理**：迁移 `016_event_media_assets.sql` 新增 `event_media_assets`，强制事件-资产唯一、角色枚举和每事件每角色仅一个 canonical 对象；先按事件与资产去重，再选择角色 canonical，随后删除事件 JSON 中的旧关系字段。服务端、App 事件投影、视觉复核、生命周期、账户导出和 PostgreSQL 序列化统一读取关系表。设备仍可在上传 API 中提交旧字段，但入口会立即正规化，不再持久化第二份关系。共享 `event-media.js` 统一角色合法化、重复资产消除、canonical 选择和载荷清理规则。
**本地验证**：专项覆盖重复事件-资产、同角色多资产、跨事件相同角色、事件级联删除、旧载荷非破坏清理和云导出正规化。全部 Node 测试 `110` 通过、`1` 项因本机未配置 PostgreSQL 集成地址跳过、`0` 失败；完整 `npm test`、App Server 流程、iOS 发布元数据和 `git diff --check` 均通过。生产只读预检确认 `466` 个事件含 `624` 条旧证据关系，全部能关联当前资产且时间字段均可转换；没有重复事件-资产关系，存在 `4` 组同事件同角色多资产，将由迁移确定一个 canonical、其余保留为非 canonical。物理删除继续关闭。
**生产部署**：提交 `0c2514b` 已由原子安装器部署为 `/opt/gohome/releases/20260804101820-0c2514b5617e`，迁移 `016_event_media_assets.sql` 在切流前成功应用。生产共有 `624` 条关系、`619` 条 canonical、`5` 条非 canonical，覆盖 `241` 个事件；事件-资产组合全部唯一，悬空事件和悬空资产均为 `0`，`events.payload` 中旧关系字段残留为 `0`。API、MediaMTX、Coturn 均为 active，API `NRestarts=0` 且无 warning。部署后生命周期 dry-run 成功，COS 孤儿为 `0`、物理删除为关闭状态；`227` 个历史本地候选仍按计划保留，等待独立恢复流程，不允许提前删除。
**剩余验收**：下一次真实设备事件需在正式 App 中确认三帧证据展示与视觉复核结果正常。历史恢复已由 GH-057 完成，20 个尚未确定归属的文件保持受保护审计状态。生产删除清单经人工确认前不得启用物理删除。

### GH-056 未确定归属媒体缺少保护状态

**现象**：`media_orphan_cleanup` 只能记录 pending、failed、deleted 和 resolved 等清理状态。人工确认“文件真实存在但当前无法证明产品归属”后没有正式状态可用；一旦未来启用物理删除，它仍会进入最老优先队列。
**根因**：孤儿清理状态机只覆盖删除执行，没有覆盖数据修复期间的人工审计与法律/产品保留边界。
**处理**：迁移 `017_protected_media_orphans.sql` 增加受数据库约束的 `protected` 状态、`protection_reason` 和 JSON 审计元数据；生命周期在对象内容身份未变化时持续更新盘点时间但绝不放入删除选择。文件消失或成为正式资产后可转为 resolved；物理文件发生变化时原保护身份失效，必须重新审计，不能把保护套用到不同内容。
**验收**：受保护对象在普通运行、dry-run、classification-only 和删除开关开启的测试中 selected/deleted 均为 `0`；PostgreSQL 批量持久化保留保护原因和审计元数据；生产迁移后将未确定归属对象置为 protected，再运行生命周期 dry-run 验证保护数量和删除候选互斥。
**本地验证**：受保护孤儿专项及 PostgreSQL 批量持久化通过；全部 Node 测试 `111` 通过、`1` 项因本机未配置 PostgreSQL 集成地址跳过、`0` 失败，完整 `npm test` 与 `git diff --check` 通过。
**生产部署**：提交 `3d74c42` 已部署为 `/opt/gohome/releases/20260804103318-3d74c42a9b58`，迁移 017 成功应用；保护原因和 JSON 元数据字段存在，三项服务 active、API `NRestarts=0` 且无 warning。当前尚未提前写入保护记录，需由 GH-057 的固定计划清单一次性生成，避免人工散写状态。
**生产阻塞复核**：GH-057 应用后首次生命周期 dry-run 错误显示本地孤儿 `planned=20、selected=20、protected=0`。物理删除开关保持关闭，因此未删除文件。根因不是保护策略失效，而是 PostgreSQL `timestamptz` 被驱动返回为 `Date` 后，公共 `iso()` 又交给 `Date.parse()`；隐式字符串化丢失毫秒，使 `source_modified_at` 与磁盘 `mtime` 不再相等，20 个保护对象全部被误判为内容已替换并重置成 pending。修复必须在 PostgreSQL 反序列化边界保留 `Date.getTime()` 精度，并以真实 `Date` 行、带毫秒文件时间和完整 dry-run 共同回归。部署验收要求固定为 `planned=20、protected=20、selected=0、deleted=0`，否则不得启用物理删除。
**关闭验证**：提交 `dd4b23f` 在 PostgreSQL 公共反序列化边界保留 `Date.getTime()` 毫秒精度，并加入真实 PostgreSQL `Date`、磁盘毫秒 `mtime` 和完整生命周期 dry-run 回归。全部 Node 测试 `116` 通过、`1` 项因本机未配置 PostgreSQL 集成地址跳过、`0` 失败，完整 `npm test` 与 `git diff --check` 通过。生产原子部署为 `/opt/gohome/releases/20260804110642-dd4b23fa30ee` 后，dry-run 为本地 `planned=20、protected=20、selected=0、deleted=0`，COS 孤儿为 `0`；运行时删除能力明确为关闭。三项服务 active，API `NRestarts=0` 且发布后无 warning。GH-056 已关闭。

### GH-057 历史本地媒体身份恢复

**现象**：生命周期盘点发现 `227` 个未被当前 `media_assets` 跟踪的本地文件。历史审计证明旧进程曾复用数字资产 ID，数据库行后来指向新物理文件，而旧文件仍保留在磁盘；直接按当前数字 ID 认领会把事件与错误画面继续关联。
**根因**：旧资产身份依赖可复用数字序列，物理路径、内容校验和与事件证据角色没有不可变联合身份；清库、导入或并发边界后，数据库身份和物理文件生命周期分裂。
**处理**：新增只读优先的 `reconcile-historical-media.js`。旧数字前缀只用于识别候选，永久新 ID 由 `local + 相对路径` 的 SHA-256 生成；事件归属必须由结构化 `temporal_evidence_bundle.snapshots[].snapshot_path` 唯一命中，并记录文件 SHA-256、大小、mtime、角色和采集时间。同事件同快照内容冲突、跨事件命中、同事件多角色或无命中全部转 protected。应用必须提供固定计划文件和哈希，在可串行化事务中同时持有 App Store、生命周期和恢复锁，重新扫描文件与数据库后完全一致才写入。事务只新增资产身份、修正事件关系和写审计，不移动、覆盖或删除文件。
**验收**：先在生产生成 0600 只读计划，人工核对恢复数、保护数、关系删除数和事件主图更新数；计划哈希固定后才允许 `--apply`。应用后所有恢复文件必须有唯一资产和事件关系，错误碰撞关系必须消失，protected 不得进入删除选择；再次预览必须幂等为零恢复。最后运行生命周期 dry-run，生产物理删除继续保持关闭。
**本地验证**：覆盖错误碰撞关系替换、正确现有关系保留、同快照重复物理文件、内容冲突、跨角色歧义、未映射衍生图保护和路径驱动稳定身份。全部 Node 测试 `115` 通过、`1` 项因本机未配置 PostgreSQL 集成地址跳过、`0` 失败；完整 `npm test`、脚本语法、发布清单语法和 `git diff --check` 均通过。
**生产执行**：固定计划 `/var/lib/gohome/reconciliation/historical-media-v1.json` 以 SHA-256 `8530ee04b4d11296de0d5ec593741c5451dca9ee7e1afc20e179a95d8d36b11c` 在可串行化事务中应用：227 个候选中恢复 207 个独立资产、207 条事件关系和 13 条 canonical 关系，更新 5 个事件主图，20 个无法可靠归属的文件写入 protected；未移动、覆盖或删除物理文件。全部 207 个恢复文件按物理路径重新读取并通过大小与 SHA-256 校验；应用后再次预览为恢复 0、保护 20、关系变更 0、事件变更 0。GH-056 毫秒精度修复部署后再次预览仍为 `untracked=20、recoverable=0、protected=20、relations_to_create=0、relations_to_remove=0、events_to_update=0`；数据库恢复资产和恢复关系均为 207，悬空事件与资产均为 0。GH-057 已关闭，物理删除继续关闭。

### GH-058 晚到 Hailo 结果被持续 Pose Tracker 错误丢弃

**实机现象**：两路源流稳定约 `15 FPS`，Hailo Pose 单次推理中位约 `34 ms`、P95 约 `54 ms`、失败为 `0`，但持续 Pose 状态累计 `late_anchor_count=4309/816`、`late_model_result_drop_count=8719/7866`。当前显示多数由 KLT 推进，真实模型结果不能稳定刷新当前骨架；模型已经确认无人时，旧姿态仍可能保留到跟踪超时。
**根因**：Worker 的模型分析和逐帧跟踪并行运行。模型完成时，显示线程通常已经前进一帧；Tracker 仅按 `captured_monotonic` 判断结果早于当前显示帧，就把有 Pose 的结果标为晚到且不发布，把无 Pose 的权威结果直接丢弃。现有实现虽然把晚到 Pose 留作下一帧跟踪锚点，但先把状态退回旧模型帧，不能把真实模型几何对齐到当前显示帧；晚到无人结果甚至不清理旧状态。
**处理**：同摄像头、同配置源、仍在严格时限内的模型结果必须在 Tracker 内部重基准到当前显示帧。有 Pose 时使用模型帧到当前帧的一次受质量约束光流变换，发布当前帧身份下的新锚点；模型明确无人时立即清除旧 Pose，并把当前显示帧发布为权威空结果。跨源、超过时限、无法通过几何质量门槛的结果继续拒绝，禁止视频倒退或用旧姿态覆盖当前画面。
**验收**：确定性并发回归覆盖模型结果晚一帧、晚到无人、跨源、超时和光流失败；显示帧号不得倒退，权威无人后下一成品帧不得有骨架。Pi 部署后以计数差分验证 `late_*_drop` 不再随正常 30-100 ms 推理持续增长，真人活动时真实模型锚点达到调度目标，残留骨架归零。
**本地与 Pi 进展**：Tracker 已增加最长 `200 ms` 的同源模型重基准窗口；有 Pose 时只在光流、前后向误差、仿射内点和几何尺度全部通过后更新当前帧，权威无人或人物无完整 Pose 时直接清除旧姿态。晚到结果不再先回写旧模型帧；跨源和超时结果保持拒绝。首轮 Pi 部署确认两路源流约 `15 FPS`，摄像头 32 在约 80 秒内成功重基准 `84` 个模型锚点；随后补正同一 `frame_id` 的模型结果为原位更新且不重复计算显示 FPS。第二轮约两分钟采样中，两路累计成功接纳/重基准超过 `280` 次，真正 late drop 仅 `1/2`，显示约 `14-15 FPS`；当前有人路真实模型锚点约 `3 Hz`，仍受自适应调度状态与整管线串行限制，GH-030 未关闭。专项和完整边缘回归 `58/58` 通过。GH-058 根因已部署，待真人进入、离开和残留骨架实机验收后关闭。
**2026-08-07 部署后复核**：提交 `1094411` 部署并重启后，Pi `/health` 两路 `late_anchor_count=0`、`late_model_result_drop_count=0`，持续显示输出约 `14.01/13.74 FPS`，Pose/Object/分割失败为 0，服务 `active`、`NRestarts=0`。本次窗口无人，尚未产生真人晚到结果和离场清空证据；状态从“处理中”调整为“待实机验收”，不提前关闭。

### GH-059 H.264 发布时间戳和运行状态不可审计

**实机现象**：两路 FFmpeg 持续报告 `Non-monotonic DTS`。摄像头 31 三天内启动 `77` 次、失败 `18` 次，摄像头 32 启动 `1` 次；全局 `last_error` 长期保留已经恢复的“capture thread did not stop within 5 seconds”。`bytes_written` 达数百 GB，实际统计的是写入 FFmpeg stdin 的原始 BGR 字节，不是编码输出或公网传输字节。
**根因**：rawvideo 输入同时指定固定 `-framerate` 和 `-use_wallclock_as_timestamps 1`，在帧间隔抖动或连续写入时生成重复时间戳；发布器没有记录进程启动/停止原因，无法区分隐私模式切换、源代次变化和真实故障。Relay 只保存一个永不自动清除的全局错误，恢复后仍显示故障。指标名称把原始输入字节误写成传输字节。
**处理**：rawvideo 使用唯一固定帧率时间基，移除墙钟输入时间戳，保证单调 PTS/DTS；发布器记录受控重启、进程异常和最近恢复，公开各类原因计数。Relay 改为逐路当前错误和最近已恢复错误，成功输出后清除当前故障。原指标改名为 `raw_input_bytes_written`；如需网络码率，必须读取编码器或 MediaMTX 的真实输出指标，禁止推算冒充。
**验收**：本地命令契约和真实 FFmpeg 连续变速输入均无非单调 DTS；隐私模式、源代次和异常退出的重启原因可区分。Pi 重启后双路运行 30 分钟，stderr 无新 DTS 警告，当前错误为空，进程无无因重启，字节指标语义与名称一致。
**本地与 Pi 进展**：FFmpeg rawvideo 输入已删除 `use_wallclock_as_timestamps`，改为固定 `-framerate` 与 `+genpts` 单一时间基。发布器公开原始输入字节、停止原因、最近故障与恢复，不再把 BGR 输入量描述成公网输出；Relay 使用逐路当前错误和最近恢复记录，成功提交后清除当前故障。两轮 Pi 部署后均无新 `Non-monotonic DTS`，Relay 当前错误为空，摄像头 31 发布进程单次启动无失败；摄像头 32 启动阶段发生一次 `FFmpeg frame write timed out`，进程按正式状态机恢复，随后约 `14.5 FPS`，但旧进程留下半帧 rawvideo stderr。该一次性启动故障仍需 30 分钟确认不再复发，不能提前关闭 GH-059。专项与完整边缘回归 `58/58` 通过。
**半帧根因与生命周期修复**：进一步确认每个 `640x360 BGR24` 输入帧约 `691 KB`，远大于管道原子写入上限。旧 `_write_all()` 会先向非阻塞 stdin 写入部分帧，再因 `0.75s` 超时或控制 revision 变化退出；旧停止顺序随后先关闭 stdin，FFmpeg 因 EOF 把截断 rawvideo 当作一个损坏包解析，形成 `Packet corrupt`。现将启动握手和稳态写入分为两个明确阶段：FFmpeg 通过 `-progress pipe:2` 报告首个编码输出前使用有界 `5s` 启动时限，首个输出后恢复 `0.75s` 稳态时限；状态公开 `publish_ready`、进程年龄和编码器报告帧数。任何已写入部分帧的超时、进程退出、暂停或关闭都必须先硬终止对应 FFmpeg，再关闭 stdin，保证半帧不会以 EOF 进入解码器；同时记录部分帧字节数、完整帧大小和中止次数。正常完整帧边界仍使用有序关闭，不改变最新帧单槽、隐私模式上下文重启或指数退避语义。
**本地验证补充**：专项新增冷启动延迟读取、部分管道容量超时、关闭发生在帧写入中间、硬终止与 stdin 关闭顺序、启动/稳态时限和进度命令契约。冷启动读取延迟超过稳态时限但小于启动时限时完整帧成功提交且无进程失败；部分帧失败和关闭中断均固定为 `kill -> stdin_close`，并公开准确部分字节计数。编码进度中的 `frame/fps/out_time/speed/stream quality` 只更新发布就绪和帧数，不得进入 `stderr_tail`；真正 FFmpeg warning 才进入故障审计。专项通过，完整边缘回归 `58/58` 通过。
**长时监控新增根因**：部署后冷启动三轮均通过，但 30 分钟监控中摄像头 32 的发布器对象在源流未重连、`stream_generation=1`、配置版本和隐私模式未变化时被单独重建，原对象生命周期指标随之丢失。根因是直播签名每三秒调用 `remote_camera_id_for_local_camera()`，该函数每次重新读取 `config-sync-state.json`；配置同步与隐私更新又直接截断重写该文件。一路签名若在写入窗口读到空或半写 JSON，会临时从云端摄像头 ID 退回本地 ID，触发无业务变化的线程销毁重建；另一路可在文件写完后读取，因此不会同时重建。处理将摄像头映射所有权收回 `ConfigSyncAgent` 的线程安全内存快照，配置和隐私状态使用同一重入锁串行更新，并以同目录临时文件、文件 fsync、`os.replace` 和目录 fsync 原子提交。上传与直播统一读取同一内存映射，不再把磁盘文件当作运行时通信通道。需完成专项、完整回归、Pi 部署和新的 30 分钟双路监控后才能关闭 GH-059。
**生命周期可观测性**：Relay 现在跨发布器对象保存每路摄像头线程的累计启动/停止次数、最后启动/停止原因和时间；签名变化、源切换、摄像头停用、线程异常退出和服务停止使用不同原因。发布器对象合法重建后不再把整个摄像头生命周期抹成“首次启动”，新的长时监控可直接区分受控重启与异常退出。
**持久状态写入收口**：配置同步原来即使云端配置、映射、规则和维护命令完全不变，也会每 10 秒重写约 29 KB 的状态文件并再次更新规则时间。现以持久事实差异作为唯一写入条件；相同规则版本不再写 SQLite，相同状态不再创建临时文件、fsync 或替换正式文件。`last_sync_at` 继续保留为内存运行指标，不再借无意义磁盘写入表达心跳。
**关闭验证**：提交 `b830d80` 将摄像头映射收归 `ConfigSyncAgent` 的锁保护内存快照，上传与直播共用同一解析器；磁盘状态使用同目录临时文件、文件 `fsync`、`os.replace` 和目录 `fsync` 原子提交。提交 `30bb662` 保留跨发布器对象的摄像头线程生命周期，提交 `43df10d` 清除启动前遗留停止原因，提交 `ffd67e0` 停止无变化状态和规则写入。四项提交与 DIS 提交 `78e24c9` 的本地完整边缘回归均为 `58/58`。Pi 上关键文件 SHA-256 与主线完全一致；重启后状态文件在配置同步持续推进超过 30 分钟期间保持 `mtime=1785898150`、大小 `29047`、SHA-256 `e4eaf64df1004e2b2c083e0201117a569b79bd5b5227044f79e99f4735868970`，无临时文件残留，`last_sync_at` 从 `2026-08-05T02:52:53Z` 推进到 `03:23:48Z`。精确最新版 61 次监控零采样错误、零服务重启、零发布器重建、零进程失败、零半帧、零 stderr，系统日志自重启后无 warning；进程实测约 `63.7% CPU`、`349472 KiB RSS`，温度 `59.5-63.4°C`。GH-059 的根因、自动回归、部署和长时验收已全部完成，状态关闭。

### GH-060 重启复验把不可验证误判为机位改变

**实机现象**：SQLite 受控压缩后的服务重启中，两路持久空房基线文件均存在且校准信息完整，但摄像头 31/32 同时进入 `scene_review_required`。摄像头 31 的几何证据为 13 个匹配、8 个内点、内点率 0.6154；摄像头 32 为 22 个匹配、12 个内点、内点率 0.5455。两路结果均为 `unverifiable / insufficient_geometry_inliers`，并没有得到 `camera_view_changed` 结论。

**根因**：几何验证器在固定内点门槛前提前返回，未计算已有变换的实际视角位移；背景状态机又只消费 `accepted` 布尔值，把 `unverifiable` 与可靠确认的 `camera_view_changed` 当成同一种失败。单次复验失败会立即清零进度并永久切换为 `scene_review_required`，后续可靠同视角帧没有恢复机会。

**处理**：场景验证统一为 `same_view / camera_view_changed / unverifiable` 三态。中等但可用的几何证据继续计算视角位移并公开置信度；只有可靠的移动证据才能产生 `camera_view_changed`。状态机按路保存连续同视角、连续可靠移动和不可验证次数；不可验证保留基线、保持严格复验并继续有界采样，不得升级为需重校准。只有连续多帧可靠确认移动才阻断该路骨架；任一可靠同视角证据会清除移动候选。已就绪流遇到暂时不可验证时继续使用同帧人物掩码和持久基线完成隐私合成，不回退原画、模糊或旧帧。

**首轮 Pi 反证与继续修复**：提交 `596dcff` 精确部署后，两份 `.npz` 文件哈希保持不变、服务 `NRestarts=0`，但单应矩阵仍把两路判为强移动：32 路中位/最大角点位移约 `0.04770 / 0.05512`，31 路约 `0.05813 / 0.08850`。这证明三态状态机已不再混淆 unverifiable，但单一单应模型仍会把局部特征簇上的稳定匹配外推为全画面移动。后续判定增加独立部分仿射 RANSAC、两种模型的一致性和双端内点空间覆盖；只有单应与仿射均为强证据、均确认超限且匹配覆盖足够画面时，才允许输出 `camera_view_changed`。模型冲突、仿射不可用或特征集中时统一回到 `unverifiable`，不得按重复帧累计成移动证据。

**第二轮 Pi 反证与低纹理复验**：提交 `4f9c402` 部署后，32 路自动完成 `3/3` 并恢复 ready。其单应位移约 `0.05127 / 0.06293`，但仿射位移仅 `0.00518 / 0.00581`，空间覆盖 `0.1076`，正确识别为局部模型冲突而不是整机移动。31 路不再进入场景复核，但当前只有约 6 个 ORB 匹配，持续处于 `insufficient_geometry_matches`。低纹理路径新增全局梯度相位对齐，只在响应充分且全局位移接近零时提供中等置信 `same_view`；它不能单独输出 `camera_view_changed`。全局平移、低响应或位移超限仍保持 unverifiable，避免为恢复 ready 降低 ORB 阈值。

**关闭验证**：提交 `57d7578` 的几何 v4 精确部署后，31 路相位响应约 `0.21231-0.21288`、全局位移比 `0.00111-0.00106`，连续完成 `3/3` 并恢复 ready；32 路保持 ready，相位响应 `0.34105`、位移比 `0.00074`。两路移动候选、不可验证计数和活动复验调度最终均为 0。两份 `.npz` 文件 SHA-256 始终保持 `291639...bd8` 与 `8a2cda...344`；运行源码哈希与主线一致，服务 active、`NRestarts=0`、无 warning。完整边缘回归 `59/59`，GH-060 根因、自动回归、真实重启恢复和文档均完成，状态关闭。

**验收**：确定性回归覆盖低纹理同视角、局部特征簇伪移动、普通大面积家具变化、灯光变化、连续不可验证、随后恢复可靠同视角、连续可靠机位移动和双路独立状态。完整边缘回归通过后精确部署 Pi；服务重启不重新校准，两路现有基线都应自动恢复 `ready`。随后才允许切换纯骨架并执行模式逐样本断言的长时真人验收。

### GH-061 TestFlight 与生产 WHEP 发布顺序错配

**现象**：App 可以切换原画、模糊和骨架，但三种模式均无画面，并提示“服务器数据格式暂时无法读取”。

**现场证据**：生产云 `/api/v1/video/sessions` 持续返回 `200`，同一时段 MediaMTX 两路 H.264 发布均为 `ready`，但 WHEP reader 和 WebRTC session 始终为 `0`。说明 App 在视频协商前已经失败。TestFlight Build 9 归档时间为 2026-08-03 23:16，仍要求 `ticket / stream_url / stream_path / edge-composed-mjpeg-v1`；正式 WHEP 迁移提交 `ecc694a` 于 2026-08-04 09:27 才完成，生产云当前返回 `session_id / whep_url / authorization / media_path / whep-h264-v1`。Build 9 因必填 `ticket` 缺失在 JSON 解码阶段失败，未发起任何 WHEP `OPTIONS` 或 `POST`。

**根因**：实时传输协议迁移先部署到生产云，支持新契约的 iOS 版本尚未进入 TestFlight；历史 GH-005 只关闭了 Build 8 到 Build 9 的旧 MJPEG 协议错配，不能覆盖后续 WHEP 迁移。发布流程缺少跨端协议一致性硬门禁和 App Store 分发滞后检查。

**处理**：保持生产唯一链路为 H.264 + WHEP/WebRTC，不恢复 MJPEG、不放宽协议校验、不增加双轨传输。正式 iOS 主线使用 `CameraPlaybackSession` 的 WHEP 契约和 `WHEPStreamClient`，补充生产响应缺少可选 `minimum_privacy_mode` 时的解码回归；发布校验同时读取 iOS 与云端协议源，要求传输标识、组合所有者、WHEP 客户端文件和工程版本源一致。版本提升为 `1.0.0 (10)` 并从唯一正式工程重新归档上传。

**验收**：Build 10 从 TestFlight 安装后，原画、模糊、骨架均能在不退出 App 的情况下建立 WHEP reader；生产访问日志出现会话签发和 WHEP 协商，MediaMTX reader 大于 0；两路切换、前后台恢复和模式切换不再出现数据格式错误。完成真机验收前 GH-061 不关闭。

**当前进展**：修复提交 `79ead62` 已推送主线；WHEP 定向测试 `7/7`、iOS 完整测试 `148/148`、云端回归 `118/119`（唯一跳过为未配置真实 PostgreSQL URL）和发布门禁均通过。Build 10 已于 2026-08-06 08:35 上传 App Store Connect 并进入处理，等待 TestFlight 真机完成关闭证据。

Build 10 真机已证明旧 JSON 契约错误消失并成功完成 WHEP `OPTIONS`，但进一步暴露 GH-062 的 ICE 状态机错误。GH-061 在 Build 11 完成真实播放前仍不关闭。

### GH-062 WHEP Trickle ICE 状态机错误

**现象**：TestFlight Build 10 已能解析生产会话并切换三种模式，但均无画面，约 8 秒后提示 `NSURLErrorDomain -1001`。

**现场证据**：Build 10 对两路 `/api/v1/video/sessions` 均取得 `200`，随后 WHEP `OPTIONS` 均立即取得 `204`；每次重试都没有 SDP `POST`，MediaMTX WHEP session 与 reader 始终为 0。盒子两路纯骨架合成约 `12.5/13.2 FPS`，H.264 path ready、输入错误为 0，排除盒子、算法、编码和云端流源。错误周期与 iOS `completeOffer()` 固定 8 秒超时完全一致。

**根因**：iOS 在发送 WHEP offer 前轮询 `iceGatheringState == complete`，偏离 MediaMTX 1.19.3 正式 reader 的 Trickle ICE 流程；真实 iPhone 的候选收集没有在 8 秒内进入 complete，因此永远不发送 offer。生产 `/etc/gohome/media/mediamtx.env` 已正确包含公网 `webrtcAdditionalHosts`、Coturn TCP 中继和 secret-based 临时凭据，8189/3478 TCP 从电脑和盒子均可达；静态 YAML 的空数组只是由环境覆盖的安全默认值，不是配置缺失。

**处理**：iOS 改为设置本地描述后立即 POST offer，按 SDP media line 生成 `application/trickle-ice-sdpfrag`，通过同一 WHEP resource 串行 PATCH 当前候选；候选与会话代次绑定，切路、切模式和停止后拒绝旧候选。保持现有生产公网 8189 直连候选与 Coturn TCP 中继，不改动或输出共享密钥。不得通过增加等待时间、忽略候选失败或恢复 MJPEG 处理。

**验收**：自动测试覆盖 offer 不等待 gathering complete、候选片段严格生成、Bearer PATCH、乱序串行和生命周期失效；生产日志必须出现 `POST -> PATCH`，MediaMTX WHEP reader 大于 0并产生 outbound bytes。Build 11 真机完成两路三模式、Wi-Fi/蜂窝、前后台、切路切模式和断网恢复后关闭。

**当前进展**：版本已提升为 `1.0.0 (11)`；实现已与 MediaMTX `v1.19.3` 正式 reader 对齐，并进一步删除冗余 `sdpMid`、使用锁保护的单一候选队列和 Actor 批量排空，补齐连接建立期早期失败与候选 PATCH 失败的资源清理。WHEP/Peer 定向测试 `13/13`、iOS 完整单元与 UI `154/154` 均零失败零跳过；云端回归 `118/119`，唯一跳过为未配置真实 PostgreSQL URL；发布门禁通过。提交 `e023531` 已推送主线，Build 11 于 2026-08-06 09:39 上传并进入处理；尚未取得真机 `SDP POST 201 / candidate PATCH 204 / reader > 0` 证据，因此保持处理中。

### GH-063 MediaMTX systemd 网络族限制阻断全部 WHEP 会话

**现象**：TestFlight Build 11 的两路摄像头、原画/模糊/骨架三种模式全部提示“实时视频协商失败”。

**现场证据**：2026-08-06 09:55-09:56，生产 `/api/v1/video/sessions` 持续返回 `200`，WHEP `OPTIONS` 返回 `204`，Build 11 已立即发送 SDP `POST`；两路 SDP `POST` 稳定返回 `400`。MediaMTX 同时记录每个 WebRTC session 创建后立即关闭，正文为 `error getting local interfaces: route ip+net: netlinkrib: address family not supported by protocol`。两路 H.264 path 均 `ready/online`、输入字节持续增长、编码为 `640x360 H264 Baseline`，reader 与 outbound bytes 为 0，排除盒子、摄像头、隐私模式和 H.264 发布链路。

**根因**：正式 `gohome-mediamtx.service` 使用 `RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6`，遗漏 Linux 路由查询所需的 `AF_NETLINK`。MediaMTX/Pion 即使配置 `webrtcIPsFromInterfaces: false`，仍需通过 route netlink 选择可达的本地 ICE 地址；systemd 在进程级拒绝该 socket 后，服务器无法生成 SDP answer，所有摄像头和隐私模式共用的 WHEP 协商都必然失败。Build 10 因客户端尚未发送 SDP `POST`，没有提前暴露这个独立部署缺陷。

**处理**：只在 MediaMTX 单元的受限网络族中加入 `AF_NETLINK`，保留 `NoNewPrivileges`、`ProtectSystem=strict`、专用账号、回环 WHEP/API/metrics 和其余最小权限边界；Coturn 与云端 API 不需要 route netlink，不扩大其权限。新增正式部署契约校验，锁定 MediaMTX 只允许 `AF_UNIX/AF_INET/AF_INET6/AF_NETLINK`，同时确认 WHEP 回环监听、8189 UDP/TCP 和禁止 `PrivateNetwork=true`。

**验收**：生产 unit 与主线一致并单次重启 MediaMTX；真实 Build 11 请求必须达到 `session 200 -> OPTIONS 204 -> SDP POST 201 -> candidate PATCH 204`，MediaMTX 不再记录本机接口错误，reader 和 outbound bytes 持续增长。随后完成双路三模式切换、前后台与 Wi-Fi/蜂窝验收；在取得真实证据前 GH-063 不关闭。

**关闭验证（2026-08-06）**：提交 `efc7977` 已推送并部署到轻量服务器；只重启 `gohome-mediamtx`，API、Coturn 和盒子未重启。两路发布约 2 秒内恢复 `ready/online`，MediaMTX `NRestarts=0`。Build 11 真实请求在 11:49-11:50 多次达到 `session 200 -> OPTIONS 204 -> SDP POST 201 -> candidate PATCH 204`；两路均出现 peer connection established 并读取 H.264 path，`outboundBytes` 实测约 `199147 / 5176909` 且持续增长。日志不再出现 `error getting local interfaces`。Build 11 已证明 WHEP 协商根因修复，GH-061、GH-062、GH-063 关闭；三模式与网络长时仍按交付验收计划继续，不以短时播放替代长测。

### GH-064 本地闭环校验器把合法空资料当成失败

**现象**：使用当前主线启动全新 JSON 临时服务运行 `verify-local-closed-loop.js` 时，服务、登录和家庭创建通过，但在首次读取老人资料处以 `404` 退出。

**根因**：新家庭允许先完成账号/家庭/盒子流程，再由用户补充老人资料；正式 API 对未填写资料返回结构化 `404`，而校验器无条件要求读取必须 `2xx`。此前直接复用本机 7 月遗留的 8788 服务，未暴露这个初始状态边界。

**处理与验证**：校验器现在只将精确的“老人资料尚未填写”状态记为 warning，继续执行天气、关怀卡、设备、摄像头、事件和清理检查；其他 `404/5xx` 仍立即失败。未修改产品 API 或伪造默认老人资料。修复完成后重新运行全新临时服务闭环，后续以实际结果决定是否还存在产品问题。

**关闭验证**：全新临时 JSON 实例进一步确认没有预置盒子和摄像头，这是干净开发环境而非绑定丢失。校验器只在“回环地址 + JSON store”组合下把空拓扑标为 warning，并明确引用独立 onboarding 验证；生产 PostgreSQL、远程地址或已配置摄像头仍使用严格失败条件。当前主线临时实例的 onboarding `13/13` 通过，闭环校验无产品错误退出，测试数据由正式清理接口删除。GH-064 关闭。

### GH-065 重复内容帧错误拖低直播投递帧率

**现象**：Pi 两路实际解码约 `15 FPS`，但静止场景的 H.264 编码输入常降到约 `10-12 FPS`；摄像头厂商 App 明显比盒子管理端和 GoHome App 流畅。Hailo、FFmpeg 和公网 WHEP 同期没有失败或积压。

**根因**：共享读取器用内容指纹识别冻结源时，将所有未达到冻结阈值的重复像素帧直接丢弃。内容去重原本用于保证分析帧、Pose 身份和有效 FPS 不被重复画面污染，却同时成为 H.264 直播的唯一上游，导致合法重复解码帧也无法按源节奏进入发布器。算法帧语义和视频投递帧语义被错误合并。

**处理**：每路保留两个有界最新槽位和两套单调身份。内容帧只在指纹变化时推进，继续作为 Motion、Pose、风险和活动日志的唯一输入；直播投递帧接收未冻结的每个有效解码帧，保留内容 `frame_id` 供同帧 Pose/分割合成，并使用独立 `delivery_frame_id` 与投递时间进入 H.264 发布器。重复内容命中同一隐私成品缓存，不重复执行 Hailo、分割或风险判断。近黑帧不进入任何槽位；连续重复达到冻结阈值后仍原子清空两个槽位、提升 `stream_generation`、清除旧 Pose 并重连。

**自动验证**：专项证明重复投递不会推进分析帧身份、改变分析时间戳或唤醒分析消费者；直播投递身份持续前进；冻结后内容与直播缓存同时清空，旧 Pose 生命周期失效；近黑恢复、缓存超前、H.264 最新帧单槽和隐私校准暂停保持通过。完整边缘回归 `59/59` 通过。

**验收**：精确部署 Pi 后，在两路静止、人物轻微移动和正常行走场景同步记录 `decoded_fps / content_fps / live_delivery_fps / encoder_input_fps_10s`。非冻结健康流的直播投递和编码输入应接近实际解码节奏，内容 FPS 可按真实变化降低；持续冻结必须停止投递、提升流代并重连。完成 30 分钟双路三模式、零旧骨架、零串流、零服务重启后才能关闭。

**首轮 Pi 实机证据（2026-08-06 20:59 CST）**：主线提交 `c0539a0` 的
`camera_agent.py` 和 `live_relay_agent.py` 已按文件精确部署到
`192.168.1.12`，盒子服务只重启一次，两个运行文件 SHA-256 分别为
`740994bcbcdf046850627ffb09de9860e1f602a1b6d19e3169bd0c2db8afecfc` 和
`f6368c525d82ecce699dbb643ef9422a00007eab1eaa6f2a90ba8c6b47e09381`。
重启后等待复验完成，两路均 `streaming`，`decoded_fps/live_delivery_fps` 为
`15.01/15.01` 与 `15.05/15.05`，`content_fps` 为 `13.61/14.75`，说明静止
场景的重复内容没有污染分析，但没有再压低直播投递；H.264 编码输入为
`14.48/14.23 FPS`，两路 `publish_ready=true`，读取失败、重连、编码失败和
连续失败均为 `0`。两路纯骨架复验均 `ready`，端侧成品输出约 `13.1/13.9 FPS`。

**当前结论**：GH-065 的根因修复已通过首轮实机指标验收，但不提前关闭。仍需
在管理端登录后确认实时画面，正式 App 确认两路播放无延迟堆积，并完成有人
进入、静止、行走、遮挡、离开、断流恢复和 30 分钟三模式长测；任何一项未完成
前状态保持“待实机验收”。

**手机端首轮现场结果（2026-08-06）**：用户在正式 GoHome App 守护页保持纯骨架
模式，依次经过两路摄像头并离开画面；确认手机端可以看到骨架，主观流畅度“比较
流畅”。盒子结构化证据同时确认两路人物帧均完成纯骨架合成，离开后目标清零，
没有旧骨架状态、跨摄像头或发布错误。该结果通过“纯骨架可见性与基本跟随”子门槛，
不替代三模式切换、遮挡、残影和长时指标验收。

**三模式现场结果（2026-08-06）**：用户在同一正式 App 会话依次切换原画、人物
模糊和纯骨架，确认三种模式均能显示且主观流畅。切换后盒子仍保持两路
`camera_privacy_states=ready`，源解码/直播投递约 `15.2/15.2 FPS`，H.264 编码
输入约 `12.7/12.3 FPS`，无读取、重连或发布失败。该结果证明协议和模式状态机
已完成基础切换闭环；编码输入低于源流的长尾仍需在遮挡、快速移动和 30 分钟长测
中继续观察，不能据此关闭 GH-008/GH-020。

### GH-066 摄像头断流恢复后隐私复验卡死

**现象**：拔掉一路摄像头后，该路 App 提示“实时视频协商失败”；摄像头重新插回后，盒子源流已自动恢复（`stream_generation` 增加、解码 FPS 恢复），但隐私状态长期停留在 `revalidating`，H.264 发布器保持暂停，云端没有可协商的该路媒体源。

**根因**：复验只把 `mask != nil && pose_synchronized` 当作完整证据。摄像头恢复后的空房帧可能没有同帧 Pose，但 Hailo 分割锚点已经明确给出空掩码；原逻辑将这种合法“当前帧无人”证据误判为 `person_evidence_unavailable`，每次都清零复验计数，导致发布链路无法恢复。该问题不是摄像头重新绑定或重新校准问题，也不能用旧 Pose 通过。

**处理**：复验证据按当前帧拆分：分割锚点缺失时继续阻断；分割明确为空时允许复验累计，不读取旧 Pose；分割确认有人时仍要求同帧 Pose 同步，否则清零并阻断。新增回归覆盖空分割、无 Pose、流代变化和原有人证据阻断。

**状态**：代码修复与隐私回归通过。主线 `privacy_stream.py` 已精确部署到盒子，远端 SHA-256 为 `d7a7cc077dcbff552c6f4faa29700d697068a11b61b5b231884e0a9c9aa2421e`，旧文件已备份到 `/home/gohome/deploy-backups/574df9b-gh066/app/vision/privacy_stream.py`；服务重启后 `active`、`NRestarts=0`、无 warning，两路源均 `streaming`，隐私状态均 `ready`，发布器均 `publish_ready=true`，输入约 `14.6 FPS`。尚未执行真实拔出/插回，因此保持“待实机验收”；验收时不删摄像头、不重新绑定，确认该路自动恢复 `ready/publish_ready` 且另一路持续播放。

### GH-067 App WHEP 会话失败后没有持续自动恢复

**现象**：一路摄像头离线时，App 在有限次 WHEP 重试后显示“实时视频协商失败”；摄像头插回并且盒子恢复发布后，App 不退出、不重新进入页面就不会再次协商。

**根因**：`GuardViewModel` 的播放循环在 `maxReconnectAttempts` 后直接结束并进入永久 `failed` 状态。当前选中摄像头、页面可见和 App 活跃这些恢复条件仍然成立，但没有任何生命周期任务继续探测盒子恢复状态。

**处理**：播放循环保留有限次快速重试，随后进入同一会话代次的退避恢复，延迟按 `5/10/20/30s` 封顶；成功建立新会话后自动恢复播放。切换摄像头、停止守护、页面离开和 App 进入后台会取消任务，WHEP 资源、Peer、候选队列由现有 generation 机制统一失效，保证同一路同一时间最多一个会话。

**状态**：iOS 守护定向测试 `13/13` 通过，包含持续恢复回归；今日正式工程回归单元 `130/130`、UI `25/25` 全部通过。Build 12 已从唯一正式工程归档并上传 App Store Connect，版本为 `1.0.0 (12)`，Bundle ID 为 `com.gohome.family`；需与 GH-066 一起完成断流实机验收。上传仅提示上游 WebRTC framework 缺少匹配 dSYM，不影响应用安装和播放。尚未把“真实拔出/插回后 App 不退出自动恢复”写成已验收事实，因此状态保持处理中。

### GH-068 摄像头源在线但隐私成品不可发布，客户端只显示在线/连接中

**现象**：电视柜上摄像头（ID `31`）在盒子源流状态为 `streaming`，解码约 `15 FPS`，App 设备页也显示在线；但管理端和 App 守护实时没有画面，一直处于连接中。

**现场证据**：盒子 `live_relay_agent` 显示摄像头 `31` 的 `running=false`、`publish_ready=false`、`paused=true`、`last_error=scene_revalidation_required`；摄像头 `32` 同时 `publish_ready=true` 且编码约 `14.9 FPS`。摄像头 `31` 的背景状态为 `scene_review_required`，几何判定为 `camera_view_changed`，当前相对基线的仿射位移约 `7.7%`。因此 App 的“在线”是源流/设备在线，不代表隐私成品媒体路径可读。

**根因边界**：当前不能确定这次位移是插拔时摄像头实际被碰动，还是固定机位下的几何误判。不能在未确认房间无人前自动覆盖基线，也不能跳过隐私阻断发布原画或错误骨架。

**处理**：每路摄像头按 `camera_id + configured_source_key + resolution` 持有最多 4 份用户明确确认的机位基线。重连或云台转动后，先对已有基线逐一执行光度和几何匹配；命中已知机位时只切换活动基线并完成有限帧复验，旧基线不删除。未命中时保持原画/模糊可用，纯骨架明确进入“新机位待空房确认”，绝不用有人画面自动学习或覆盖旧基线。新增基线使用候选帧平均、原子写入和容量淘汰，失败时保留全部旧基线；旧版单文件基线仍可启动加载。

**处理要求**：若摄像头确实被碰动到未知角度，只在房间无人时执行一次空房确认；如果回到以前确认过的角度，不需要删除摄像头、重新绑定或手工输入 IP。客户端和管理端应显示“已知机位复核中”或“新机位待空房确认”，不能只显示在线或连接中。

**自动验证**：隐私回归覆盖新增机位原子提交、旧基线保留、重启后两份机位自动发现、已知机位切换到复验、返回原机位、未知机位继续阻断和单路隔离；`verify-video-privacy.py` 已通过并报告 `known_camera_views_restore_after_restart=true`。

**部署证据（2026-08-06）**：提交 `cb88d26` 中四个盒子运行文件已精确安装到 `192.168.1.12`，远端 SHA-256 与主线一致。服务重启后 `active`、`NRestarts=0`；两路源流约 `15 FPS`，两路隐私状态 `ready`、`publish_ready=true`，纯骨架端侧输出约 `14.7 / 11.8 FPS`。原有两份无后缀基线文件数量和大小未改变。

**补充根因**：此前 `CameraAgent` 的流断续通知沿用摄像头源变更通道，`LiveRelayAgent` 会把一次 RTSP 重连当成配置变化，停止整路采集后的发布线程，下一轮再重新创建编码器。这会把正常重连放大成额外黑屏和发布延迟。现在事件明确区分 `transition_type=stream_discontinuity` 与 `transition_type=configuration`：前者只暂停现有发布器并进入流复核，后者才停止并重建摄像头发布线程。发布器、编码进程和摄像头配置在重连期间保持不变。

**状态**：多机位代码、专项回归和 Pi 部署已完成；流断续与配置变化分离的本地回归已通过，仍需真实执行云台转动/重连、已知机位自动恢复、未知机位提示和两路互不影响验收；未删除摄像头、未重新绑定、未覆盖现有校准文件。

### GH-069 正式 App 首页与社区视觉状态不完整

**现象**：正式 `ios-shell` 首页接入家庭图片后，图片按自身纵向比例参与布局，Hero 高度超出设计范围，标题和说明被推到首屏之外；社区推荐数据使用无效或不可达图片地址时，卡片显示大片无意义的灰色空块，用户无法区分“图片加载中”和“图片暂不可用”。

**根因**：Hero 的图片和遮罩没有在固定画布内布局，外层高度约束不足以限制图片的测量尺寸；推荐卡片和详情页各自实现图片失败状态，失败分支只绘制通用占位矩形，没有稳定的产品状态语义。

**处理**：Hero 使用固定 `220pt` 画布，图片和遮罩都受同一画布约束，文案始终叠在画布底部。新增唯一的 `ProductRecommendationImage` 组件，统一卡片和详情页的远端图片加载、裁剪、圆角和失败状态；失败状态显示类别图标和“图片暂不可用”，不伪造商品图，也不把失败当成成功缓存。

**验证**：正式工程重新由 `project.yml` 生成；最终代码状态的单元测试 `130/130`、UI 测试 `25/25` 通过，`git diff --check` 通过。模拟器截图确认 Hero 固定高度、主体与文案均位于首屏，社区失败卡片显示类别图标和明确状态，“我的”分组与账户入口无重叠；本项尚未完成 TestFlight 真机验收，保持“待实机验收”。

### GH-070 共享 Web 客户端存在第二套用户 API 路径

**发现**：根目录 Web 试点页面仍随云端发布包交付，正式 iOS 已完全原生化；共享 `edge-client.js` 却在 `/api/app/device`、摄像头、评估、事件和汇总请求返回特定 `403` 时，自动退回 `/api/device`、`/api/cameras`、`/api/events` 和 `/api/summary`。这会把正式用户契约错误掩盖成“降级成功”，并让同一页面的读取、权限和缓存可能落到不同路径。

**处理**：保留 Web 试点入口，但所有 App 语义读取固定使用 `/api/app/*`；删除 `withDeviceAccessFallback`、错误文本识别和全部旧路径回退。摄像头创建、编辑、启停和删除继续使用生产云 `/api/cameras*` 配置写入契约，不能与盒子本地管理 API 混为一谈。仓库清理清单同步写明 Web 试点、正式 iOS、生产云和盒子管理端边界。

**验收**：前端门禁直接检查共享客户端不存在 fallback，并锁定设备、摄像头、事件和汇总的正式路径；云端接口回归必须证明 `/api/app/*` 的鉴权、家庭隔离和数据返回正常。该变更只收敛云端静态客户端，不修改盒子代码、数据库、媒体、摄像头配置或 TestFlight 工程。

**部署证据**：提交 `f35d27d` 已通过 `npm test`、云端 Node 测试 `119` 项中 `118` 通过、`1` 项因本机未配置 PostgreSQL 集成地址跳过；正式云归档 `20260807142001-f35d27d15a4c` 已通过唯一安装器原子部署到腾讯云轻量服务器 `118.25.151.101`。迁移全部 skipped；`/opt/gohome/current`、远端静态客户端和公网脚本 SHA-256 均为 `f305cab849cd09f6c74168a309c1edfdb68bec93470174661f2e45e7a0835f51`。部署后 `gohome-app.service=active`、`NRestarts=0`、主进程退出码 `0`、PostgreSQL/WHEP 健康检查通过；匿名访问 `/api/app/cameras` 返回 `401`。未修改盒子、数据库业务数据、摄像头配置、媒体资产和 TestFlight 工程。

### 2026-08-07 控制面退休接口残留

**发现**：前一轮已从盒子删除用户侧设备、包发布、rollout 和运行控制路由，但盒子仍保留用户查询型 `/api/v1/events`、`/api/v1/events/{id}`、`PATCH /api/v1/events/{id}` 和 `/api/v1/summary/today`。这些路由依赖已删除的 `current_user`/设备访问逻辑，属于断裂接口；正式管理端使用本地 `/api/events`，正式 iOS 事件读取使用生产云，不应再访问盒子用户查询面。

**处理**：删除盒子用户查询型 v1 事件和汇总路由及无消费者的 `current_user`；保留盒子本地管理端 `/api/events*`，以及设备令牌保护的 `/api/v1/device/events` 上行和事件响应摘要。同步从共享 `assets/scripts/edge-client.js` 删除无消费者的设备查询、用户侧包发布、rollout、同步目标和 v1 事件查询方法；保留设备同步与设备升级执行方法。

**验收要求**：生产边界门禁必须确认退休路由不存在；管理端事件列表仍从本地 `/api/events` 获取；设备事件上行仍可幂等接收；正式 iOS 不得重新依赖盒子用户查询接口。云端正式包发布与 rollout 仍属于 GH-034/GH-038 缺口，本次不伪造完成。

### 2026-08-07 包发布协议模型残留

**发现**：盒子用户侧包发布、下载链接、执行列表、rollout 和同步目标路由已经退休，但 `schemas.py` 和 `PackageService` 仍保留只服务这些路由的请求模型与家庭权限方法，增加了错误的“盒子可以发布包”的表面能力。

**处理**：删除无消费者的 `V1PackageReleaseCreate`、`V1PackageDownloadLinkCreate`、`V1DeviceSyncTargetUpdate` 和 rollout 请求模型；删除 `PackageService` 中包创建、用户查询、下载链接和用户执行列表方法。保留设备命令触发的 `run_pending_upgrades`、签名安装、原子切换、运行时回滚、执行记录和状态回报；不删除历史 SQLite 表及其读取方法。

**验收要求**：设备升级执行专项、签名供应链专项和配置同步专项必须继续通过；生产边界不得重新挂载用户侧包管理接口。正式云端包发布与 rollout 仍需在 GH-034/GH-038 中由云端实现后，再补齐设备命令闭环。

### 2026-08-07 盒子包工件上传职责残留

**发现**：盒子没有挂载包工件路由，但 `PackageArtifactService` 仍实现用户家庭权限、上传会话、流式写入、临时文件、下载签名和公开下载，这些代码既无生产调用方，也与“包发布由云端所有”冲突。

**处理**：将盒子包工件服务收敛为只读 `object_key` 根目录约束、留存状态校验和安装器所需资产元数据；删除上传、下载签名和路由构建代码及对应请求模型。专项验证改为覆盖目录穿越、删除资产不可读、留存类型校验和字节一致性。设备签名校验、原子安装和失败回滚保持不变。

**验收要求**：边缘媒体所有权门禁必须确认盒子不含包上传/公开下载路由，但签名包供应链和设备升级执行仍通过；云端发布链路完成前，不在盒子恢复任何用户侧包接口。

**部署证据**：主线 `7f46994` 已通过唯一生产包入口部署到 `192.168.1.12`，远端 `.release-commit` 为 `7f46994df91847c341ff01b763e613124aaab908`。部署前 Pi 运行时预检确认 Python、YOLO、RTMPose、Hailo Platform、Pose/Object HEF 和 `/dev/hailo0` 全部可用；部署后服务 `active/running`、`NRestarts=0`，最近 5 分钟无 warning。两路源解码和直播投递约 `15 FPS`，H.264 编码输入约 `14.6-14.8 FPS`，两路 `publish_ready=true`，Pose/Object/分割失败为 `0`。配置同步首次启动快照中一路发布未就绪，下一次同步自动收敛为两路就绪，未发现持续状态分裂。

### 2026-08-07 生产入口残留演示分支

**发现**：演示摄像头注入和 `GOHOME_ENABLE_DEMO_CAMERA` 已删除，但根路由仍保留“所有摄像头都是 `demo:` 时进入配网”的历史判断。生产运行时不再拥有演示源，这个分支会让真实配置状态依赖已退役协议。

**处理**：根路由只根据是否存在摄像头配置决定进入配网；删除演示源判断，并在生产边界门禁中禁止该分支回归。内存测试中的 demo 夹具不受影响。

**验收**：生产边界门禁、完整边缘回归、Python 编译和部署包检查通过；已有真实摄像头配置仍进入管理端，空配置仍进入配网。

**补充处理**：抓图测试和实时分析入口也不再根据摄像头 URL 注入 `force_demo_vision`；演示帧只允许出现在回归脚本的内存配置中，生产入口不再切换到演示识别逻辑。

### 2026-08-07 断流恢复回归受线程调度影响

**发现**：完整边缘回归偶发 `58/59`，单独重跑立即通过。旧专项把消费者取得的前两张帧直接解释为重连前后帧；测试将退避等待置零后，共享读取线程可能在消费者读取首帧前完成读取失败、近黑重连和恢复，导致两个输出携带相同最新代次。

**处理**：不修改生产恢复状态机。专项改为直接收集源中断通知，要求同时出现 `stream_read_failed` 和 `stream_near_black`，并验证通知中的流代次严格单调；输出帧仍继续验证无近黑像素、无旧像素重发和身份唯一。

**验收**：专项连续重复运行和完整 `59/59` 回归必须稳定通过，不再依赖线程调度顺序。

### 2026-08-07 盒子保留无消费者的通配 CORS

**发现**：盒子管理端与正式 iOS 均不通过浏览器跨源访问盒子，但入口仍配置 `allow_origins=["*"]`、凭据和全方法/全请求头。该配置没有正式消费者，且通配来源与凭据不能形成有效的浏览器策略。

**处理**：删除盒子 FastAPI 的 CORS 中间件；管理端使用同源请求，正式 iOS 使用原生网络栈，局域网配对和配置唤醒不依赖浏览器 CORS。生产边界门禁禁止 CORS 中间件重新进入盒子入口。

**验收要求**：边界门禁、完整边缘回归、管理端静态契约和部署健康检查通过；不新增跨源兼容兜底。

**部署证据**：提交 `bc970cd` 的完整边缘回归 `59/59` 通过，并由唯一生产部署入口同步到 Pi；远端 `.release-commit` 为 `bc970cde1e96827901c9521be781ea4f2ff74526`。部署后服务 `active/running`、`NRestarts=0`，双路保持 `streaming`，解码约 `14.97-14.99 FPS`，H.264 编码输入约 `14.38 / 14.54 FPS`，两路 `publish_ready=true`。Pose、Object 和人物分割失败均为 `0`，配置同步无错误，最近十分钟 journal warning 为空；未修改数据库、模型、摄像头配置、隐私校准或运行数据。

**继续收口（2026-08-07）**：`Storage` 中仍有日历和消息候选的完整运行时 CRUD，但盒子无路由、Worker 无调用、正式 App 与生产云不依赖这些方法；它们属于已收回云端的业务职责。删除这两组方法及其仅供转换使用的辅助函数，保留 `init_schema` 中的历史表和迁移，不做表删除或数据迁移。生产边界门禁新增 Storage 方法契约，阻止这些云业务实现回到盒子；设备绑定、设备同步、事件上行、算法和媒体生命周期方法保持不变。

**本项验收要求**：边界门禁、完整边缘回归、Python 编译和 Pi 部署健康检查通过；历史日历/消息表仍可由旧数据库启动迁移保留，但盒子生产运行时不再提供对应业务实现。

**部署证据**：提交 `49a8d73` 已通过完整边缘回归 `59/59` 并部署到 Pi；远端 `.release-commit` 为 `49a8d73663dc90371e9e7d0bbc11a002384731bf`。重启初期一路短暂处于 `segmentation_unavailable` 复验窗口，约 20 秒后自动恢复；最终两路隐私状态均为 `ready`，H.264 发布均 `publish_ready=true`，编码输入约 `14.36 / 14.50 FPS`，Pose、Object、分割失败均为 `0`，共享分割 runtime 数量为 `1`，服务 `NRestarts=0`，最近十分钟 warning 为空。两路持久校准基线 SHA-256 未变化，未删除摄像头、未重绑、未清理运行数据。

**继续收口（2026-08-07）**：`Storage` 中老人资料读写方法没有盒子生产调用方，正式资料接口归生产云；删除 `get_elder_profile`、`upsert_elder_profile` 及仅供它们使用的转换函数，保留 `elder_profiles` 历史表和迁移定义，不做 DROP 或数据清理。生产边界门禁同步禁止这些云业务方法回到盒子。

**本项验收要求**：完整边缘回归和媒体生命周期迁移继续通过；设备绑定/同步、事件上行、摄像头、算法和隐私链路不受影响，历史老人资料表仍可被旧数据库启动保留。

**部署证据**：提交 `5065613` 已通过完整边缘回归 `59/59` 并部署到 Pi；远端 `.release-commit` 为 `5065613911f767a4678eaf11b2723ad0449de7cc`。部署后两路源流约 `14.97 / 15.04 FPS`，隐私状态均为 `ready`，发布器均 `publish_ready=true`，H.264 编码输入约 `13.92 / 14.33 FPS`；Pose、Object、分割失败均为 `0`，服务 `NRestarts=0`，最近十分钟 warning 为空。持久老人资料表未删除，摄像头、绑定、校准和运行数据未改变。

**继续收口（2026-08-07）**：盒子已无 rollout 创建、查询或状态推进路由，`Storage` 中的 `create/get/list/update_device_rollout*` 只在内部互相调用，没有生产消费者。删除 rollout 编排 CRUD 和专用转换器，保留 `device_rollouts` 历史表及迁移；设备端签名包查找、校验、安装、原子激活、失败回滚和 `package_executions` 记录继续保留。生产边界门禁禁止盒子重新拥有 rollout 编排方法。

**本项验收要求**：签名包供应链、设备升级执行、配置同步、历史数据库迁移和完整边缘回归全部通过；不得因为删除 rollout 编排而削弱设备端安装验证或回滚能力。

**部署证据**：提交 `6709c13` 已通过完整边缘回归 `59/59` 并部署到 Pi；远端 `.release-commit` 为 `6709c13ec56e9e1caddb27b0bdbb9bf2058b771a`。两路源解码和直播投递约 `15.07 / 14.97 FPS`，H.264 编码输入约 `14.44 / 14.26 FPS`，隐私状态和发布状态均为 `ready`，进程失败、读取失败、重连及 Pose/Object/分割失败均为 `0`；服务 `NRestarts=0`，最近十分钟 warning 为空。设备升级执行和签名验证专项保持通过，历史 rollout 表未删除。

**继续收口（2026-08-07）**：盒子 `Storage` 中的包发布创建/列表方法没有生产调用方；`PackageService` 只读取云端已发布版本并在盒子执行安装。删除 `create_package_release`、`list_package_releases`，保留发布记录读取、包工件只读解析、签名验证、原子安装、回滚和执行记录；`package_releases` 历史表及迁移保持不变。生产边界门禁阻止盒子重新拥有包发布编排写入能力。

**本项验收要求**：签名供应链、包工件只读、设备升级执行、配置同步和完整边缘回归通过；不得恢复盒子侧用户包发布接口。

**部署证据**：提交 `9858321` 已通过完整边缘回归 `59/59` 并部署到 Pi；远端 `.release-commit` 为 `9858321afe34d7010d90ff08b6cc58677b48a38f`。连续三个运行窗口中两路源解码/直播投递约 `14.95-15.08 FPS`，隐私状态均 `ready`，发布器均 `publish_ready=true`，编码输入约 `13.46-14.64 FPS`，进程失败、读取失败、重连及 Pose/Object/分割失败均为 `0`；服务 `NRestarts=0`，近期 warning 为空。启动后未出现包发布接口或历史数据迁移错误，包发布和 rollout 历史表均保留。

**继续收口（2026-08-07）**：`list_package_executions` 没有盒子生产调用方；设备升级只需读取单个执行记录并更新状态，管理端和用户侧执行列表已退休。删除列表查询方法，保留执行写入、单条读取、状态更新和最新执行摘要；`package_executions` 表及历史记录保持不变。

**部署证据**：提交 `d337287` 已通过完整边缘回归 `59/59` 并部署到 Pi；远端 `.release-commit` 为 `d337287d8948a92b31bbae5a7b17c97082e13929`。部署后两路源解码/直播投递约 `15.02 / 14.94 FPS`，H.264 编码输入约 `14.13 / 14.79 FPS`，隐私状态和发布状态均 `ready`，读取失败、重连、进程失败及 Pose/Object/分割失败均为 `0`；服务 `NRestarts=0`，最近十分钟 warning 为空。设备执行写入、单条状态更新和最新执行摘要保持可用。

**继续收口（2026-08-07）**：盒子 `Storage` 中媒体资产创建、事件关联、按快照/事件/路径查询，以及媒体上传会话 CRUD 均无生产调用方；当前生产只需读取已发布包资产，并由事件证据队列和媒体生命周期任务处理本地留存与 COS 对账。删除这组旧业务写入/查询方法及其转换器；保留 `media_assets`、`media_upload_sessions` 历史表、迁移、生命周期清理、资产读取和包安装链路。媒体生命周期验证脚本改为直接构造数据库夹具，避免测试代码继续证明盒子拥有已退休的媒体上传 API。

**本项验收要求**：生产边界门禁禁止上述方法回归；媒体生命周期迁移、证据留存、包资产读取、完整边缘回归和 Python 编译通过；历史表与已有数据不删除，COS/本地留存清理行为不改变。

**部署证据**：提交 `9891d8e` 已通过完整边缘回归 `59/59` 并部署到 `192.168.1.12`。部署后服务 `active/running`、`NRestarts=0`、主进程退出码 `0`；摄像头 31/32 解码与实时发布约 `14.99-15.06 FPS`，重连和读取失败均为 `0`；两路隐私状态均为 `ready`，共享 Pose runtime 数量为 `1`，Pose 成功推理 `397` 次、失败 `0` 次；配置同步连续失败 `0`，最近十分钟 warning 为空。盒子运行数据、摄像头配置、校准基线和历史媒体表未删除或替换。

### 2026-08-07 盒子账号与绑定编排职责残留

**发现**：`Storage` 仍实现本地用户认证、会话、家庭创建、绑定码签发/消费、设备令牌签发和用户访问判断，但盒子生产只校验云端签发的设备令牌，读取绑定关系用于摄像头配置权威判断，并记录设备心跳。其余方法没有生产调用方，会让盒子继续暴露已经收回云端的账号和设备绑定写入职责。

**处理**：删除上述账号/家庭/绑定编排方法及仅供它们使用的转换器，保留 `list_device_bindings_by_device()`、`list_device_bound_family_ids()`、`get_device_token_by_raw_token()` 和 `record_device_heartbeat()`。保留 users、families、family_members、device_bindings、device_binding_codes、device_tokens 历史表和迁移定义；生命周期验证脚本直接写入最小用户/家庭夹具，不把测试夹具变成盒子业务 API。

**验收要求**：生产边界门禁禁止账号/绑定编排方法回归；安全配对、设备令牌校验、心跳、摄像头配置权威、媒体生命周期、完整边缘回归和 Python 编译通过；已有绑定、令牌、心跳及历史账户数据不删除。

**部署证据**：提交 `e24f79f` 已通过完整边缘回归 `59/59` 并部署到 `192.168.1.12`；远端 `.release-commit` 为 `e24f79f040b738156c68f4d1b1ced20a8631019f`。部署后服务 `active/running`、`NRestarts=0`、主进程退出码 `0`；摄像头 31/32 解码与实时发布约 `14.89-14.98 FPS`，有效内容约 `12.3/13.6 FPS`，重连和读取失败均为 `0`；Pose 状态 `ready`，成功推理 `138` 次、失败 `0` 次，共享 runtime 为 `1`；两路隐私状态均为 `ready`，配置同步连续失败 `0`，最近十分钟 warning 为空。历史账号、绑定、令牌和心跳表未删除，运行数据与校准基线未替换。

### 2026-08-07 设备同步与上传队列旧辅助方法残留

**发现**：`Storage` 仍保留没有生产调用方的规则 patch 转换、latest detection 查询、已完成上传任务查询、无租约上传状态覆盖、设备同步目标覆盖和独立同步状态查询。现行生产路径已经固定为本地规则更新、上传任务租约状态机和云端目标配置拉取/设备上报；这些旧方法会形成第二套状态写入语义，增加同步分裂和上传状态被旁路修改的风险。

**处理**：删除上述无调用辅助方法，保留 `get_rules/update_rules`、设备同步的 `ensure/report/mark-applied`、上传队列的 `claim/complete/fail/list/summary` 以及检测结果写入。历史 `device_sync_states`、`upload_jobs` 和 `detection_results` 表及迁移保持不变，不改现有同步数据或上传记录。

**验收要求**：生产边界门禁禁止旧辅助方法回归；设备同步、上传租约恢复、媒体留存、检测/事件、完整边缘回归和 Python 编译通过；不得出现第二套目标配置或上传状态写入路径。

**部署证据**：提交 `d6af43a` 已通过完整边缘回归 `59/59` 并部署到 `192.168.1.12`；远端 `.release-commit` 为 `d6af43a0cc9a12f232e115b36fac193508bd7df3`。部署后服务 `active/running`、`NRestarts=0`、主进程退出码 `0`；摄像头 31/32 解码与实时发布约 `14.97-15.06 FPS`，有效内容约 `13.34/14.17 FPS`，重连和读取失败均为 `0`；Pose 状态 `ready`，成功推理 `149` 次、失败 `0` 次；两路隐私状态均为 `ready`，配置同步连续失败 `0`，最近十分钟 warning 为空。历史同步、上传和检测结果表未删除，运行数据与校准基线未替换。

### GH-071 管理端手动分析与后台正式分析争抢同一状态

**发现**：管理端 `/api/cameras/{camera_id}/capture` 直接调用 `EdgeWorker.process_camera`，而实时分析入口也直接调用 `DetectAgent.analyze_frame_with_config`。这些入口与后台调度共享 Detect/Hailo 推理资源，但没有统一的摄像头级分析所有权。手动抓帧可能与后台分析同时推进同一路的 `previous_frames`、时序引擎、姿态因子图、规则评估和事件候选，造成推理排队、结果顺序不确定和管理端操作改变正式事件状态。

**处理**：`EdgeWorker.process_camera` 成为同一路摄像头正式分析的唯一串行边界，内部通过按 `camera_id` 的可重入锁调用实际处理；管理端 `capture_and_store` 也必须进入同一所有权锁后再读取、分析和写入。不同摄像头仍可并行，不把两路摄像头不必要地串成一条全局队列；不修改摄像头共享读取器、Hailo 模型、历史数据或隐私成品流。

**验收**：新增 `verify-analysis-ownership.py`，并发触发同一路两次分析时最大并发数必须为 `1`；同时检查管理端入口使用同一所有权锁。随后运行 Python 编译、完整边缘回归、媒体/姿态/流同步专项和部署健康检查。

**部署证据（2026-08-08）**：提交 `d6ab0aa` 已通过新增并发专项、完整盒子 `60/60` 验证、Python 编译和 `git diff --check`，并由唯一部署入口安装到 `192.168.1.12`。远端 `.release-commit` 为 `d6ab0aa5c180646daad7facbdb1ca9750933d27f`；服务 `active`、`NRestarts=0`、主进程退出码 `0`。部署后的健康检查确认共享 Pose runtime 数量为 `1`，Pose 成功 `77` 次/失败 `0`，Object 成功 `28` 次/失败 `0`，两路源读取与共享缓存运行正常；H.264 发布器两路最终均 `publish_ready=true`，隐私状态均 `ready`，最近十分钟 warning 为空。生产部署按边界不上传专项验证脚本，历史表、摄像头配置、校准基线、COS 资产和用户资料均未修改。

### GH-072 连接测试仍调用完整视觉管线，且盒子保留无消费者的实时分析入口

**发现**：管理端“测试连接”只需要验证拉流和画面质量，但 `capture_preview` / 已保存摄像头测试仍调用完整 `VisionPipeline`，会额外触发 Hailo Pose、Object、宠物和场景分析；这会与后台正式调度竞争全局推理锁，却不产生正式产品结果。`/api/cameras/{camera_id}/analysis/live` 在全仓库没有正式消费者，管理端实时感知已使用连续姿态状态接口和服务端成品视频，保留它会继续维护第二套直接分析语义。

**处理方案**：连接测试改为复用共享帧，使用 `DetectAgent.analyze_frame_quality()` 只计算亮度、对比度、黑屏和运动质量，不运行任何检测模型、不写快照历史、不推进时序/规则/事件状态。删除无消费者的盒子 `/analysis/live` 路由及其完整分析辅助链；正式后台分析仍由 `EdgeWorker` 唯一拥有，管理端手动正式抓帧仍通过 GH-071 的摄像头级所有权锁。

**验收要求**：新增质量检查专项必须证明连接测试不会调用完整管线；生产边界必须确认 `/analysis/live` 不存在；现有摄像头共享读取、Hailo 运行时、管理端测试结果、正式抓帧、完整边缘回归和 Pi 健康检查全部通过。

**部署证据（2026-08-08）**：提交 `069fe23` 已通过质量专用捕获专项、完整盒子 `61/61` 验证、Python 编译和 `git diff --check`，并由唯一部署入口安装到 `192.168.1.12`。远端 `.release-commit` 为 `069fe23cb00011d8243dc1596bf47df60938ad48`；服务 `active`、`NRestarts=0`、主进程退出码 `0`。部署后 Pose 状态 `ready`、成功 `114` 次/失败 `0`，Object 状态 `ready`、成功 `42` 次/失败 `0`；两路源均 `streaming`，解码/直播投递约 `15.04/15.03 FPS`，读取失败 `0`；两路 H.264 发布均 `publish_ready=true`、未暂停、进程失败 `0`，最近十分钟 warning 为空。历史表、摄像头配置、校准基线、COS 资产和用户资料均未修改。

### GH-073 连接测试产生不受留存策略管理的孤立预览文件

**发现**：未保存摄像头的“测试连接”会把 JPEG 写入 `snapshot_dir/preview/`，但不创建数据库快照记录；历史清理只按快照表和事件证据清理，这些预览文件没有引用、没有有效期，会随每次测试永久增长。

**处理方案**：连接测试结果直接使用内存 JPEG Data URL 返回给当前页面，不再写 `preview/` 文件。正式事件证据和数据库快照仍由后台事件驱动留存链路管理，不改变 COS 资产、历史快照或用户资料。

**验收要求**：质量专用捕获专项必须确认连接测试不调用 `save_frame`；完整边缘回归、存储环形留存、媒体生命周期和 Pi 健康检查通过。

**部署证据（2026-08-08）**：提交 `1793991` 已通过质量专用捕获专项、存储环形留存专项、完整盒子 `61/61` 验证、Python 编译和 `git diff --check`，并由唯一部署入口安装到 `192.168.1.12`。远端 `.release-commit` 为 `1793991c82e6722f80edca311db7686baa25ce7e`；服务 `active`、`NRestarts=0`、主进程退出码 `0`。部署后 Pose/Object 均 `ready` 且失败 `0`，两路源均 `streaming`，解码/直播投递约 `14.99/15.06 FPS`，读取失败 `0`；两路 H.264 发布均 `publish_ready=true`、未暂停、进程失败 `0`，最近十分钟 warning 为空。历史表、摄像头配置、校准基线、COS 资产和用户资料均未修改。

### GH-074 连续姿态两个输出分支重复组装展示契约

**发现**：`continual_pose_live_snapshot` 在 `include_frame=false` 和 `include_frame=true` 两条路径中重复构造 `display_poses`、`display_people` 以及分析字段。两套实现目前相同，但未来一处修改很容易导致状态轮询和带图诊断返回不同的人物/骨架数量或跟踪状态。

**处理方案**：提取无副作用的展示组装函数，统一过滤过期姿态、生成展示人物和补齐分析字段；两条 API 路径只负责各自的帧/元数据获取和响应格式，不重复实现展示规则。不得改变连续追踪器、Hailo 推理、隐私成品流或正式事件证据边界。

**验收要求**：连续姿态 API、实时帧同步、管理端静态契约、完整边缘回归、Python 编译和 Pi 健康检查通过。

**部署证据（2026-08-08）**：提交 `3dd0fd0` 已通过连续姿态专项、完整盒子 `61/61` 回归、Python 编译和 `git diff --check`，并由唯一部署入口安装到 `192.168.1.12`。远端 `.release-commit` 为 `3dd0fd0d5ddfef8a8494ded920261754f3409b25`；服务 `active`、`SubState=running`、`NRestarts=0`、主进程退出码 `0`。部署后 Pose、Object 和人员分割均 `ready`，推理失败数均为 `0`；两路摄像头均 `streaming`，解码/实时投递约 `14.95/14.98 FPS`，读取失败 `0`，当前无连续重复帧；最近十分钟 warning 为空。当前 31 路背景状态为 `revalidating/person_present`，属于有人时拒绝自动重新校准的既有保护状态，不改变校准基线。历史表、摄像头配置、校准基线、COS 资产和用户资料均未修改。提交已部署到 Pi；GitHub 推送因本机到 `github.com:443` 建连超时待网络恢复后重试。

**状态**：已完成。不得删除历史表、摄像头配置、校准基线、COS 资产或用户资料。

### GH-085 已登录启动故障被误判为退出登录

**发现**：正式 App 持有有效令牌时，只要启动数据请求发生超时、断网、服务端 5xx 或数据解码失败，根状态机就进入登录页；从本地会话恢复时，仓储又把 HTTP 401 与普通刷新失败一并转换成陈旧数据提示，可能继续展示已经失效的会话。退出登录对启动任务的取消也没有贯穿到仓储回调，晚返回的旧请求存在重新覆盖根路由的风险。

**根因**：根状态只有“已登录内容”和“未登录”，缺少已验证会话的可恢复故障状态；仓储启动接口使用无类型字符串吞掉鉴权错误，启动、引导完成和家庭切换创建的异步任务也没有统一纳入同一个可取消生命周期。

**处理**：只有明确的 `APIError.unauthorized` 才进入登录页；超时、离线、5xx 和解码失败进入唯一的会话恢复页并保留重试入口，不伪造家庭或预览数据。仓储继续允许普通刷新失败展示可信缓存，但 401 必须向根状态传播；任务取消不再发送完成回调，所有启动数据任务由 `AppModel` 统一持有并在下一次加载或退出时取消。家庭数据契约错误与会话连接故障复用同一个恢复视图组件，但保持不同路由和文案。

**验收要求**：单元测试覆盖临时失败不退出、重试成功进入真实主界面、401 退出、有缓存的 401 不作为陈旧内容展示，以及退出后旧请求晚返回不恢复主界面；正式 iOS 单元测试和根路由 UI 回归通过。不得清理用户令牌以外的生产数据，不得修改 Bundle ID、盒子配置、校准基线、COS 资产或用户资料。

**验证（2026-08-08）**：`AppSmokeTests` `6/6` 通过，固定临时故障进入恢复页、重试进入真实家庭、401 退出和取消任务不回写；`AppRepositoryTests` `4/4` 通过，确认有缓存的 401 仍向根状态传播。正式 iOS 单元测试 `146/146`、根启动 UI `LaunchTests` `2/2` 全部通过，`git diff --check` 通过。未修改 Bundle ID、盒子配置、校准基线、COS 资产或用户资料。

**状态**：已完成，待下一次统一 TestFlight 构建真机确认断网、超时和会话过期三种启动路径。

### GH-086 单文件上传取消被当作业务失败

**发现**：网络客户端的 JSON 请求、二进制下载和 COS 直传都会把 `URLError.cancelled` 统一转换为 Swift `CancellationError`，但单文件上传入口直接调用 `URLSession.data(for:)`，漏掉了同一取消契约。头像或兼容单文件媒体上传随页面生命周期取消时，上层因此会显示“保存失败”，而不是安静结束已经取消的任务。

**根因**：四种 HTTP 方法分别维护底层传输调用，取消映射复制在三处，遗漏的上传分支没有共享统一传输边界。

**处理**：提取唯一 `perform` 传输入口，统一四种请求的 Swift 任务取消和 `URLError.cancelled` 映射；HTTP 状态、鉴权、解码和 COS 直传契约保持各自边界，不增加重试、兜底或第二上传管线。

**验收要求**：网络专项覆盖普通 JSON 请求和单文件上传取消；完整 iOS 单元测试通过，`git diff --check` 通过。不得修改媒体数据、COS 资产、用户头像、Bundle ID、盒子配置或云端业务数据。

**验证（2026-08-08）**：`APIClientTests` `7/7` 通过，普通 JSON 请求与单文件上传取消均保持为 `CancellationError`；正式 iOS 单元测试 `147/147`、`git diff --check` 全部通过。未修改媒体数据、COS 资产、用户头像、Bundle ID、盒子配置或云端业务数据。

**状态**：已完成，待下一次统一 TestFlight 构建随头像和记忆发布流程真机复核。

### GH-087 关闭记忆编辑页后发布任务仍可能继续

**发现**：记忆编辑页用未持有的 `Task` 执行媒体上传和发布；发布过程中“取消”按钮仍可关闭页面，页面消失也不会取消该任务。用户以为已经取消，上传却可能继续并最终创建记忆。即使外层任务被系统取消，ViewModel 的通用错误分支还会把取消显示为保存失败。

**根因**：编辑页的展示生命周期与发布事务没有共同所有者，上传完成到创建记忆之间也没有取消检查；网络取消、业务失败和用户取消被合并到同一个错误状态。

**处理**：编辑页持有唯一发布任务，取消按钮和页面消失均终止该任务；上传完成后、创建记忆前强制检查取消，防止不响应取消的晚返回上传继续提交；ViewModel 对 `CancellationError` 只复位发布阶段，不写失败提示。正常成功和真实错误路径保持不变，不增加第二发布管线。

**验收要求**：专项必须模拟上传忽略取消并晚返回，确认不会调用创建记忆、不会写错误提示且发布状态恢复空闲；记忆模块和完整 iOS 单元测试通过，`git diff --check` 通过。不得修改既有记忆、COS 资产、用户资料、Bundle ID、盒子配置或云端业务数据。

**验证（2026-08-08）**：`MemoryViewModelTests` `19/19` 通过；专项让上传忽略取消并晚返回，确认记忆创建调用为 `0`、没有失败提示且发布阶段恢复空闲。正式 iOS 单元测试 `148/148`、`git diff --check` 全部通过。未修改既有记忆、COS 资产、用户资料、Bundle ID、盒子配置或云端业务数据。

**状态**：已完成，待下一次统一 TestFlight 构建真机复核发布中取消和侧滑退出编辑页。

### GH-088 离开个人资料页后头像保存仍可能提交

**发现**：个人资料编辑页用未持有的 `Task` 执行头像上传和资料更新；用户在“保存中”返回上一页时，页面消失不会取消任务，头像上传晚返回后仍可能提交昵称、城市和头像。任务被系统取消时，ViewModel 还会显示“账户资料未能保存”，把用户离开页面误报成网络故障。

**根因**：资料编辑页与保存事务没有共同任务所有权，头像上传和资料更新之间缺少取消边界，取消与真实服务端失败共用一个错误分支。

**处理**：编辑页持有唯一保存任务并在页面消失时取消；头像上传完成后、资料更新前强制检查取消，防止晚返回上传继续提交；ViewModel 对取消恢复原资料且不写错误提示。正常成功和真实失败路径保持不变，不新增副本、重试或第二保存管线。

**验收要求**：专项模拟头像上传忽略取消并晚返回，确认资料更新调用为 `0`、资料保持原值、无错误提示且保存状态复位；资料权限专项和完整 iOS 单元测试通过，`git diff --check` 通过。不得修改用户头像、COS 资产、家庭资料、Bundle ID、盒子配置或云端业务数据。

**验证**：`ProfilePermissionTests` 15/15 通过；完整 `GoHomeShellTests` 149/149 通过；`git diff --check` 通过；生成的 `GoHomeShell.xcodeproj/project.pbxproj` 零差异。

**状态**：已完成，待下一次统一 TestFlight 构建真机复核保存中返回资料页。

### GH-089 取消媒体直传后上传意图无法可靠终止

**发现**：记忆图片或视频取得 COS 直传意图后，用户取消发布会进入已取消的父任务；原实现直接在该任务中请求 `memory-media-upload-abort`，网络层会立即把请求判定为取消。服务端返回上传意图数量异常时又在清理范围之外退出，已创建的意图和可能存在的 COS 对象只能等待过期任务回收。

**根因**：媒体上传没有形成“创建意图后只能完成或终止”的单一事务边界；终止请求错误继承业务任务取消状态，契约校验也发生在事务保护范围之外。

**处理**：将意图、并发直传、完成和终止收敛到独立上传事务；意图创建后的所有失败和取消统一进入终止路径；终止请求使用独立任务执行，不继承编辑页取消状态，同时继续保留原始错误语义。正式仓库工厂仅注入该事务，不保留第二套上传实现。

**验收要求**：网络级专项覆盖直传中取消和服务端意图数量异常，确认两种情况都实际发送带鉴权的 abort，且请求包含全部已创建资产；取消仍向调用方返回 `CancellationError`。专项和完整 iOS 单元测试、`git diff --check` 通过。不得写入生产 COS、云端记忆、用户资料或盒子配置。

**验证**：`MemoryMediaUploadTransactionTests` 2/2 通过；完整 `GoHomeShellTests` 151/151 通过；取消用例保持 `CancellationError`，两条异常路径均验证 abort 的鉴权、顺序和完整资产集合。

**状态**：已完成，待下一次统一 TestFlight 构建真机复核发布中取消及弱网清理。

### GH-090 iOS 仍保留第二套记忆媒体上传管线

**发现**：正式 App 已使用 COS 上传意图事务，但 `AppRepository` 仍保留旧单文件上传依赖和公开方法；批量依赖缺失时还会自动退回逐张调用旧接口。对应的 Base64 批量请求模型也没有任何调用者。这使同一产品存在两套上传契约、限额、持久化和取消行为。

**根因**：COS 直传上线后只新增了正式路径，没有删除原型期本地上传模型和兼容降级，仓库构造器继续隐藏第二条执行路径。

**处理**：iOS 仓库只保留必需的批量上传事务依赖；删除单文件上传类型、属性、构造参数、公开方法、LiveFactory 旧接口调用及无调用者的 Base64 请求模型。测试依赖未注入时明确返回无效响应，不再静默切换传输方案。

**验收要求**：从正式 `AppRepositoryLiveFactory` 发起成功上传，网络请求必须且只能按 intent、签名 PUT、complete 顺序执行并返回资产；上传事务专项与完整 iOS 单元测试、`git diff --check` 通过。不得访问生产 COS、云端记忆或用户资料。服务端旧入口另立独立问题删除。

**验证**：`MemoryMediaUploadTransactionTests` 3/3 通过；完整 `GoHomeShellTests` 152/152 通过；正式仓库成功链路严格为 intent、签名 PUT、complete，`git diff --check` 通过。

**状态**：已完成，待下一次统一 TestFlight 构建真机复核唯一上传链路。

### GH-091 云端仍暴露两条旧记忆媒体上传入口

**发现**：正式 App 已收敛到 COS 上传意图事务，但云端仍注册 `/api/v2/memory-media` 本地单图上传和 `/api/v2/memory-media-batch` Base64 JSON 上传。两条入口绕过上传意图、签名直传、abort 清理和视频统一策略，并在轻量云服务器本地写入媒体文件。

**根因**：云端在上线 COS 直传后保留了原型兼容入口和对应处理器，旧服务端测试继续调用旧接口，使重复管线没有被编译或测试淘汰。

**处理**：删除两条旧路由及全部处理器；记忆增删改持久化测试改由正式 intent、COS 对象确认、complete 契约创建资产；历史本地资产的读取与生命周期实现保持不变，不迁移、不删除已有资产。新增旧入口 404 专项，防止重复管线重新出现。

**验收要求**：记忆服务专项必须通过，正式 COS 事务继续覆盖图片、视频、abort、过期回收和资产删除；旧单图与 Base64 批量入口均返回 404；完整云端原生服务测试和 `git diff --check` 通过。不得连接生产数据库或 COS，不得修改用户记忆和历史资产。

**验证**：`native-memories.test.js` 3/3 通过；完整 `npm run test:native-server` 119 项中 118 通过、1 项因未配置 PostgreSQL 环境跳过、0 失败；`git diff --check` 通过。正式 COS 图片、视频、abort、过期回收和资产生命周期回归均通过。

**状态**：已完成，待云端部署后的生产环境健康检查。

### GH-092 已取消的仓库刷新仍可能写入新缓存

**发现**：页面离开后调用者任务已经取消，但仓库把网络请求放入共享任务并继续等待；`home/events/products/profile/memories/activity` 刷新拿到结果后没有再次检查调用者取消状态，仍可能写缓存并向已离开的页面发送新数据。这样会让下次打开页面看到不属于本次生命周期的旧回写结果。

**根因**：共享网络任务的生命周期与调用者不同是正确的，但刷新事务没有在“共享结果 -> 本地缓存 -> UI 回调”之间设置取消提交边界。

**处理**：所有刷新入口在取得共享结果后、写缓存前和写缓存后统一检查 `Task` 取消；取消的调用者不提交新缓存、不发送新鲜数据回调，仍保留既有取消状态收口。正式共享网络任务不被无关页面取消，其他活跃调用者仍可正常复用结果。

**验收要求**：用忽略取消的延迟网络加载器模拟页面退出，确认取消调用者不会把 fresh 数据写入缓存；完整 iOS 单元测试和 `git diff --check` 通过。不得修改云端数据、盒子配置、用户资料或生产缓存。

**验证**：专项 `AppRepositoryTests` 5/5 通过；完整 `GoHomeShellTests` 153/153 通过；取消调用者不会收到 fresh 回调或写入 fresh 缓存，`git diff --check` 通过。

**状态**：已完成，待统一 TestFlight 真机复核页面快速进出和弱网恢复。

### GH-093 事件处理任务未绑定详情页生命周期

**发现**：事件详情页点击“确认安全”或“标记误报”后，ViewModel 用未保存的 `Task` 提交服务端；返回上一页时任务无法取消，晚返回可能覆盖本地事件状态，网络取消也会被当成普通失败处理。

**根因**：事件处理任务没有进入 ViewModel 的任务所有权集合，详情页没有退出时的取消边界。

**处理**：ViewModel 按事件 ID 持有唯一处理任务，详情页消失时取消；取消恢复原事件的乐观状态并清除处理中标记，不写错误提示；真实服务端失败仍保留原错误提示和回滚。任务结束后自清理，销毁时统一取消。

**验收要求**：专项让处理请求阻塞后模拟详情页离开，确认任务取消、事件状态回滚、处理中状态清除且无错误提示；事件完整单测和 `git diff --check` 通过。不得修改云端事件、推送记录、盒子配置或用户资料。

**验证**：专项 `EventsViewModelTests` 3/3 通过；完整 `GoHomeShellTests` 154/154 通过；取消详情页操作会回滚乐观状态、清除处理中标记且不展示错误，`git diff --check` 通过。

**状态**：已完成，待统一 TestFlight 真机复核事件详情页快速退出和弱网恢复。

### GH-094 记忆页交互任务脱离页面生命周期

**发现**：记忆页的收藏、评论和删除由 View 创建未保存的异步 `Task`；离开页面后任务仍可能继续写入本地记忆状态，取消请求还会被当成网络失败并弹出错误提示。

**根因**：交互任务没有由 `MemoryViewModel` 持有，页面没有统一的退出取消边界；三种操作也没有统一的取消回滚契约。

**处理**：ViewModel 按记忆 ID 持有交互任务的取消句柄；记忆页消失时统一取消，任务取消时恢复收藏和评论的乐观状态、保留删除对象并清除处理中标记，不显示错误；真实请求失败仍按原操作显示失败。

**验收要求**：分别阻塞收藏、评论、删除请求后模拟离开页面，确认任务结束、状态回滚、处理中标记清除且无错误提示；完整 iOS 单测和 `git diff --check` 通过。不得修改云端记忆、盒子配置、用户资料或媒体资产。

**验证**：专项 `MemoryViewModelTests` 22/22 通过；完整 `GoHomeShellTests` 157/157 通过；收藏、评论、删除取消路径均完成状态回滚且不显示错误，`git diff --check` 通过。

**状态**：已完成，待统一 TestFlight 真机复核记忆页快速退出和弱网恢复。

### GH-095 首页关怀操作未绑定页面生命周期

**发现**：关怀消息的已联系、稍后、忽略和分享完成操作由 View 创建异步任务，`HomeViewModel` 不持有任务；用户关闭编辑页或切换 Tab 后，晚返回结果仍可能关闭关怀卡片，取消还会被显示成保存失败。

**根因**：关怀操作只有 `pendingCareAction` 展示状态，没有对应的任务所有权和页面退出取消边界。

**处理**：ViewModel 持有唯一关怀操作取消句柄；首页或关怀编辑页消失时统一取消。取消保留原关怀消息、清除处理中状态且不显示错误；服务端成功和真实失败继续使用原有产品行为。

**验收要求**：阻塞关怀操作后模拟页面退出，确认任务取消、原消息保留、处理中状态清除且无错误提示；首页专项、完整 iOS 单测和 `git diff --check` 通过。不得修改云端消息、推送记录、盒子配置或用户资料。

**验证**：专项 `HomeViewModelTests` 10/10 通过；完整 `GoHomeShellTests` 158/158 通过；取消关怀操作会保留原消息、清除处理中状态且不展示错误，`git diff --check` 通过。

**状态**：已完成，待统一 TestFlight 真机复核关怀编辑页快速退出和弱网恢复。

### GH-096 活动记录清空任务未绑定页面生命周期

**发现**：活动时间线点击清空后，ViewModel 用未保存的异步任务删除记录；页面退出后任务仍可能刷新时间线或把错误写回已离开的页面，取消请求也会显示成清空失败。

**根因**：清空操作只有 `clearingHistory` 状态，没有任务所有权和页面退出取消边界。

**处理**：ViewModel 持有唯一清空任务；活动时间线消失时取消。取消不改变已有轨迹和摘要、不显示错误，只收口处理中状态；真实失败继续显示错误，成功后才清空本地状态并刷新。

**验收要求**：阻塞清空请求后模拟页面退出，确认原轨迹保留、处理中状态清除且无错误提示；活动时间线专项、完整 iOS 单测和 `git diff --check` 通过。不得修改安全事件、告警证据、盒子配置或云端历史数据。

**验证**：专项 `ActivityTimelineTests` 7/7 通过；完整 `GoHomeShellTests` 159/159 通过；取消清空会保留原轨迹、清除处理中状态且不展示错误，`git diff --check` 通过。

**状态**：已完成，待统一 TestFlight 真机复核活动时间线快速退出和弱网恢复。

### GH-097 守护页隐私模式更新任务未绑定页面生命周期

**发现**：守护页切换隐私模式时，更新家庭策略的任务由 ViewModel 外部创建且未保存；页面退出或切换实时画面后，晚返回结果仍可能重启旧摄像头流，取消还可能残留“更新中”状态。

**根因**：隐私策略同步任务和一次性更新任务只有状态字段，没有统一的任务所有权；`stop()` 只能取消轮询和视频流，不能取消策略写入。

**处理**：ViewModel 持有唯一隐私模式更新任务；守护页停止或销毁时取消，取消不写错误、不切换模式并清除处理中状态；成功响应才应用服务端策略并重启当前摄像头流，真实失败继续显示错误。

**验收要求**：阻塞隐私策略更新后模拟离开守护页，确认模式不变化、处理中状态清除、无错误且不会重启旧流；守护专项、完整 iOS 单测和 `git diff --check` 通过。不得修改盒子配置、摄像头校准、云端历史数据或用户资料。

**验证**：专项 `GuardViewModelTests` 17/17 通过；完整 `GoHomeShellTests` 160/160 通过；取消隐私更新会保留原模式、清除处理中状态且不展示错误，`git diff --check` 通过。

**状态**：已完成，待统一 TestFlight 真机复核切换隐私模式时退出守护页和弱网恢复。

### GH-098 资料页三类偏好保存任务未绑定页面生命周期

**发现**：守护规则、内容偏好和产品偏好保存都由 ViewModel 内部创建未保存任务；用户离开设置页后，晚返回结果仍可能覆盖当前资料，取消请求会进入普通失败提示。

**根因**：三类乐观保存只有布尔处理中状态，没有对应任务所有权和页面退出取消边界。

**处理**：三类保存分别由 ViewModel 持有任务；规则、内容偏好、产品偏好设置页以及资料页退出时统一取消。取消恢复保存前的完整资料、不显示错误并清除处理中状态；真实服务端失败继续按原链路回滚并提示。

**验收要求**：分别阻塞三类保存请求后模拟页面退出，确认原资料保留、处理中状态清除且无错误提示；资料权限专项、完整 iOS 单测和 `git diff --check` 通过。不得修改云端家庭资料、盒子配置、历史数据或用户资产。

**状态**：已完成，`ProfilePermissionTests` 18/18、全量 `GoHomeShellTests` 163/163，`git diff --check` 通过。

### GH-099 账户资料刷新和保存缺少取消后的提交边界

**发现**：账户资料刷新由未持有的异步任务执行，退出资料编辑页后，晚返回结果仍可能覆盖当前头像、昵称和地区。账户资料更新请求返回后也没有再次检查取消状态，服务端忽略取消时仍可能提交已退出页面的结果。

**根因**：刷新任务没有 ViewModel 所有权；保存链路只在头像上传阶段检查取消，更新账户资料返回后直接发布状态。

**处理**：ViewModel 持有账户资料刷新任务，新的刷新会取消旧刷新，个人资料页退出时取消未完成刷新；账户资料更新返回后增加提交前取消检查，取消只恢复原资料、不显示错误。

**验收要求**：阻塞资料刷新后退出页面不得回写；阻塞且忽略取消的保存请求，取消后不得调用资料发布回调、不得改变资料、不得显示错误；资料专项与全量 iOS 单测通过。

**状态**：已完成，`ProfilePermissionTests` 20/20、全量 `GoHomeShellTests` 165/165，`git diff --check` 通过。

### GH-100 家庭成员页异步操作未绑定页面生命周期

**发现**：家庭成员刷新、邀请码生成/撤销以及成员移除、退出家庭、转让创建者均可能在页面退出后继续运行；服务端晚返回时仍可能修改成员列表、邀请码或触发家庭切换回调。

**根因**：成员刷新任务由 ViewModel 临时创建，页面操作任务由未保存的 `Task` 创建；各服务端调用返回后没有统一检查取消状态。

**处理**：ViewModel 持有并取消成员刷新任务；家庭页持有刷新以外的操作任务并在退出时取消；所有家庭操作在服务端返回后检查取消，取消不修改列表、不回调家庭切换、不显示错误。

**验收要求**：阻塞刷新、邀请码和成员操作后退出页面，确认取消后状态不变、处理中标记清除、没有错误提示或家庭切换回调；家庭权限专项与全量 iOS 单测通过。

**状态**：已完成，`ProfilePermissionTests` 23/23、全量 `GoHomeShellTests` 168/168，`git diff --check` 通过。

### GH-101 照护资料保存任务未绑定编辑弹层生命周期

**发现**：照护资料编辑弹层通过临时 `Task` 保存，退出弹层后服务端晚返回仍可能写入老人资料；取消请求也可能被当成普通失败提示。

**根因**：编辑弹层没有持有保存任务，ViewModel 在更新照护资料返回后没有检查取消状态。

**处理**：编辑弹层持有并在退出时取消保存任务；ViewModel 在服务端返回后、写入资料前检查取消，取消不更新资料、不显示错误，真实失败继续提示。

**验收要求**：阻塞且忽略取消的照护资料保存，退出编辑弹层后不得写回、不得显示错误，处理中状态必须清除；资料专项与全量 iOS 单测通过。

**状态**：已完成，`ProfilePermissionTests` 24/24、全量 `GoHomeShellTests` 169/169，`git diff --check` 通过。

### GH-102 设备配置和解绑任务退出页面后仍可能提交旧结果

**发现**：摄像头搜索、保存、删除、编辑和盒子解绑均可能由页面外的临时任务继续执行；摄像头保存事务在保存后无条件请求盒子同步并完成退出，页面已经离开时仍可能触发旧回调。

**根因**：设备表单没有持有搜索/保存任务；设备变更和解绑在服务端返回后没有统一取消检查；配置事务没有在保存、同步和完成之间定义取消边界。

**处理**：设备表单和管理页持有对应任务并在退出时取消；增删改和解绑在写入可见状态前检查取消；配置事务在云端保存后、盒子同步后再次检查取消，取消不关闭页面、不发布旧状态。

**验收要求**：分别阻塞搜索、保存、删除、编辑和解绑后退出页面，确认无迟到状态回写、无错误提示、无旧页面完成回调；设备权限专项、配置事务专项和全量 iOS 单测通过。

**状态**：已完成，`ProfilePermissionTests` 25/25、`OnboardingModelTests` 13/13、全量 `GoHomeShellTests` 171/171，`git diff --check` 通过。

### GH-103 已取消的家庭刷新可能清除新一轮刷新状态

**发现**：用户取消家庭成员刷新后立即重试，旧任务即使已经取消，仍可能在晚结束的 `defer` 中清空新任务的 `familyActionID` 和任务句柄，造成新刷新仍在运行但页面可重复操作。

**根因**：刷新清理只依赖共享状态，没有区分任务代次；旧任务和新任务写入同一组状态。

**处理**：为家庭刷新增加单调递增代次；结果提交、错误提示和结束清理都必须命中当前代次，取消会立即使旧代次失效。

**验收要求**：第一轮刷新忽略取消并晚结束，第二轮刷新仍保持处理中状态且最终只提交第二轮结果；资料专项和全量 iOS 单测通过。

**状态**：已完成，`ProfilePermissionTests` 26/26、全量 `GoHomeShellTests` 172/172，`git diff --check` 通过。

### GH-078 正式 App 默认头像资源未进入用户资料页面

**发现**：正式仓库已有 `assets/images/avatar.jpg`，但 `ios-shell/project.yml` 没有把它加入 App Bundle；`AccountAvatar` 在没有远程头像时只显示 SF Symbol，首页“我的”入口和个人资料编辑页因此没有产品默认头像。

**根因**：资源清单与头像展示契约不一致。远程头像是用户可选数据，不能替代本地默认头像；本地 JPEG 也不能使用 `UIImage(named:)` 假定 Asset Catalog 查找。

**处理**：将唯一正式工程的 `avatar.jpg` 加入 Copy Bundle Resources；`AccountAvatar` 沿用共享 `GoHomeImageResource.loadJPEG(named:)` 读取默认头像，只有本地资源确实不可用时才显示系统图标。远程头像存在时仍优先显示远程头像，头像上传、保存和服务端资产路径不变；未创建副本、第二资源目录或兼容入口。

**验证**：产品图片资源单元测试 `133/133` 通过，确认 `avatar` 与四张首页/文章图片均能从 App Bundle 解码；资料页 UI 测试 `8/8` 通过，创建者和成员入口均能发现 `profile-account-avatar`；已生成唯一 `GoHomeShell.xcodeproj`，编译日志确认 `avatar.jpg` 被复制进 App。保留下一次 TestFlight 真机查看默认头像和远程头像切换验收。

**状态**：待实机验收。不得修改 Bundle ID、旧 WebView 工程、盒子数据、云端业务数据或用户资料。

### GH-079 绑定盒子后家庭固定位置读取失败被静默跳过

**发现**：绑定盒子成功后，客户端需要读取照护资料中的家庭固定位置。原实现读取失败就直接调用 `onComplete()`，用户进入主界面时盒子已经绑定，但首页距离无法计算、社区服务无法按家庭位置打开，界面没有说明是哪一步未完成。

**根因**：设备绑定成功和家庭位置设置被当成一个无错误的流程；位置资料请求失败没有独立状态，也没有重试入口。位置确认弹层还允许用户直接下滑关闭，可能绕过明确的“确认”或“稍后设置”动作。

**处理**：绑定成功后保留设备绑定结果；家庭资料读取失败时停留在绑定流程，显示结构化错误并提供“重新读取家庭位置”，不重复绑定、不删除盒子、不使用手机当前位置伪造家庭坐标。读取到已有坐标时直接完成；没有坐标时继续进入一次性确认弹层，保留“稍后设置”作为用户明确选择。弹层改为只能通过“设为家庭位置”或“稍后设置”离开，避免误滑关闭。

**验证**：`OnboardingModelTests` 9/9 通过，其中 3 项专项覆盖资料读取失败重试、已有坐标直接完成和缺少坐标进入确认。正式 iOS 单元测试 136/136 通过。家庭位置缺失/已有位置的首页与社区入口、五个主 Tab、守护页内容切换专项 UI 均通过；整组 UI 首次运行时两项 Runner 启动超时，分别独立重跑后均通过，未出现业务断言失败。

**状态**：已完成，待后续 TestFlight 统一真机验收绑定后首次位置确认。

### GH-080 摄像头找不到所属盒子时错误回退到第一台盒子

**发现**：设备与守护页面编辑摄像头时，如果摄像头的 `device_id` 暂时不在当前家庭绑定列表，原实现会静默选用第一台盒子。多盒子家庭中这会把本摄像头的局域网搜索和配置同步发到错误设备，导致状态错乱、实时画面和配置对应关系不可信。

**根因**：摄像头到盒子的关联解析使用了“精确匹配，否则第一项”的隐式回退，掩盖了云端绑定数据不一致。

**处理**：关联解析只接受 `camera.device_id == binding.device_id` 的精确匹配；未匹配时保留摄像头记录但显示“盒子未绑定”，不提供指向错误盒子的编辑入口，不触发错误盒子的同步唤醒。

**验证**：`ProfilePermissionTests` 12/12 通过，新增双盒子精确命中和未匹配返回空专项。正式 iOS 单元测试 137/137 通过；`ProfileFlowTests` 8/8 通过，创建者/成员设备管理页面、编辑和删除入口均可正常加载。

**状态**：已完成，待包含云端绑定数据不一致场景的真机验收。

### GH-081 新设备操作会被上一项完成提示的延迟任务清空

**发现**：设备配置完成后，页面会短暂显示“摄像头已在线”或“设备配置已更新”。如果用户在这段时间内开始下一次保存、删除或解绑，上一项操作留下的 1.5 秒异步清除任务可能把新操作的“正在保存/正在删除”状态清掉，造成操作实际进行但页面没有等待状态。

**根因**：完成提示的异步清除任务没有被保存、没有和当前状态建立对应关系，也没有在新设备操作开始时取消。上一项设备状态轮询也要等到新请求完成后才被替换，期间仍可覆盖新操作状态。

**处理**：设备操作统一通过单一入口启动，立即取消上一项状态轮询和完成清除任务；完成提示清除任务由 ViewModel 持有，只有在当前状态仍是它创建的同一条完成消息时才清除。ViewModel 销毁时同步取消全部设备状态任务。

**验证**：并发专项覆盖两条竞态：首次摄像头同步成功后立即开始第二次保存，跨过旧的 1.5 秒清除窗口后仍保持“正在保存摄像头设置”；首次轮询尚在等待时开始第二次保存，旧轮询不再继续请求或覆盖新状态。`ProfilePermissionTests` 14/14、正式 iOS 单元测试 139/139 全部通过，`git diff --check` 通过。

**状态**：已完成，待下一次 TestFlight 统一真机验收连续设备操作的等待状态。

### GH-082 摄像头保存页面先退出再唤醒盒子配置同步

**发现**：添加或编辑摄像头时，云端保存成功会由父页面立即退出；共享表单恢复执行后才尝试唤醒局域网盒子。页面退出会触发表单停止发现服务并清空盒子端点，因此唤醒请求经常无法发出，只能等待盒子后台轮询，放大“长期配置中”和实时画面迟迟不可用的延迟。

**根因**：云端持久化、盒子同步唤醒和页面完成跳转分散在父页面与表单两层，执行顺序与页面生命周期相反；唤醒错误又使用 `try?` 作为非权威局域网加速结果，不会阻止已成功的云端配置。

**处理**：建立唯一摄像头配置提交事务，严格执行“云端持久化成功 -> 使用仍有效的已发现端点请求盒子立即同步 -> 页面完成跳转”。管理页和首次引导页的保存回调只负责云端持久化，不再自行退出；局域网唤醒仍只是云端配置同步的加速信号，等待上限为 2 秒，不会把已成功保存的配置回滚、长时间阻塞页面或诱导用户重复创建。

**验证**：专项固定成功路径的三阶段顺序，并确认云端保存失败时既不唤醒盒子也不退出页面。`OnboardingModelTests` 11/11、设备管理 `ProfileFlowTests` 8/8、首次引导 `OnboardingFlowTests` 4/4、正式 iOS 单元测试 141/141 全部通过。

**状态**：已完成，待下一次 TestFlight 在同一局域网实测添加、编辑后的盒子同步耗时。

### GH-083 云端可绑定设备加载失败被伪装成没有设备

**发现**：绑定盒子页面请求云端可绑定设备失败时，客户端直接把异常转成空数组。页面随后显示“没有发现设备，请确认盒子已通电并连接同一 Wi-Fi”，把服务器超时、鉴权或手机公网故障错误归因到盒子和局域网；刷新失败还会清空上一份已成功加载的设备列表。

**根因**：云端设备加载只有数组，没有成功、失败和刷新中的明确状态；`try?` 同时吞掉错误与数据来源，空结果无法区分“服务成功返回零台”和“请求根本失败”。

**处理**：建立单一云端设备加载状态。成功时原子替换列表并清除错误；刷新开始只清除旧错误，不清空已有设备；失败时保留上一份成功列表，并明确显示“设备列表未能更新”和重试提示。局域网 Bonjour 发现继续独立工作，不因云端请求失败被抹除。

**验证**：专项覆盖成功加载、失败保留上一份列表、错误文案和重新加载时不清空设备。`OnboardingModelTests` 12/12、绑定引导 `OnboardingFlowTests` 4/4、正式 iOS 单元测试 142/142 全部通过，`git diff --check` 通过。

**状态**：已完成，待下一次 TestFlight 真机确认断网、云端失败与局域网盒子仍可发现三种状态文案。

### GH-084 正式主界面缺少家庭数据时展示预览假数据

**发现**：App 路由进入主界面后，如果启动数据没有 `active_family_id`，正式代码会直接展示 `MainTabView.preview`；如果存在活动家庭 ID 但家庭列表没有对应记录，又会临时合成名为“我的家庭”的家庭继续运行。生产用户因此可能看到测试预览内容或无法归属的页面，而不是云端数据契约错误。

**根因**：正式主界面装配与 UI 测试夹具共用隐式 fallback，没有在根路由验证“活动家庭 ID 必须精确命中家庭列表”。测试便利逻辑越过了生产数据边界。

**处理**：建立唯一主界面解析契约。只有启动数据、活动家庭 ID 和对应家庭三者完整且精确匹配时才装配真实主 Tab；正式运行缺失任一项时显示明确的家庭数据错误和重新加载入口，不创建合成家庭、不展示预览内容。`MainTabView.preview` 仅允许显式 `-uiTestState` 进程使用。

**验证**：专项覆盖有效家庭、缺少活动家庭、活动家庭不匹配和 UI 测试预览四种分支。`AppSmokeTests` 4/4、五 Tab `TabStateTests` 8/8、正式 iOS 单元测试 143/143 全部通过，`git diff --check` 通过。

**状态**：已完成，待下一次 TestFlight 真机确认异常家庭数据只显示重试页、不进入任何假内容页面。

### GH-076 App 客户端显示 FPS 在停帧和重连期间保留旧值

**发现**：正式 iOS 守护页的 FPS 来源是 `RTCVideoRenderer` 实际交付给显示目标的帧回调，口径正确；但滚动窗口只在新帧到达时计算。视频停帧后没有回调触发归零，重连循环也没有重置窗口，因此“连接中”或无新帧期间可能继续显示上一会话的正 FPS。

**处理方案**：滚动窗口增加按单调时钟计算当前值的唯一入口；帧看门任务以不高于 0.5 秒的节奏刷新客户端显示 FPS，无新展示帧超过窗口后归零。每次会话开始和失败重连都原子清空窗口与 UI 值，禁止跨会话合并样本。盒子 `output_fps` 继续只表示端侧成品输出，不替代客户端展示 FPS。

**验收要求**：单元测试覆盖滚动窗口衰减、失败重连立即归零和展示标签语义；正式 iOS 单元/UI 回归通过。不得修改 Bundle ID、旧 WebView 工程、盒子数据、云端业务数据或用户资料。

**状态（2026-08-08）**：提交 `42b5141` 已完成守护专项 `16/16`、正式 iOS 单元测试 `132/132`、UI 测试 `25/25` 和 `git diff --check`，并已推送 `origin/main`。本项不涉及盒子或云端部署；代码与自动回归已完成，等待下一次正式 TestFlight 构建后真机确认停帧归零和重连不继承旧会话 FPS。

### GH-077 正式 App 已打包的本地图片全部显示为占位图

**发现**：正式 iOS 工程将首页 Hero 和文章本地图片作为原始 `.jpg` 资源加入 App Bundle，但共享 `GoHomeLocalImage` 使用 `UIImage(named:)` 按 Asset Catalog 名称查找不带扩展名的资源。UI 全量截图因此显示灰蓝色图片占位块，尽管对应 JPEG 已存在并进入 Copy Bundle Resources。

**处理方案**：本地内容图片统一为明确的 Bundle JPEG 契约，唯一加载入口使用 `Bundle.url(forResource:withExtension:)` 取得原始文件并解码，不复制图片、不增加第二套资源目录，也不把加载失败伪装成成功。单元测试固定核验首页和文章使用的四个生产 JPEG 均能从 App Bundle 解析。

**验收要求**：资源专项、正式 iOS 单元/UI 回归通过；新的首页截图必须显示真实 Hero 和文章图而非占位块。不得修改远端内容图片代理、用户媒体、App 图标、Bundle ID 或盒子/云端数据。

**状态（2026-08-08）**：资源专项确认四个生产 JPEG 均可从 App Bundle 解码；AppSurface 截图确认首页 Hero 和首篇文章已显示真实图片，不再出现本地资源占位块。正式 iOS 单元测试 `133/133`、UI 测试 `25/25` 全部通过。待提交、推送并随下一次 TestFlight 构建进行真机确认。

### GH-075 上传代理保留未使用的 time 导入

**发现**：`app/upload_agent.py` 导入 `time` 后没有任何运行时引用。它不改变行为，但会增加无意义的依赖表面，掩盖真正的上传时序实现，并违反盒子运行时代码不保留垃圾的要求。

**处理方案**：删除未使用导入，不新增替代层或兜底逻辑。以 Python 编译、边缘专项回归和差异检查确认上传代理行为未被改动。

**验收要求**：上传队列、租约恢复、锁重试、生命周期和完整边缘回归通过；不得删除历史表、摄像头配置、校准基线、COS 资产或用户资料。

**部署证据（2026-08-08）**：提交 `c451cc3` 已通过盒子全部 `61/61` 专项、Python 编译和 `git diff --check`，并由唯一部署入口安装到 `192.168.1.12`。远端 `.release-commit` 为 `c451cc3f387966eec44dd7346271b1e028a0b91e`；服务 `active`、`SubState=running`、`NRestarts=0`、主进程退出码 `0`。部署后 Pose、Object 和人员分割均 `ready`，推理失败数均为 `0`；两路摄像头均 `streaming`，解码/实时投递约 `15.06/15.06 FPS`，读取失败 `0`；最近十分钟 warning 为空。当前 31 路背景状态为 `revalidating/person_present`，属于有人时拒绝自动重新校准的既有保护状态，不改变校准基线。历史表、摄像头配置、校准基线、COS 资产和用户资料均未修改。GitHub 推送因本机到 `github.com:443` 建连超时待网络恢复后重试。

**状态**：已完成。不得删除历史表、摄像头配置、校准基线、COS 资产或用户资料。
